---
phase: 10-qr-code-poster-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudfunctions/works/index.js
  - cloudfunctions/works/config.json
  - miniprogram/services/works.js
  - miniprogram/pages/works/detail.js
  - miniprogram/pages/works/detail.wxml
  - miniprogram/pages/works/detail.wxss
  - miniprogram/app.json
autonomous: true
requirements:
  - GROW-01
  - GROW-02

must_haves:
  truths:
    - "化妆师在作品详情页的 detail-action 区域看到'生成海报'按钮（客户端不显示）"
    - "点击'生成海报'跳转到 pages/works/poster?id={workId}"
    - "works 云函数 getShareQRCode action 可生成小程序码并缓存到云存储"
    - "QR 码 scene=workId，扫码跳转 pages/works/detail"
    - "同一作品重复调用返回缓存的 QR 码（不重复生成）"
  artifacts:
    - path: "cloudfunctions/works/index.js"
      provides: "新增 getShareQRCode action（QR 码生成 + 云存储缓存）"
      contains: "getShareQRCode"
    - path: "cloudfunctions/works/config.json"
      provides: "wxacode API 权限声明"
      contains: "wxacode.getUnlimitedQRCode"
    - path: "miniprogram/services/works.js"
      provides: "客户端 getShareQRCode(id) 方法"
      contains: "getShareQRCode"
    - path: "miniprogram/pages/works/detail.wxml"
      provides: "'生成海报'按钮（artist-only 条件渲染）"
      contains: "goToPoster"
    - path: "miniprogram/pages/works/detail.js"
      provides: "isArtist 判断 + goToPoster 方法"
      contains: "goToPoster"
  key_links:
    - from: "miniprogram/pages/works/detail.wxml"
      to: "pages/works/poster"
      via: "navigateTo with workId per D-03"
      pattern: "goToPoster"
    - from: "miniprogram/services/works.js"
      to: "cloudfunctions/works/index.js"
      via: "callCloudFunction('works', { action: 'getShareQRCode' }) per D-27"
      pattern: "getShareQRCode"
    - from: "cloudfunctions/works/index.js"
      to: "cloud.openapi.wxacode.getUnlimitedQRCode"
      via: "QR 码生成 per D-05"
      pattern: "getUnlimitedQRCode"
    - from: "cloudfunctions/works/index.js"
      to: "云存储 qrcodes/{workId}.png"
      via: "QR 缓存读写 per D-07/D-25"
      pattern: "qrcodes"
---

<objective>
构建海报生成后端基础设施（works 云函数 getShareQRCode action + 客户端服务层），并在作品详情页添加"生成海报"入口按钮。

Purpose: 这是海报功能的数据管道——小程序码生成、缓存、客户端调用链路。同时建立详情页到海报页的导航入口，只有化妆师能看到。

Output: works 云函数新增 getShareQRCode action + config.json 权限更新 + works.js 新增 getShareQRCode 方法 + detail 页面按钮 + app.json 路由注册
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-qr-code-poster-generation/10-CONTEXT.md

@cloudfunctions/works/index.js
@cloudfunctions/works/config.json
@cloudfunctions/shared/auth.js
@miniprogram/services/works.js
@miniprogram/services/api.js
@miniprogram/pages/works/detail.js
@miniprogram/pages/works/detail.wxml
@miniprogram/pages/works/detail.wxss
@miniprogram/app.json

<interfaces>
<!-- Executor needs these existing interfaces — no codebase exploration needed -->

From cloudfunctions/works/index.js — action-dispatcher pattern:
```javascript
// Line 8-109: exports.main with switch(event.action)
// Existing actions: list, detail, create, update, delete
// New action 'getShareQRCode' goes before the default case
```

From cloudfunctions/shared/auth.js — requireArtist:
```javascript
const ARTIST_OPENID = 'oDmtI48gecuF3n9OkEBJN91BJliI'
function requireArtist(wxContext) {
  if (wxContext.OPENID === ARTIST_OPENID) return { ok: true }
  return { ok: false, response: { errCode: -1, errMsg: '无权限操作' } }
}
```

From miniprogram/services/works.js — service module pattern:
```javascript
const { callCloudFunction } = require('./api')
// Each function: callCloudFunction('works', { action: '...', ...params })
//   .then(result => { if (result.errCode !== 0) throw...; return result.data })
```

From miniprogram/pages/works/detail.js — current page:
```javascript
// Line 1-71: worksService + CATEGORY_MAP
// data: { work, loading, currentImage }
// onLoad → loadDetail(id)
// goToBooking, onSliderFullscreen, onShareAppMessage
// Need to add: authService import + isArtist check + goToPoster
```

From miniprogram/pages/works/detail.wxml — detail-action area (line 67-72):
```xml
<view class="detail-action">
  <button class="action-book" bindtap="goToBooking">预约此妆容</button>
  <button class="action-chat tappable" open-type="contact">
    <text class="action-chat-text">咨询</text>
  </button>
</view>
```

From miniprogram/pages/works/detail.wxss — action styles:
```css
.detail-action { display: flex; gap: 16rpx; margin-top: 44rpx; }
.action-book { flex: 1; height: 100rpx; ... background-color: var(--accent); ... }
.action-chat { width: 100rpx; height: 100rpx; border: 2rpx solid var(--separator); ... }
```

From miniprogram/services/auth.js — client-side isArtist:
```javascript
const isArtist = () => {
  if (!_userInfo || !_userInfo.openid) return false
  if (!ARTIST_OPENID) return false
  return _userInfo.openid === ARTIST_OPENID
}
```

From miniprogram/app.json — current pages:
```json
{
  "pages": [
    "pages/index/index",
    "pages/works/list", "pages/works/detail", "pages/works/compare",
    "pages/services/list",
    "pages/booking/create",
    "pages/profile/index", "pages/profile/history",
    "pages/review/create",
    "pages/demo-ui/demo-ui"
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getShareQRCode 云函数 action + 服务层方法</name>
  <files>
    cloudfunctions/works/index.js
    cloudfunctions/works/config.json
    miniprogram/services/works.js
  </files>
  <action>
    在 works 云函数新增 `getShareQRCode` action，并在客户端 works.js 服务层新增对应调用方法。

    **1. 修改 `cloudfunctions/works/config.json`：**

    添加 wxacode API 权限声明（per D-05）：
    ```json
    {
      "permissions": {
        "openapi": [
          "wxacode.getUnlimitedQRCode"
        ]
      }
    }
    ```

    **2. 修改 `cloudfunctions/works/index.js`：**

    在 `default` case 之前（line 107 前）新增 `getShareQRCode` action（per D-04, D-05, D-06, D-07, D-24, D-25, D-26）：

    ```javascript
    case 'getShareQRCode': {
      const { id } = event
      try {
        // 权限验证 per D-26
        const authCheck = requireArtist(wxContext)
        if (!authCheck.ok) return authCheck.response

        // 尝试从云存储获取已缓存的 QR 码 per D-07
        const cloudPath = 'qrcodes/' + id + '.png'
        try {
          const downloadRes = await cloud.downloadFile({
            fileID: cloudPath
          })
          // 缓存命中 — 直接返回 fileID
          return { errCode: 0, data: { fileID: downloadRes.fileID } }
        } catch (cacheErr) {
          // 缓存未命中 — 继续生成
        }

        // 生成小程序码 per D-05/D-06
        const qrResult = await cloud.openapi.wxacode.getUnlimitedQRCode({
          scene: id,
          page: 'pages/works/detail',
          width: 280,
          autoColor: false,
          lineColor: { r: 156, g: 122, b: 90 },
          isHyaline: false
        })

        // 上传到云存储 per D-07/D-25
        const uploadRes = await cloud.uploadFile({
          cloudPath: cloudPath,
          fileContent: qrResult
        })

        return { errCode: 0, data: { fileID: uploadRes.fileID } }
      } catch (error) {
        console.error('生成小程序码失败:', error)
        return { errCode: -1, errMsg: '生成小程序码失败' }
      }
    }
    ```

    注意：
    - `qrResult` 是 Buffer 类型，可直接传给 `cloud.uploadFile` 的 `fileContent`（per CONTEXT code example）
    - `cloud.downloadFile` 失败时（文件不存在）会抛异常，用 try-catch 捕获后继续生成
    - 权限验证使用 `requireArtist(wxContext)`（per D-26）

    **3. 修改 `miniprogram/services/works.js`：**

    在 `module.exports` 之前新增 `getShareQRCode` 方法（per D-27）：

    ```javascript
    const getShareQRCode = async (id) => {
      const result = await callCloudFunction('works', {
        action: 'getShareQRCode',
        id
      })
      if (result.errCode !== 0) {
        throw new Error(result.errMsg || '获取小程序码失败')
      }
      return result.data
    }
    ```

    在 `module.exports` 中添加 `getShareQRCode`：
    ```javascript
    module.exports = {
      getWorksList,
      getWorkDetail,
      createWork,
      updateWork,
      deleteWork,
      getCategories,
      getShareQRCode
    }
    ```
  </action>
  <verify>
    <automated>grep -q "getShareQRCode" cloudfunctions/works/index.js && grep -q "getUnlimitedQRCode" cloudfunctions/works/index.js && grep -q "wxacode.getUnlimitedQRCode" cloudfunctions/works/config.json && grep -q "getShareQRCode" miniprogram/services/works.js && echo "Cloud function + service layer complete"</automated>
  </verify>
  <done>
    - works 云函数新增 getShareQRCode action（per D-04/D-24）
    - requireArtist 权限验证（per D-26）
    - 使用 cloud.openapi.wxacode.getUnlimitedQRCode 生成小程序码（per D-05/D-06）
    - QR 码缓存到云存储 qrcodes/{workId}.png（per D-07/D-25）
    - config.json 声明 wxacode.getUnlimitedQRCode openapi 权限
    - works.js 新增 getShareQRCode(id) 方法（per D-27）
  </done>
</task>

<task type="auto">
  <name>Task 2: 作品详情页"生成海报"按钮 + 海报页路由</name>
  <files>
    miniprogram/pages/works/detail.js
    miniprogram/pages/works/detail.wxml
    miniprogram/pages/works/detail.wxss
    miniprogram/app.json
  </files>
  <action>
    在作品详情页 detail-action 区域添加"生成海报"按钮（仅化妆师可见），跳转到 poster 页面。注册 poster 页面路由。

    **1. 修改 `miniprogram/pages/works/detail.js`：**

    - 顶部新增引用（line 1-2 之间添加）：
      ```javascript
      const authService = require('../../services/auth')
      ```

    - data 初始值添加 `isArtist: false`：
      ```javascript
      data: {
        work: null,
        loading: true,
        currentImage: 0,
        isArtist: false
      },
      ```

    - `onLoad` 方法中，在 `this.loadDetail(options.id)` 之前添加 isArtist 判断（per D-02）：
      ```javascript
      onLoad: function (options) {
        this.setData({ isArtist: authService.isArtist() })
        if (options.id) {
      ```

    - 添加 `goToPoster` 方法（per D-03）：
      ```javascript
      goToPoster: function () {
        const work = this.data.work
        if (!work || !work._id) return
        wx.navigateTo({
          url: '/pages/works/poster?id=' + work._id
        })
      },
      ```

      放在 `goToBooking` 方法之后。

    **2. 修改 `miniprogram/pages/works/detail.wxml`：**

    在 `detail-action` 区域（line 67-72），添加"生成海报"按钮（per D-01: btn-outline 样式，位于"预约"按钮上方）。

    替换整个 detail-action 区域：
    ```xml
    <view class="detail-action">
      <button class="action-poster btn-outline" wx:if="{{isArtist}}" bindtap="goToPoster">生成海报</button>
      <button class="action-book" bindtap="goToBooking">预约此妆容</button>
      <button class="action-chat tappable" open-type="contact">
        <text class="action-chat-text">咨询</text>
      </button>
    </view>
    ```

    注意：`wx:if="{{isArtist}}"` 确保仅化妆师可见（per D-02）。使用全局 `btn-outline` 样式类。按钮顺序：生成海报 → 预约 → 咨询。

    **3. 修改 `miniprogram/pages/works/detail.wxss`：**

    在文件末尾（line 196 后）添加海报按钮样式：

    ```css
    .action-poster {
      width: 100%;
      margin-bottom: 16rpx;
    }
    ```

    btn-outline 全局样式已定义在 app.wxss（border: accent, color: accent, transparent bg）。这里只需要控制宽度和间距。

    **4. 修改 `miniprogram/app.json`：**

    在 pages 数组中 `pages/works/compare` 之后添加 poster 页面（per D-28）：

    ```json
    "pages": [
      "pages/index/index",
      "pages/works/list",
      "pages/works/detail",
      "pages/works/compare",
      "pages/works/poster",
      "pages/services/list",
      "pages/booking/create",
      "pages/profile/index",
      "pages/profile/history",
      "pages/review/create",
      "pages/demo-ui/demo-ui"
    ]
    ```
  </action>
  <verify>
    <automated>grep -q "goToPoster" miniprogram/pages/works/detail.js && grep -q "isArtist" miniprogram/pages/works/detail.js && grep -q "goToPoster" miniprogram/pages/works/detail.wxml && grep -q "isArtist" miniprogram/pages/works/detail.wxml && grep -q "action-poster" miniprogram/pages/works/detail.wxss && grep -q "pages/works/poster" miniprogram/app.json && echo "Detail page poster entry + route complete"</automated>
  </verify>
  <done>
    - detail.js 引入 authService，添加 isArtist data 字段和 goToPoster 方法（per D-02/D-03）
    - detail.wxml detail-action 区域新增"生成海报"按钮，wx:if=isArtist 控制可见性（per D-01/D-02）
    - detail.wxss 新增 action-poster 样式
    - app.json 注册 pages/works/poster 路由（per D-28）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→works getShareQRCode | 非化妆师可能尝试调用 getShareQRCode |
| works 云函数→wxacode API | 微信服务端 API 调用，消耗配额 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | getShareQRCode | mitigate | requireArtist 服务端验证 per D-26，防止非化妆师调用 |
| T-10-02 | Elevation of Privilege | detail page button | mitigate | 客户端 isArtist 条件渲染 per D-02，按钮不显示。服务端也有 requireArtist 双重保障 |
| T-10-03 | Denial of Service | wxacode API | accept | 单化妆师项目，QR 码缓存 per D-07 避免重复调用。云存储缓存后不再消耗 API 配额 |
</threat_model>

<verification>
1. 云函数：
   - cloudfunctions/works/index.js 含 getShareQRCode case
   - 含 requireArtist 权限验证
   - 含 cloud.openapi.wxacode.getUnlimitedQRCode 调用（参数 per D-06）
   - 含云存储缓存逻辑（downloadFile → uploadFile → qrcodes/ 路径 per D-07/D-25）
   - cloudfunctions/works/config.json 含 wxacode.getUnlimitedQRCode 权限
2. 服务层：
   - miniprogram/services/works.js 导出 getShareQRCode
3. 详情页：
   - detail.js 含 authService 引用、isArtist data、goToPoster 方法
   - detail.wxml detail-action 含"生成海报"按钮（wx:if=isArtist）
   - detail.wxss 含 action-poster 样式
4. 路由：
   - app.json pages 含 pages/works/poster
</verification>

<success_criteria>
- works 云函数 getShareQRCode action 正确生成小程序码并缓存到云存储
- QR 码参数正确：scene=workId, page=pages/works/detail, 品牌色线条（per D-06）
- 同一作品重复调用返回缓存的 QR 码（per D-07）
- 化妆师在作品详情页看到"生成海报"按钮，客户端不显示（per D-01/D-02）
- 点击按钮跳转到 pages/works/poster?id={workId}（per D-03）
</success_criteria>

<output>
After completion, create `.planning/phases/10-qr-code-poster-generation/10-01-SUMMARY.md`
</output>
