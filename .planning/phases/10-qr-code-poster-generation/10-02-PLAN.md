---
phase: 10-qr-code-poster-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - miniprogram/pages/works/poster.js
  - miniprogram/pages/works/poster.wxml
  - miniprogram/pages/works/poster.wxss
  - miniprogram/pages/works/poster.json
autonomous: true
requirements:
  - GROW-01
  - GROW-02
  - GROW-03

must_haves:
  truths:
    - "化妆师打开海报页后看到作品主图 + 个人信息 + 小程序码的完整海报预览"
    - "海报中的小程序码扫码后直接进入对应作品详情页"
    - "化妆师点击'保存到相册'按钮，海报保存到手机相册"
    - "首次保存时弹出相册权限授权，拒绝后引导到设置页（per D-19）"
    - "图片加载失败时显示占位灰色区域，不阻塞海报生成（per D-30）"
    - "QR 码生成失败时显示错误提示和重试按钮（per D-29）"
  artifacts:
    - path: "miniprogram/pages/works/poster.js"
      provides: "海报页完整逻辑：并行数据加载 + Canvas 2D 渲染 + 导出 + 保存相册"
      min_lines: 200
    - path: "miniprogram/pages/works/poster.wxml"
      provides: "海报页模板：离屏 canvas + 海报预览图 + 保存按钮 + 关闭按钮 + loading"
      contains: "posterCanvas"
    - path: "miniprogram/pages/works/poster.wxss"
      provides: "海报页样式：深色 overlay + 居中预览 + 操作栏"
    - path: "miniprogram/pages/works/poster.json"
      provides: "页面配置：t-loading 组件注册 + 自定义导航栏"
  key_links:
    - from: "miniprogram/pages/works/poster.js"
      to: "services/works.js"
      via: "worksService.getWorkDetail + worksService.getShareQRCode per D-23/D-27"
      pattern: "worksService\\.get"
    - from: "miniprogram/pages/works/poster.js"
      to: "services/profile.js"
      via: "profileService.getArtistProfile per D-23 (获取头像和名字)"
      pattern: "profileService"
    - from: "miniprogram/pages/works/poster.js"
      to: "Canvas 2D API"
      via: "canvas.getContext('2d') + createImage + drawImage per D-08"
      pattern: "getContext.*2d"
    - from: "miniprogram/pages/works/poster.js"
      to: "wx.canvasToTempFilePath"
      via: "Canvas 导出为临时图片 per D-17"
      pattern: "canvasToTempFilePath"
    - from: "miniprogram/pages/works/poster.js"
      to: "wx.saveImageToPhotosAlbum"
      via: "保存到相册 per D-18"
      pattern: "saveImageToPhotosAlbum"
---

<objective>
创建海报生成页面，使用 Canvas 2D API 渲染包含作品图片、化妆师信息和小程序码的分享海报，支持保存到手机相册。

Purpose: 这是海报功能的完整用户链路——从"进入海报页"到"看到生成的海报"到"保存到相册"。Canvas 2D 渲染是本阶段技术风险最高的部分（DPR 处理、多图并行加载、精确布局）。

Output: poster 页面 4 文件（js, wxml, wxss, json）——完整的海报生成与保存体验
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-qr-code-poster-generation/10-CONTEXT.md
@.planning/phases/10-qr-code-poster-generation/10-01-SUMMARY.md

@miniprogram/pages/works/compare.js
@miniprogram/pages/works/compare.wxml
@miniprogram/pages/works/compare.json
@miniprogram/services/works.js
@miniprogram/services/profile.js
@miniprogram/services/api.js
@miniprogram/services/auth.js
@miniprogram/app.wxss

<interfaces>
<!-- Executor needs these interfaces from Plan 01 and existing code -->

From miniprogram/services/works.js (Plan 01 adds getShareQRCode):
```javascript
// getShareQRCode(id) → { fileID: string }
// getWorkDetail(id) → { _id, title, images[], category, description, ... }
```

From miniprogram/services/profile.js:
```javascript
// getArtistProfile(forceRefresh?) → { name, avatar, bio, style_tags[], ... }
```

From miniprogram/services/auth.js:
```javascript
// isArtist() → boolean
```

From miniprogram/pages/works/compare.js — fullscreen page pattern:
```javascript
// onLoad: get statusBarHeight + load data by id
// onBack: wx.navigateBack()
// Custom navbar with statusBarHeight padding
```

From miniprogram/app.wxss — global styles:
```css
--accent: #9C7A5A;
--accent-light: #F7F2EC;
--bg-elevated: #FDFCF9;
--text-primary: #1F1B18;
--text-secondary: rgba(0, 0, 0, 0.42);
--text-tertiary: rgba(0, 0, 0, 0.22);
--radius-md: 14rpx;
--radius-lg: 20rpx;
--fs-xs: 22rpx;
--fs-sm: 26rpx;
--fs-base: 32rpx;
--page-pad: 32rpx;
--icon-close: url("data:image/svg+xml,...");
.btn-primary { ... }
.btn-outline { ... }
```

Canvas 2D API Pattern (from CONTEXT):
```javascript
// 获取 canvas 实例
const query = wx.createSelectorQuery()
query.select('#posterCanvas')
  .fields({ node: true, size: true })
  .exec((res) => {
    const canvas = res[0].node
    const ctx = canvas.getContext('2d')
    const dpr = wx.getWindowInfo().pixelRatio
    canvas.width = 750 * dpr
    canvas.height = 1060 * dpr
    ctx.scale(dpr, dpr)
    // 绘制逻辑...
  })

// 导出 — Canvas 2D 使用 canvas 参数（不是 canvasId）
wx.canvasToTempFilePath({
  canvas: canvas,
  x: 0, y: 0,
  width: 750 * dpr, height: 1060 * dpr,
  destWidth: 750 * dpr, destHeight: 1060 * dpr,
  fileType: 'png',
  quality: 1,
  success: (res) => { /* res.tempFilePath */ }
})
```

Cloud function wxacode result (from CONTEXT):
```javascript
// cloud.openapi.wxacode.getUnlimitedQRCode() returns Buffer
// Upload to cloud storage → get fileID
// Client gets fileID → wx.cloud.getTempFileURL → use URL for canvas.createImage().src
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 海报页数据加载 + Canvas 渲染逻辑</name>
  <files>
    miniprogram/pages/works/poster.js
    miniprogram/pages/works/poster.json
  </files>
  <action>
    创建海报页面逻辑和配置。包含：并行数据加载、Canvas 2D 初始化与渲染、导出图片、保存到相册、权限处理、错误处理。

    **1. 创建 `miniprogram/pages/works/poster.json`：**

    自定义导航栏（与 compare 页面模式一致，per D-21 深色背景需要自定义导航栏）：
    ```json
    {
      "usingComponents": {
        "t-loading": "tdesign-miniprogram/loading/loading"
      },
      "navigationStyle": "custom",
      "navigationBarTextStyle": "white",
      "backgroundColor": "#000000"
    }
    ```

    **2. 创建 `miniprogram/pages/works/poster.js`：**

    完整的海报生成页面逻辑。核心流程（per D-23）：
    onLoad → 并行加载（作品详情 + 化妆师资料 + QR码）→ 初始化 Canvas → 并行加载图片 → 绘制海报 → 导出图片 → 显示预览

    ```javascript
    const worksService = require('../../services/works')
    const profileService = require('../../services/profile')
    const authService = require('../../services/auth')

    // 海报布局常量 per D-09/D-11
    const POSTER_WIDTH = 750
    const POSTER_HEIGHT = 1060
    const IMAGE_AREA_HEIGHT = 750
    const INFO_AREA_HEIGHT = 310

    Page({
      data: {
        loading: true,
        posterImage: '',
        error: false,
        saving: false,
        statusBarHeight: 20
      },

      _canvas: null,
      _dpr: 1,
      _workId: '',

      onLoad: function (options) {
        // 权限检查 per D-02
        if (!authService.isArtist()) {
          wx.showToast({ title: '无权限访问', icon: 'none' })
          setTimeout(function () { wx.navigateBack() }, 1500)
          return
        }

        var sysInfo = wx.getSystemInfoSync()
        this.setData({ statusBarHeight: sysInfo.statusBarHeight })
        this._dpr = wx.getWindowInfo().pixelRatio

        if (!options.id) {
          wx.navigateBack()
          return
        }

        this._workId = options.id
        this.loadAndGenerate(options.id)
      },

      /**
       * 并行加载所有数据，然后生成海报 per D-23
       */
      loadAndGenerate: function (workId) {
        var self = this

        // 并行加载：作品详情 + 化妆师资料 + QR 码
        Promise.all([
          worksService.getWorkDetail(workId).catch(function (err) {
            console.error('加载作品失败:', err)
            return null
          }),
          profileService.getArtistProfile().catch(function (err) {
            console.error('加载资料失败:', err)
            return null
          }),
          worksService.getShareQRCode(workId).catch(function (err) {
            console.error('获取QR码失败:', err)
            return null
          })
        ]).then(function (results) {
          var work = results[0]
          var artist = results[1]
          var qrData = results[2]

          if (!work) {
            self.setData({ loading: false, error: true })
            return
          }

          if (!qrData) {
            // QR 码生成失败 per D-29
            self.setData({ loading: false, error: true })
            wx.showToast({ title: '生成海报失败，请重试', icon: 'none' })
            return
          }

          // 获取 QR 码临时 URL
          wx.cloud.getTempFileURL({
            fileList: [qrData.fileID]
          }).then(function (tempRes) {
            var qrUrl = tempRes.fileList[0].tempFileURL
            self.generatePoster(work, artist || {}, qrUrl)
          }).catch(function (err) {
            console.error('获取QR临时URL失败:', err)
            self.setData({ loading: false, error: true })
          })
        })
      },

      /**
       * 生成海报 — 初始化 Canvas + 加载图片 + 绘制 + 导出
       */
      generatePoster: function (work, artist, qrUrl) {
        var self = this
        var dpr = this._dpr

        var query = wx.createSelectorQuery()
        query.select('#posterCanvas')
          .fields({ node: true, size: true })
          .exec(function (res) {
            if (!res || !res[0]) {
              console.error('Canvas 节点获取失败')
              self.setData({ loading: false, error: true })
              return
            }

            var canvas = res[0].node
            var ctx = canvas.getContext('2d')
            self._canvas = canvas

            // DPR 适配 per D-10
            canvas.width = POSTER_WIDTH * dpr
            canvas.height = POSTER_HEIGHT * dpr
            ctx.scale(dpr, dpr)

            // 并行加载所有图片 per D-13
            self.loadImages(canvas, work, artist, qrUrl)
              .then(function (images) {
                // 绘制海报
                self.drawPoster(ctx, work, artist, images)
                // 导出图片
                self.exportPoster(canvas, dpr)
              })
              .catch(function (err) {
                console.error('海报生成失败:', err)
                // 图片加载失败仍尝试绘制 per D-30
                var fallbackImages = self.getFallbackImages()
                self.drawPoster(ctx, work, artist, fallbackImages)
                self.exportPoster(canvas, dpr)
              })
          })
      },

      /**
       * 并行加载 3 张图片 per D-13
       * 返回 { workImage, avatar, qr }
       */
      loadImages: function (canvas, work, artist, qrUrl) {
        var self = this

        function loadImage(src) {
          return new Promise(function (resolve, reject) {
            var img = canvas.createImage()
            img.onload = function () { resolve(img) }
            img.onerror = function (err) {
              console.error('图片加载失败:', src, err)
              reject(err)
            }
            img.src = src
          })
        }

        // 加载作品主图 per D-14
        var workImagePromise = work.images && work.images.length > 0
          ? loadImage(work.images[0])
          : Promise.reject(new Error('无作品图片'))

        // 加载头像 per D-15
        var avatarPromise = artist.avatar
          ? loadImage(artist.avatar)
          : Promise.reject(new Error('无头像'))

        // 加载 QR 码 per D-16
        var qrPromise = loadImage(qrUrl)

        // 每张图片独立 fallback per D-30
        return Promise.all([
          workImagePromise.catch(function () { return null }),
          avatarPromise.catch(function () { return null }),
          qrPromise.catch(function () { return null })
        ]).then(function (results) {
          return { workImage: results[0], avatar: results[1], qr: results[2] }
        })
      },

      getFallbackImages: function () {
        return { workImage: null, avatar: null, qr: null }
      },

      /**
       * 绘制海报 per D-11 layout
       * ┌─────────────────────────┐
       * │   作品图片 750×750      │
       * ├─────────────────────────┤
       * │ 头像 名字 标签    QR码   │  310px 信息区
       * │        扫码查看作品      │
       * └─────────────────────────┘
       */
      drawPoster: function (ctx, work, artist, images) {
        // 清空画布
        ctx.clearRect(0, 0, POSTER_WIDTH, POSTER_HEIGHT)

        // === 主图区域 750×750 per D-11 ===
        if (images.workImage) {
          // aspectFill 裁切模式
          var img = images.workImage
          var imgRatio = img.width / img.height
          var targetRatio = POSTER_WIDTH / IMAGE_AREA_HEIGHT
          var sx, sy, sw, sh
          if (imgRatio > targetRatio) {
            sh = img.height
            sw = img.height * targetRatio
            sx = (img.width - sw) / 2
            sy = 0
          } else {
            sw = img.width
            sh = img.width / targetRatio
            sx = 0
            sy = (img.height - sh) / 2
          }
          ctx.drawImage(img, sx, sy, sw, sh, 0, 0, POSTER_WIDTH, IMAGE_AREA_HEIGHT)
        } else {
          // 占位：灰色背景 per D-30
          ctx.fillStyle = '#E8E4DF'
          ctx.fillRect(0, 0, POSTER_WIDTH, IMAGE_AREA_HEIGHT)
          ctx.fillStyle = 'rgba(0,0,0,0.15)'
          ctx.font = '24px sans-serif'
          ctx.textAlign = 'center'
          ctx.fillText('作品图片', POSTER_WIDTH / 2, IMAGE_AREA_HEIGHT / 2)
        }

        // === 信息区域背景 per D-11 ===
        var infoY = IMAGE_AREA_HEIGHT
        ctx.fillStyle = '#FDFCF9'
        ctx.fillRect(0, infoY, POSTER_WIDTH, INFO_AREA_HEIGHT)

        // 底部分隔线
        ctx.strokeStyle = 'rgba(0,0,0,0.05)'
        ctx.lineWidth = 1
        ctx.beginPath()
        ctx.moveTo(0, infoY)
        ctx.lineTo(POSTER_WIDTH, infoY)
        ctx.stroke()

        // === 左侧：头像 + 名字 + 标签 ===
        var leftX = 40
        var avatarY = infoY + 45
        var avatarSize = 80

        // 头像（圆形裁切）
        if (images.avatar) {
          ctx.save()
          ctx.beginPath()
          ctx.arc(leftX + avatarSize / 2, avatarY + avatarSize / 2, avatarSize / 2, 0, Math.PI * 2)
          ctx.clip()
          ctx.drawImage(images.avatar, leftX, avatarY, avatarSize, avatarSize)
          ctx.restore()
        } else {
          // 默认头像：灰色圆形
          ctx.fillStyle = '#D5D0CB'
          ctx.beginPath()
          ctx.arc(leftX + avatarSize / 2, avatarY + avatarSize / 2, avatarSize / 2, 0, Math.PI * 2)
          ctx.fill()
          ctx.fillStyle = '#FFFFFF'
          ctx.font = '32px sans-serif'
          ctx.textAlign = 'center'
          ctx.fillText('👤', leftX + avatarSize / 2, avatarY + avatarSize / 2 + 10)
        }

        // 名字 per D-12
        var nameX = leftX + avatarSize + 20
        var name = artist.name || '化妆师'
        ctx.fillStyle = '#1F1B18'
        ctx.font = 'bold 28px -apple-system, PingFang SC, sans-serif'
        ctx.textAlign = 'left'
        ctx.fillText(name, nameX, avatarY + 32)

        // 风格标签 per D-12
        var tags = artist.style_tags || []
        var tagText = ''
        if (Array.isArray(tags) && tags.length > 0) {
          // 将标签对象数组转为文字
          tagText = tags.map(function (t) { return typeof t === 'string' ? t : t.label || '' }).filter(Boolean).join(' · ')
        }
        if (tagText) {
          // 截断过长标签
          if (tagText.length > 12) tagText = tagText.substring(0, 12) + '...'
          ctx.fillStyle = 'rgba(0,0,0,0.42)'
          ctx.font = '20px -apple-system, PingFang SC, sans-serif'
          ctx.fillText(tagText, nameX, avatarY + 60)
        }

        // === 右侧：QR 码 + 提示文字 ===
        var qrSize = 150
        var qrX = POSTER_WIDTH - qrSize - 40
        var qrY = infoY + 50

        if (images.qr) {
          ctx.drawImage(images.qr, qrX, qrY, qrSize, qrSize)
        } else {
          // QR 占位
          ctx.fillStyle = '#E8E4DF'
          ctx.fillRect(qrX, qrY, qrSize, qrSize)
        }

        // "长按扫码查看作品" 提示文字 per D-11/D-12
        ctx.fillStyle = 'rgba(0,0,0,0.32)'
        ctx.font = '18px -apple-system, PingFang SC, sans-serif'
        ctx.textAlign = 'center'
        ctx.fillText('扫码查看作品', qrX + qrSize / 2, qrY + qrSize + 28)

        // 重置 textAlign
        ctx.textAlign = 'left'
      },

      /**
       * 导出 Canvas 为临时图片 per D-17
       */
      exportPoster: function (canvas, dpr) {
        var self = this
        wx.canvasToTempFilePath({
          canvas: canvas,
          x: 0,
          y: 0,
          width: POSTER_WIDTH * dpr,
          height: POSTER_HEIGHT * dpr,
          destWidth: POSTER_WIDTH * dpr,
          destHeight: POSTER_HEIGHT * dpr,
          fileType: 'png',
          quality: 1,
          success: function (res) {
            self.setData({
              posterImage: res.tempFilePath,
              loading: false
            })
          },
          fail: function (err) {
            console.error('导出海报失败:', err)
            self.setData({ loading: false, error: true })
          }
        })
      },

      /**
       * 保存到相册 per D-18/D-19/D-20
       */
      onSavePoster: function () {
        var self = this
        var filePath = this.data.posterImage
        if (!filePath) return

        this.setData({ saving: true })

        // 检查相册权限 per D-19
        wx.getSetting({
          success: function (res) {
            if (res.authSetting['scope.writePhotosAlbum']) {
              // 已授权 → 直接保存
              self.doSave(filePath)
            } else {
              // 请求授权
              wx.authorize({
                scope: 'scope.writePhotosAlbum',
                success: function () {
                  self.doSave(filePath)
                },
                fail: function () {
                  // 用户拒绝 → 引导到设置页 per D-19
                  wx.showModal({
                    title: '需要相册权限',
                    content: '需要相册权限才能保存海报，请前往设置开启',
                    confirmText: '去设置',
                    success: function (modalRes) {
                      if (modalRes.confirm) {
                        wx.openSetting({
                          success: function (settingRes) {
                            if (settingRes.authSetting['scope.writePhotosAlbum']) {
                              self.doSave(filePath)
                            }
                          }
                        })
                      }
                      self.setData({ saving: false })
                    }
                  })
                }
              })
            }
          },
          fail: function () {
            self.setData({ saving: false })
          }
        })
      },

      doSave: function (filePath) {
        var self = this
        wx.saveImageToPhotosAlbum({
          filePath: filePath,
          success: function () {
            wx.showToast({ title: '海报已保存到相册', icon: 'success' }) // per D-20
            self.setData({ saving: false })
          },
          fail: function (err) {
            console.error('保存失败:', err)
            wx.showToast({ title: '保存失败', icon: 'none' })
            self.setData({ saving: false })
          }
        })
      },

      /**
       * 重试（QR 码失败时）per D-29
       */
      onRetry: function () {
        this.setData({ loading: true, error: false })
        this.loadAndGenerate(this._workId)
      },

      /**
       * 关闭页面 per D-21
       */
      onClose: function () {
        wx.navigateBack()
      }
    })
    ```

    关键实现点：
    - Canvas 元素 ID 为 `#posterCanvas`（与 WXML 配合）
    - `loadImages` 中每张图片独立 catch，不互相影响（per D-30）
    - `drawPoster` 中 aspectFill 手动计算裁切区域
    - 头像圆形裁切使用 `ctx.arc + ctx.clip`
    - `exportPoster` 使用 Canvas 2D 的 `canvas` 参数（不是 canvasId）
    - 权限处理完整流程 per D-19
    - `style_tags` 兼容字符串数组和对象数组两种格式
  </action>
  <verify>
    <automated>test -f miniprogram/pages/works/poster.js && test -f miniprogram/pages/works/poster.json && grep -q "posterCanvas" miniprogram/pages/works/poster.js && grep -q "getShareQRCode" miniprogram/pages/works/poster.js && grep -q "canvasToTempFilePath" miniprogram/pages/works/poster.js && grep -q "saveImageToPhotosAlbum" miniprogram/pages/works/poster.js && grep -q "writePhotosAlbum" miniprogram/pages/works/poster.js && grep -q "openSetting" miniprogram/pages/works/poster.js && echo "Poster page logic complete"</automated>
  </verify>
  <done>
    - poster.js 完整实现：并行数据加载 + Canvas 2D 渲染 + 导出 + 保存
    - Canvas DPR 适配 per D-10
    - 并行图片加载 per D-13，独立 fallback per D-30
    - 海报布局 per D-11（作品主图 750×750 + 信息区 310px）
    - 圆形头像裁切 + aspectFill 主图裁切
    - 字体 per D-12（系统字体）
    - 导出 per D-17（canvasToTempFilePath, DPR 尺寸）
    - 保存 per D-18（saveImageToPhotosAlbum）
    - 权限处理 per D-19（getSetting → authorize → openSetting）
    - 错误处理 per D-29（QR 失败重试）+ D-30（图片占位）+ D-31（权限引导）
    - poster.json 自定义导航栏 + t-loading 组件
  </done>
</task>

<task type="auto">
  <name>Task 2: 海报页 UI 模板和样式</name>
  <files>
    miniprogram/pages/works/poster.wxml
    miniprogram/pages/works/poster.wxss
  </files>
  <action>
    创建海报页面的 WXML 模板和 WXSS 样式。

    **1. 创建 `miniprogram/pages/works/poster.wxml`：**

    海报页面 UI 结构（per D-21/D-22）：

    ```xml
    <view class="poster-page">
      <!-- 自定义导航栏（与 compare 页面模式一致） -->
      <view class="poster-navbar" style="padding-top: {{statusBarHeight}}px;">
        <view class="navbar-content">
          <view class="navbar-back tappable" bindtap="onClose">
            <view class="navbar-back-icon"></view>
          </view>
          <text class="navbar-title">分享海报</text>
          <view class="navbar-placeholder"></view>
        </view>
      </view>

      <!-- 加载状态 -->
      <view class="poster-loading" wx:if="{{loading}}">
        <t-loading theme="circular" size="80rpx" text="海报生成中..." />
      </view>

      <!-- 错误状态 per D-29 -->
      <view class="poster-error" wx:if="{{error}}">
        <text class="poster-error-text">生成海报失败</text>
        <button class="poster-retry-btn btn-outline" bindtap="onRetry">重试</button>
      </view>

      <!-- 海报预览区域 -->
      <view class="poster-content" wx:if="{{!loading && !error && posterImage}}">
        <!-- 离屏 Canvas per D-22 -->
        <canvas
          type="2d"
          id="posterCanvas"
          class="poster-canvas"
        ></canvas>

        <!-- 海报图片预览（用户看到的是导出的图片，不是 canvas 本身）per D-22 -->
        <view class="poster-preview">
          <image
            class="poster-image"
            src="{{posterImage}}"
            mode="widthFix"
            show-menu-by-longpress
          />
        </view>

        <!-- 底部操作栏 -->
        <view class="poster-actions">
          <button class="poster-save-btn btn-primary" bindtap="onSavePoster" disabled="{{saving}}">
            {{saving ? '保存中...' : '保存到相册'}}
          </button>
        </view>
      </view>
    </view>
    ```

    关键点：
    - Canvas 元素 `type="2d"` `id="posterCanvas"`（per D-08，poster.js 通过 querySelector 获取此节点）
    - Canvas 用 CSS 设为不可见（per D-22）
    - 用户看到的是 `posterImage`（canvas 导出后的图片）
    - 保存按钮使用全局 `btn-primary` 样式
    - 重试按钮使用全局 `btn-outline` 样式
    - `show-menu-by-longpress` 让用户可以长按图片保存（额外便利）

    **2. 创建 `miniprogram/pages/works/poster.wxss`：**

    ```css
    .poster-page {
      min-height: 100vh;
      background-color: rgba(0, 0, 0, 0.7);
      display: flex;
      flex-direction: column;
    }

    /* 自定义导航栏 */
    .poster-navbar {
      background-color: transparent;
    }

    .navbar-content {
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 88rpx;
      padding: 0 var(--page-pad);
    }

    .navbar-back {
      width: 68rpx;
      height: 68rpx;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
    }

    .navbar-back-icon {
      width: 36rpx;
      height: 36rpx;
      background-image: var(--icon-close);
      background-size: contain;
      background-repeat: no-repeat;
      background-position: center;
    }

    .navbar-title {
      font-size: var(--fs-base);
      font-weight: 500;
      color: #F5F0EB;
    }

    .navbar-placeholder {
      width: 68rpx;
    }

    /* 加载状态 */
    .poster-loading {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* 错误状态 */
    .poster-error {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 32rpx;
    }

    .poster-error-text {
      font-size: var(--fs-base);
      color: rgba(255, 255, 255, 0.6);
    }

    .poster-retry-btn {
      width: 280rpx;
      height: 80rpx;
      font-size: var(--fs-sm);
    }

    /* 离屏 Canvas per D-22 */
    .poster-canvas {
      position: fixed;
      left: -9999px;
      width: 750px;
      height: 1060px;
    }

    /* 海报预览区域 */
    .poster-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 0 var(--page-pad);
    }

    .poster-preview {
      width: 85%;
      border-radius: var(--radius-lg);
      overflow: hidden;
      box-shadow: 0 8rpx 40rpx rgba(0, 0, 0, 0.3);
    }

    .poster-image {
      width: 100%;
      display: block;
    }

    /* 底部操作栏 */
    .poster-actions {
      width: 100%;
      padding: 36rpx var(--page-pad) 60rpx;
    }

    .poster-save-btn {
      width: 100%;
      height: 100rpx;
      font-size: var(--fs-base);
    }

    .poster-save-btn[disabled] {
      opacity: 0.5;
      background-color: var(--accent);
      color: #FDFBF8;
    }
    ```

    关键样式点：
    - 页面背景 `rgba(0,0,0,0.7)` 深色半透明（per D-21）
    - Canvas `position: fixed; left: -9999px` 离屏隐藏（per D-22）
    - 海报预览 `width: 85%` 居中展示（per D-09: CSS 显示尺寸）
    - 预览区域有 `border-radius + box-shadow` 增加视觉层次
    - 保存按钮 `width: 100%` 铺满底部操作栏
    - 使用全局 CSS 变量保持视觉一致性
  </action>
  <verify>
    <automated>test -f miniprogram/pages/works/poster.wxml && test -f miniprogram/pages/works/poster.wxss && grep -q "posterCanvas" miniprogram/pages/works/poster.wxml && grep -q "type=\"2d\"" miniprogram/pages/works/poster.wxml && grep -q "posterImage" miniprogram/pages/works/poster.wxml && grep -q "onSavePoster" miniprogram/pages/works/poster.wxml && grep -q "poster-canvas" miniprogram/pages/works/poster.wxss && grep -q "saveImageToPhotosAlbum\|save.*btn\|poster-save" miniprogram/pages/works/poster.wxml && echo "Poster page UI complete"</automated>
  </verify>
  <done>
    - poster.wxml：自定义导航栏 + loading 状态 + 错误重试 + 离屏 Canvas(type="2d") + 海报图片预览 + 保存按钮
    - poster.wxss：深色半透明背景 + Canvas 离屏隐藏 + 海报居中预览 + 底部操作栏
    - Canvas ID 为 posterCanvas，与 poster.js 中 querySelector 匹配
    - 海报预览使用 canvas 导出图片（不是 canvas 本身）per D-22
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户→Canvas 2D 渲染 | Canvas 在客户端渲染，无外部输入注入风险 |
| 客户端→相册权限 | 需要用户授权 scope.writePhotosAlbum |
| 云存储图片→Canvas | 云存储 fileID 作为 img.src 加载到 canvas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | poster page | mitigate | authService.isArtist() 权限检查 per D-02，非化妆师无法进入海报页 |
| T-10-05 | Tampering | Canvas output | accept | Canvas 渲染的图片仅用于保存到相册，不涉及数据写入。海报内容（作品图+QR码+公开资料）均为非敏感信息 |
| T-10-06 | Elevation of Privilege | save flow | accept | 保存到相册需要用户主动授权，权限流程 per D-19 遵循微信标准 |
</threat_model>

<verification>
1. 海报页逻辑：
   - poster.js 含完整数据流：loadAndGenerate → generatePoster → loadImages → drawPoster → exportPoster
   - 并行加载使用 Promise.all（work + profile + QR per D-23）
   - Canvas 2D 初始化：canvas.width/height × DPR + ctx.scale(dpr, dpr) per D-10
   - 图片加载：canvas.createImage() + Promise.all per D-13，独立 catch per D-30
   - 海报绘制布局 per D-11（主图 750×750 + 信息区 310px）
   - 导出：wx.canvasToTempFilePath with canvas parameter per D-17
   - 保存：wx.saveImageToPhotosAlbum per D-18
   - 权限流程：getSetting → authorize → openSetting per D-19
   - 错误：QR 失败重试 per D-29，图片占位 per D-30
2. 海报页 UI：
   - poster.wxml 含 canvas type="2d" id="posterCanvas"
   - poster.wxml 含海报图片预览 + 保存按钮 + loading + error + retry
   - poster.wxss Canvas 离屏隐藏 (position: fixed; left: -9999px)
   - poster.wxss 深色半透明背景
3. 配置：
   - poster.json 含 t-loading 组件 + 自定义导航栏
</verification>

<success_criteria>
- 海报页面 Canvas 2D 渲染完成：作品主图（aspectFill）+ 化妆师头像（圆形裁切）+ 名字 + 风格标签 + 小程序码 + 提示文字
- Canvas DPR 适配正确（ctx.scale per D-10）
- 化妆师点击"保存到相册"后海报保存到手机
- 权限处理完整：授权→拒绝→引导设置→返回重试（per D-19）
- 图片加载失败时海报仍能生成（灰色占位 per D-30）
- QR 码生成失败时显示错误提示和重试按钮（per D-29）
</success_criteria>

<output>
After completion, create `.planning/phases/10-qr-code-poster-generation/10-02-SUMMARY.md`
</output>
