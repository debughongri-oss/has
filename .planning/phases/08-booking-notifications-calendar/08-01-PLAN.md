---
phase: 08-booking-notifications-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudfunctions/booking-reminder/index.js
  - cloudfunctions/booking-reminder/package.json
  - cloudfunctions/booking-reminder/config.json
  - miniprogram/pages/booking/create.wxml
  - miniprogram/pages/booking/create.js
  - miniprogram/pages/booking/create.wxss
autonomous: true
requirements:
  - BOOK-06
  - BOOK-07
  - BOOK-15

must_haves:
  truths:
    - "预约状态变更（接受/拒绝/完成）时客户收到微信订阅消息通知 — 已在 v1.0 实现，本次验证无回归"
    - "预约前一天 20:00，已确认预约的客户收到提醒订阅消息"
    - "客户预约选择日期后，时段列表上方显示剩余可用时段数量"
    - "所有时段已满时显示'该日期已约满'提示，时段 grid 全部灰色不可选"
  artifacts:
    - path: "cloudfunctions/booking-reminder/index.js"
      provides: "定时触发器云函数，每天 20:00 扫描明天已确认预约发送提醒"
      contains: "exports.main"
    - path: "cloudfunctions/booking-reminder/config.json"
      provides: "定时触发器配置"
      contains: "triggers"
    - path: "cloudfunctions/booking-reminder/package.json"
      provides: "云函数依赖声明"
      contains: "wx-server-sdk"
    - path: "miniprogram/pages/booking/create.wxml"
      provides: "剩余时段提示 UI"
      contains: "available"
  key_links:
    - from: "cloudfunctions/booking-reminder/index.js"
      to: "cloud.openapi.subscribeMessage.send"
      via: "微信订阅消息 API"
      pattern: "subscribeMessage\\.send"
    - from: "cloudfunctions/booking-reminder/index.js"
      to: "bookings collection"
      via: "cloud database query"
      pattern: "booking_date.*tomorrow"
    - from: "miniprogram/pages/booking/create.wxml"
      to: "create.js data.availableCount"
      via: "WXML data binding"
      pattern: "availableCount"
---

<objective>
实现预约前一天定时提醒（BOOK-07）和预约创建页剩余时段提示（BOOK-15），并验证 BOOK-06 已有实现无回归。

Purpose: 预约提醒让客户不会忘记明天的预约，时段提示帮助客户快速判断日期是否有空。这两个功能独立于日历视图，可并行开发。

Output: booking-reminder 云函数（3 文件）、create 页面时段提示增强（3 文件修改）
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-booking-notifications-calendar/08-CONTEXT.md
@cloudfunctions/bookings/index.js
@cloudfunctions/bookings/config.json
@cloudfunctions/bookings/package.json
@miniprogram/pages/booking/create.js
@miniprogram/pages/booking/create.wxml
@miniprogram/pages/booking/create.wxss
@miniprogram/utils/constants.js
@miniprogram/services/bookings.js

<interfaces>
<!-- Executor needs these existing interfaces — no codebase exploration needed -->

From cloudfunctions/bookings/index.js (reference for sendNotify pattern):
```javascript
// Line 8: Template ID (reuse this same ID)
const TEMPLATE_ID = '-i6OevJwdS5fGFXCsB9Xux4zaaxUkXTR0xfLg5T48jM'

// Line 10-16: Status labels
const STATUS_LABELS = {
  pending: '待确认',
  accepted: '已确认',
  rejected: '已拒绝',
  cancelled: '已取消',
  completed: '已完成'
}

// Line 18-35: sendNotify function pattern to reuse
async function sendNotify(booking, status) {
  try {
    await cloud.openapi.subscribeMessage.send({
      touser: booking.user_openid,
      templateId: TEMPLATE_ID,
      page: `pages/profile/history`,
      data: {
        thing1: { value: booking.service_name || '化妆服务' },
        date2: { value: `${booking.booking_date} ${booking.booking_time}` },
        date3: { value: `${booking.booking_date} ${booking.booking_time}` },
        thing4: { value: booking.special_needs || booking.notes || '无' },
        phrase5: { value: STATUS_LABELS[status] || status }
      }
    })
  } catch (err) {
    console.error('发送订阅消息失败:', err)
  }
}
```

From miniprogram/utils/constants.js:
```javascript
const SUBSCRIBE_TEMPLATE_ID = '-i6OevJwdS5fGFXCsB9Xux4zaaxUkXTR0xfLg5T48jM'
```

From miniprogram/pages/booking/create.js (onSelectDate callback):
```javascript
// Line 70-84: Available slots are already fetched and mapped
onSelectDate: function (e) {
  const date = e.currentTarget.dataset.date
  this.setData({ selectedDate: date, selectedTime: '', loadingSlots: true })
  bookingsService.getAvailableSlots(date)
    .then(data => {
      const slots = data.all.map(time => ({
        time,
        available: data.available.includes(time)
      }))
      this.setData({ timeSlots: slots, loadingSlots: false })
    })
}
```

From cloudfunctions/bookings/config.json:
```json
{
  "permissions": {
    "openapi": ["subscribeMessage.send"]
  }
}
```

From cloudfunctions/bookings/package.json:
```json
{
  "name": "bookings",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~3.0.4"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 booking-reminder 定时触发器云函数</name>
  <files>
    cloudfunctions/booking-reminder/index.js
    cloudfunctions/booking-reminder/package.json
    cloudfunctions/booking-reminder/config.json
  </files>
  <action>
创建独立的 `booking-reminder` 云函数，实现每天 20:00 自动扫描明天的已确认预约并发送提醒消息。

**1. 创建 `cloudfunctions/booking-reminder/package.json`：**
```json
{
  "name": "booking-reminder",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~3.0.4"
  }
}
```

**2. 创建 `cloudfunctions/booking-reminder/config.json`：**
- 声明定时触发器，cron 表达式 `"0 0 20 * * * *"`（每天 20:00）
- 声明 `subscribeMessage.send` openapi 权限（与 bookings/config.json 同模式）

```json
{
  "permissions": {
    "openapi": [
      "subscribeMessage.send"
    ]
  },
  "triggers": [
    {
      "name": "bookingReminderTimer",
      "type": "timer",
      "config": "0 0 20 * * * *"
    }
  ]
}
```

**3. 创建 `cloudfunctions/booking-reminder/index.js`：**

按以下逻辑实现（per D-05~D-10）：

```javascript
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

const TEMPLATE_ID = '-i6OevJwdS5fGFXCsB9Xux4zaaxUkXTR0xfLg5T48jM'

// 计算"明天"的日期字符串 YYYY-MM-DD
function getTomorrowDate() {
  const d = new Date()
  d.setDate(d.getDate() + 1)
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}

exports.main = async (event, context) => {
  const tomorrow = getTomorrowDate()

  // 查询明天所有已确认的预约（per D-09: 只提醒 accepted）
  const { data: bookings } = await db.collection('bookings')
    .where({
      booking_date: tomorrow,
      status: 'accepted'
    })
    .get()

  console.log(`找到 ${bookings.length} 个明天(${tomorrow})的已确认预约`)

  // 逐个发送提醒（per D-08: 使用现有模板，phrase5 设为 "预约提醒"）
  let successCount = 0
  for (const booking of bookings) {
    try {
      await cloud.openapi.subscribeMessage.send({
        touser: booking.user_openid,
        templateId: TEMPLATE_ID,
        page: 'pages/profile/history',
        data: {
          thing1: { value: booking.service_name || '化妆服务' },
          date2: { value: `${booking.booking_date} ${booking.booking_time}` },
          date3: { value: `${booking.booking_date} ${booking.booking_time}` },
          thing4: { value: booking.special_needs || booking.notes || '无' },
          phrase5: { value: '预约提醒' }
        }
      })
      successCount++
    } catch (err) {
      // per D-10: 发送失败静默吞掉，不影响其他用户
      console.error(`发送提醒失败(${booking._id}):`, err)
    }
  }

  return {
    errCode: 0,
    data: { total: bookings.length, success: successCount }
  }
}
```

关键设计点：
- `phrase5` 使用 `"预约提醒"` 而非 STATUS_LABELS 中的任何值，用于区分状态变更通知和提醒通知（per D-08）
- 定时触发器无用户上下文，不需要 `requireArtist` 验证（per auth.js 注释）
- `config.json` 中同时声明 `permissions.openapi` 和 `triggers`
- 循环中逐个发送而非 Promise.all，避免并发触发微信 API 限流
  </action>
  <verify>
    <automated>test -f cloudfunctions/booking-reminder/index.js && test -f cloudfunctions/booking-reminder/package.json && test -f cloudfunctions/booking-reminder/config.json && echo "All files exist"</automated>
  </verify>
  <done>
    - cloudfunctions/booking-reminder/ 目录含 3 个文件（index.js, package.json, config.json）
    - config.json 声明 cron "0 0 20 * * * *" 和 subscribeMessage.send 权限
    - index.js 查询明天 accepted 状态预约，逐个发送提醒，phrase5 为 "预约提醒"
    - 发送失败 try-catch 静默处理，不影响循环继续
  </done>
</task>

<task type="auto">
  <name>Task 2: 预约创建页添加剩余可用时段提示</name>
  <files>
    miniprogram/pages/booking/create.js
    miniprogram/pages/booking/create.wxml
    miniprogram/pages/booking/create.wxss
  </files>
  <action>
在客户预约创建页的时段列表上方添加剩余可用时段数量提示（per D-18~D-20）。

**1. 修改 `miniprogram/pages/booking/create.js`：**

在 `onSelectDate` 回调中，将 `data.available.length` 存入 data 供模板使用。修改 `.then` 回调：

```javascript
// 在 onSelectDate 的 .then(data => {...}) 中，修改为：
bookingsService.getAvailableSlots(date)
  .then(data => {
    const slots = data.all.map(time => ({
      time,
      available: data.available.includes(time)
    }))
    this.setData({
      timeSlots: slots,
      availableCount: data.available.length,   // 新增：剩余可用时段数
      allBooked: data.available.length === 0,  // 新增：是否全部约满
      loadingSlots: false
    })
  })
```

同时在 `data` 初始值中添加：
```javascript
availableCount: 0,
allBooked: false,
```

**2. 修改 `miniprogram/pages/booking/create.wxml`：**

在 "选择时间" form-block 中，`<text class="form-title">选择时间</text>` 之后、`<view class="slot-load">` 之前，插入提示文字：

```xml
<view class="slots-hint" wx:if="{{!loadingSlots && availableCount > 0}}">
  剩余 {{availableCount}} 个可用时段
</view>
<view class="slots-hint slots-hint--full" wx:if="{{!loadingSlots && allBooked}}">
  该日期已约满，请选择其他日期
</view>
```

**3. 修改 `miniprogram/pages/booking/create.wxss`：**

在文件末尾添加：

```css
.slots-hint {
  font-size: var(--fs-xs);
  color: var(--accent);
  background-color: var(--accent-soft);
  padding: 12rpx 24rpx;
  border-radius: var(--radius-sm);
  margin-bottom: 20rpx;
  letter-spacing: 1rpx;
}

.slots-hint--full {
  color: var(--orange);
  background-color: var(--orange-bg);
}
```

注意：
- 提示仅在时段加载完成后显示（`!loadingSlots`）
- D-19: 约满时提示文案为 "该日期已约满，请选择其他日期"
- 约满时时段 grid 已全部显示为 `time--no` 灰色不可选样式（现有逻辑已覆盖，无需额外修改）
- 使用项目已有的 CSS 变量 `--accent`, `--accent-soft`, `--orange`, `--orange-bg`
  </action>
  <verify>
    <automated>grep -q "availableCount" miniprogram/pages/booking/create.js && grep -q "slots-hint" miniprogram/pages/booking/create.wxml && grep -q "slots-hint" miniprogram/pages/booking/create.wxss && echo "Slots hint implemented"</automated>
  </verify>
  <done>
    - create.js 中 onSelectDate 回调存入 availableCount 和 allBooked
    - create.wxml 时段列表上方显示 "剩余 X 个可用时段" 或 "该日期已约满，请选择其他日期"
    - create.wxss 新增 slots-hint 样式，约满时使用橙色变体
    - 数据来源复用现有 getAvailableSlots 返回值（per D-20），无新增接口
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 定时触发器→数据库 | booking-reminder 定时触发器无用户上下文，直接查询数据库发送通知 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | booking-reminder | accept | 定时触发器仅读取 accepted 状态预约的 user_openid 发送通知，无外部输入向量 |
| T-08-02 | Denial of Service | subscribeMessage.send API | accept | 逐个发送避免并发限流，失败静默处理。个人项目预约量远低于微信 API 限额 |
| T-08-03 | Spoofing | booking-reminder trigger | accept | 云函数定时触发器由微信云开发平台管理，无法被外部伪造触发 |
</threat_model>

<verification>
1. BOOK-06 验证（无回归）：
   - `cloudfunctions/bookings/index.js` 中 `sendNotify` 函数存在（line 18）
   - `updateStatus` action 中 `['accepted', 'rejected', 'completed'].includes(status)` 触发 `sendNotify`（line 168）
   - `miniprogram/pages/booking/create.js` 中 `wx.requestSubscribeMessage` 调用存在（line 141）
2. booking-reminder 云函数：
   - 3 个文件存在（index.js, package.json, config.json）
   - config.json 含 cron 触发器配置
   - index.js 查询明天 accepted 预约并发送提醒
3. 时段提示：
   - create.wxml 含 slots-hint 元素
   - create.js 存入 availableCount
</verification>

<success_criteria>
- booking-reminder 云函数完整，每天 20:00 触发，扫描明天已确认预约并发送提醒
- 客户预约页选择日期后，时段列表上方显示剩余可用时段数量
- 日期约满时显示 "该日期已约满，请选择其他日期"，时段 grid 保持灰色不可选
- BOOK-06 现有通知代码无任何修改（零回归风险）
</success_criteria>

<output>
After completion, create `.planning/phases/08-booking-notifications-calendar/08-01-SUMMARY.md`
</output>
