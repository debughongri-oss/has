---
phase: 08-booking-notifications-calendar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudfunctions/bookings/index.js
  - miniprogram/services/bookings.js
  - miniprogram/pages/admin/bookings/calendar.js
  - miniprogram/pages/admin/bookings/calendar.wxml
  - miniprogram/pages/admin/bookings/calendar.wxss
  - miniprogram/pages/admin/bookings/calendar.json
  - miniprogram/pages/admin/bookings/list.wxml
  - miniprogram/pages/admin/bookings/list.js
  - miniprogram/app.json
autonomous: true
requirements:
  - BOOK-13
  - BOOK-14
  - BOOK-16

must_haves:
  truths:
    - "化妆师在管理后台以月历视图查看预约分布，有预约的日期标点"
    - "化妆师点击日历日期后看到当天预约列表，按时间排序"
    - "化妆师看到同一天 3+ 预约时显示橙色'紧凑日程'警告"
    - "管理端预约列表页有'日历视图'入口按钮，点击跳转日历页面"
  artifacts:
    - path: "miniprogram/pages/admin/bookings/calendar.js"
      provides: "日历页面逻辑：TDesign Calendar 渲染、getCalendarData 调用、日期选择、预约列表展示"
      min_lines: 80
    - path: "miniprogram/pages/admin/bookings/calendar.wxml"
      provides: "日历页面模板：t-calendar 组件 + 预约列表 + 紧凑日程警告"
      contains: "t-calendar"
    - path: "miniprogram/pages/admin/bookings/calendar.json"
      provides: "页面配置：引入 t-calendar 组件"
      contains: "t-calendar"
    - path: "cloudfunctions/bookings/index.js"
      provides: "新增 getCalendarData action"
      contains: "getCalendarData"
    - path: "miniprogram/services/bookings.js"
      provides: "新增 getCalendarData 客户端服务函数"
      contains: "getCalendarData"
    - path: "miniprogram/app.json"
      provides: "admin 子包新增 calendar 路由"
      contains: "bookings/calendar"
  key_links:
    - from: "miniprogram/pages/admin/bookings/calendar.js"
      to: "miniprogram/services/bookings.js"
      via: "getCalendarData service function"
      pattern: "bookingsService\\.getCalendarData"
    - from: "miniprogram/services/bookings.js"
      to: "cloudfunctions/bookings/index.js"
      via: "callCloudFunction bookings action=getCalendarData"
      pattern: "getCalendarData"
    - from: "miniprogram/pages/admin/bookings/list.wxml"
      to: "miniprogram/pages/admin/bookings/calendar"
      via: "wx.navigateTo calendar page"
      pattern: "goToCalendar"
---

<objective>
创建管理端预约日历页面，化妆师可通过 TDesign Calendar 月历视图查看预约分布，点击日期查看当天预约列表，3+ 预约显示紧凑日程警告。

Purpose: 日历视图让化妆师直观看到每日预约密度，合理安排日程。紧凑日程警告提前提示当天预约过多。

Output: 日历页面 4 个文件、bookings 云函数新增 getCalendarData action、bookings 服务新增函数、管理端列表页新增入口、app.json 路由注册
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-booking-notifications-calendar/08-CONTEXT.md
@cloudfunctions/bookings/index.js
@miniprogram/services/bookings.js
@miniprogram/services/api.js
@miniprogram/pages/admin/bookings/list.js
@miniprogram/pages/admin/bookings/list.wxml
@miniprogram/pages/admin/bookings/list.wxss
@miniprogram/app.json
@miniprogram/app.wxss
@miniprogram/utils/constants.js

<interfaces>
<!-- Executor needs these existing interfaces — no codebase exploration needed -->

From miniprogram/miniprogram_npm/tdesign-miniprogram/calendar/type.d.ts:
```typescript
// Key props for t-calendar component
value: number | number[]          // 选中日期（timestamp）
minDate: number                   // 最小可选日期
maxDate: number                   // 最大可选日期
format: (day: TDate) => TDate     // 自定义日期渲染
switchMode: 'none' | 'month' | 'year-month'  // 翻页模式
type: 'single' | 'multiple' | 'range'         // 选择类型
readonly: boolean                 // 只读模式
usePopup: boolean                 // 是否弹窗模式（false = 内嵌）
title: string                     // 标题

// TDate interface (format callback parameter)
interface TDate {
  date: Date
  day: number
  type: 'selected' | 'disabled' | 'start' | 'start-end' | 'centre' | 'end' | ''
  className?: string
  prefix?: string
  suffix?: string   // 用这个属性添加日期标记文字
}

// Events
bind:select  — 日期选择事件，返回 { value: timestamp }
bind:change  — 日期变更事件
```

From miniprogram/services/api.js:
```javascript
const callCloudFunction = async (name, data = {}) => {
  const res = await wx.cloud.callFunction({ name, data })
  const result = res.result || {}
  if (result.errCode) {
    wx.showToast({ title: result.errMsg || '请求失败', icon: 'none' })
    return Promise.reject(result)
  }
  return result
}
```

From miniprogram/services/bookings.js (existing pattern for service functions):
```javascript
const { callCloudFunction } = require('./api')

// Example pattern:
const getBookingsList = async (status, page, pageSize) => {
  const result = await callCloudFunction('bookings', { action: 'list', status, page, pageSize })
  if (result.errCode !== 0) throw new Error(result.errMsg)
  return result.data
}
```

From miniprogram/pages/admin/bookings/list.wxss (card styles to reuse):
```css
.card {
  background-color: var(--bg-elevated);
  border-radius: var(--radius-lg);
  padding: var(--page-pad);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.03);
}
.card-top { display: flex; justify-content: space-between; align-items: center; gap: 16rpx; }
.card-svc { font-size: var(--fs-sm); font-weight: 600; overflow: hidden; text-overflow: ellipsis; }
.badge { font-size: var(--fs-xs); padding: 6rpx 20rpx; border-radius: 999rpx; }
.card-info { font-size: var(--fs-xs); color: var(--text-secondary); margin-top: 12rpx; }
```

From cloudfunctions/bookings/index.js (switch pattern):
```javascript
exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID
  switch (event.action) {
    // ... existing cases ...
    default:
      return { errCode: -1, errMsg: '未知操作' }
  }
}
```

From cloudfunctions/shared/auth.js:
```javascript
function requireArtist(wxContext) {
  if (wxContext.OPENID === ARTIST_OPENID) return { ok: true }
  return { ok: false, response: { errCode: -1, errMsg: '无权限操作' } }
}
```

From miniprogram/app.json (admin subpackage):
```json
{
  "subPackages": [{
    "root": "pages/admin",
    "name": "admin",
    "pages": [
      "works/list", "works/edit",
      "services/list", "services/edit",
      "bookings/list", "bookings/detail",
      "profile/edit"
    ]
  }]
}
```

From miniprogram/app.wxss (relevant CSS variables):
```css
--accent: #9C7A5A;  --orange: #D4864A;  --orange-bg: rgba(212, 134, 74, 0.08);
--bg: #F5F2EE;  --bg-elevated: #FDFCF9;
--radius-sm: 8rpx;  --radius-md: 14rpx;  --radius-lg: 20rpx;
--fs-xs: 22rpx;  --fs-sm: 26rpx;  --fs-base: 32rpx;
--page-pad: 32rpx;
```

From miniprogram/utils/constants.js:
```javascript
const ARTIST_OPENID = 'oDmtI48gecuF3n9OkEBJN91BJliI'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 — bookings 云函数新增 getCalendarData action + 服务层 + 路由注册</name>
  <files>
    cloudfunctions/bookings/index.js
    miniprogram/services/bookings.js
    miniprogram/app.json
  </files>
  <action>
**1. 修改 `cloudfunctions/bookings/index.js`：**

在 `switch` 语句的 `default` case 之前，新增 `getCalendarData` action（per D-13）：

```javascript
case 'getCalendarData': {
  const { year, month } = event
  try {
    const authCheck = requireArtist(wxContext)
    if (!authCheck.ok) return authCheck.response

    // 构造该月的日期范围
    const startDate = `${year}-${String(month).padStart(2, '0')}-01`
    // 计算下月第一天
    let nextMonth = month + 1
    let nextYear = year
    if (nextMonth > 12) { nextMonth = 1; nextYear = year + 1 }
    const endDate = `${nextYear}-${String(nextMonth).padStart(2, '0')}-01`

    // 查询该月所有非取消的预约
    const { data: bookings } = await db.collection('bookings')
      .where({
        booking_date: _.gte(startDate).and(_.lt(endDate)),
        status: _.neq('cancelled')
      })
      .orderBy('booking_date', 'asc')
      .orderBy('booking_time', 'asc')
      .get()

    // 按日期分组（服务端分组，减少客户端计算 per D-13）
    const grouped = {}
    bookings.forEach(b => {
      if (!grouped[b.booking_date]) {
        grouped[b.booking_date] = []
      }
      grouped[b.booking_date].push(b)
    })

    return {
      errCode: 0,
      data: {
        grouped,  // { "2026-04-25": [booking1, booking2], ... }
        bookings  // 完整列表，方便客户端使用
      }
    }
  } catch (error) {
    console.error('获取日历数据失败:', error)
    return { errCode: -1, errMsg: '获取日历数据失败' }
  }
}
```

关键点：
- 使用 `requireArtist` 验证身份（管理端功能，per Phase 6 建立的 auth 模式）
- `_.gte(startDate).and(_.lt(endDate))` 范围查询，比正则高效
- `status: _.neq('cancelled')` 排除已取消的预约（cancelled 不显示在日历上）
- 服务端按日期分组返回（per D-13）

**2. 修改 `miniprogram/services/bookings.js`：**

在 `cancelBooking` 函数之后、`getStatusLabel` 之前，新增：

```javascript
const getCalendarData = async (year, month) => {
  const result = await callCloudFunction('bookings', { action: 'getCalendarData', year, month })
  if (result.errCode !== 0) throw new Error(result.errMsg)
  return result.data
}
```

在 `module.exports` 中新增 `getCalendarData`。

**3. 修改 `miniprogram/app.json`：**

在 admin subPackages 的 pages 数组中，`"bookings/detail"` 后面新增 `"bookings/calendar"`：

```json
"pages": [
  "works/list",
  "works/edit",
  "services/list",
  "services/edit",
  "bookings/list",
  "bookings/detail",
  "bookings/calendar",
  "profile/edit"
]
```
  </action>
  <verify>
    <automated>grep -q "getCalendarData" cloudfunctions/bookings/index.js && grep -q "getCalendarData" miniprogram/services/bookings.js && grep -q "bookings/calendar" miniprogram/app.json && echo "Backend + route ready"</automated>
  </verify>
  <done>
    - bookings 云函数新增 getCalendarData action，按月查询预约并按日期分组返回
    - bookings 服务层新增 getCalendarData(year, month) 函数
    - app.json admin 子包新增 bookings/calendar 路由
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 — 日历页面 + 管理端列表页入口</name>
  <files>
    miniprogram/pages/admin/bookings/calendar.json
    miniprogram/pages/admin/bookings/calendar.js
    miniprogram/pages/admin/bookings/calendar.wxml
    miniprogram/pages/admin/bookings/calendar.wxss
    miniprogram/pages/admin/bookings/list.wxml
    miniprogram/pages/admin/bookings/list.js
  </files>
  <action>
创建日历页面（4 个文件）并在管理端预约列表页添加入口按钮。

**1. 创建 `miniprogram/pages/admin/bookings/calendar.json`：**

```json
{
  "navigationBarTitleText": "预约日历",
  "usingComponents": {
    "t-calendar": "tdesign-miniprogram/calendar/calendar",
    "t-loading": "tdesign-miniprogram/loading/loading",
    "t-empty": "tdesign-miniprogram/empty/empty"
  }
}
```

**2. 创建 `miniprogram/pages/admin/bookings/calendar.js`：**

```javascript
const bookingsService = require('../../../services/bookings')
const authService = require('../../../services/auth')

const BUSY_DAY_THRESHOLD = 3  // per D-22

Page({
  data: {
    calendarValue: Date.now(),  // 当前选中日期（timestamp）
    minDate: Date.now(),        // 最小可选：今天
    format: null,               // format 函数引用（在 attached 中设置）
    grouped: {},                // 按日期分组的预约数据
    loading: true,
    selectedDate: '',           // 当前选中日期字符串 YYYY-MM-DD
    dayBookings: [],            // 选中日期的预约列表
    isBusyDay: false,           // 是否紧凑日程
    busyCount: 0                // 紧凑日程预约数
  },

  onLoad: function () {
    if (!authService.isArtist()) {
      wx.showToast({ title: '无权限访问', icon: 'none' })
      setTimeout(() => wx.navigateBack(), 1500)
      return
    }
    // TDesign Calendar 的 format 必须通过数据引用传递
    this.setData({ format: this._formatDay.bind(this) })
    this.loadCurrentMonth()
  },

  loadCurrentMonth: function () {
    const now = new Date()
    this.loadMonthData(now.getFullYear(), now.getMonth() + 1)
  },

  loadMonthData: function (year, month) {
    this.setData({ loading: true })
    bookingsService.getCalendarData(year, month)
      .then(data => {
        const grouped = {}
        data.bookings.forEach(b => {
          if (!grouped[b.booking_date]) grouped[b.booking_date] = []
          grouped[b.booking_date].push(b)
        })
        this.setData({ grouped, loading: false })
      })
      .catch(err => {
        console.error('加载日历数据失败:', err)
        this.setData({ loading: false })
      })
  },

  // TDesign Calendar format 回调 — 给有预约的日期添加标记（per D-14, D-23）
  _formatDay: function (day) {
    const d = day.date
    const dateStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
    const dayBookings = this.data.grouped[dateStr]

    if (dayBookings && dayBookings.length > 0) {
      if (dayBookings.length >= BUSY_DAY_THRESHOLD) {
        // per D-23: 3+ 预约的日期使用橙色标记
        day.className = 'calendar-day--busy'
        day.suffix = '•'
      } else {
        day.suffix = '•'
      }
    }
    return day
  },

  // 日期选择事件（per D-15）
  onSelectDate: function (e) {
    const timestamp = e.detail.value
    const d = new Date(timestamp)
    const dateStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`

    const dayBookings = (this.data.grouped[dateStr] || [])
      .map(b => ({
        ...b,
        statusLabel: bookingsService.getStatusLabel(b.status),
        statusColor: bookingsService.getStatusColor(b.status)
      }))
      .sort((a, b) => a.booking_time.localeCompare(b.booking_time))

    this.setData({
      selectedDate: dateStr,
      dayBookings,
      isBusyDay: dayBookings.length >= BUSY_DAY_THRESHOLD
    })
  },

  // 跳转预约详情
  goToDetail: function (e) {
    wx.navigateTo({ url: `/pages/admin/bookings/detail?id=${e.currentTarget.dataset.id}` })
  }
})
```

**3. 创建 `miniprogram/pages/admin/bookings/calendar.wxml`：**

```xml
<view class="calendar-page">
  <view class="loading-container" wx:if="{{loading}}">
    <t-loading theme="circular" size="80rpx" text="加载中..." />
  </view>

  <block wx:if="{{!loading}}">
    <!-- TDesign Calendar 内嵌模式（per D-12: usePopup=false, switchMode=month, readonly, single） -->
    <view class="calendar-wrap">
      <t-calendar
        value="{{calendarValue}}"
        minDate="{{minDate}}"
        format="{{format}}"
        switchMode="month"
        type="single"
        readonly="{{true}}"
        usePopup="{{false}}"
        bind:select="onSelectDate"
      />
    </view>

    <!-- 选中日期的预约列表区域 -->
    <view class="day-section" wx:if="{{selectedDate}}">
      <view class="day-header">
        <text class="day-title">{{selectedDate}}</text>
        <text class="day-count" wx:if="{{dayBookings.length}}">{{dayBookings.length}} 个预约</text>
      </view>

      <!-- 紧凑日程警告（per D-21: 3+ 预约显示橙色警告条） -->
      <view class="busy-warning" wx:if="{{isBusyDay}}">
        ⚠️ 紧凑日程：今日 {{dayBookings.length}} 个预约
      </view>

      <!-- 预约列表（复用 list.wxss 的 card 样式 per D-15） -->
      <view class="day-list" wx:if="{{dayBookings.length}}">
        <view class="card tappable" wx:for="{{dayBookings}}" wx:key="_id" bindtap="goToDetail" data-id="{{item._id}}">
          <view class="card-top">
            <text class="card-svc">{{item.service_name}}</text>
            <text class="badge badge--{{item.status}}">{{item.statusLabel}}</text>
          </view>
          <view class="card-info">
            <text>{{item.booking_time}}</text>
            <text wx:if="{{item.user_info.nickname}}"> · {{item.user_info.nickname}}</text>
          </view>
          <text class="card-note" wx:if="{{item.special_needs}}">{{item.special_needs}}</text>
        </view>
      </view>

      <!-- 无预约空状态 -->
      <view class="empty-day" wx:if="{{!dayBookings.length}}">
        <text class="empty-day-text">该日无预约</text>
      </view>
    </view>
  </block>
</view>
```

**4. 创建 `miniprogram/pages/admin/bookings/calendar.wxss`：**

```css
.calendar-page {
  min-height: 100vh;
  background-color: var(--bg);
}

.calendar-wrap {
  background-color: var(--bg-elevated);
  border-radius: 0 0 var(--radius-lg) var(--radius-lg);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.03);
  overflow: hidden;
}

/* 紧凑日程日期标记 — 橙色（per D-23） */
.calendar-day--busy .t-calendar__control {
  color: var(--orange);
}

.day-section {
  padding: 24rpx var(--page-pad);
}

.day-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.day-title {
  font-size: var(--fs-sm);
  font-weight: 600;
  color: var(--text-primary);
  letter-spacing: 1rpx;
}

.day-count {
  font-size: var(--fs-xs);
  color: var(--text-secondary);
}

/* 紧凑日程警告条（per D-21） */
.busy-warning {
  background-color: var(--orange-bg);
  color: var(--orange);
  font-size: var(--fs-xs);
  font-weight: 500;
  padding: 16rpx 24rpx;
  border-radius: var(--radius-sm);
  margin-bottom: 20rpx;
  letter-spacing: 1rpx;
}

/* 预约列表 — 复用 admin/bookings/list.wxss 的 card 样式 */
.day-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.card {
  background-color: var(--bg-elevated);
  border-radius: var(--radius-lg);
  padding: var(--page-pad);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.03);
}

.card-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16rpx;
}

.card-svc {
  font-size: var(--fs-sm);
  font-weight: 600;
  color: var(--text-primary);
  letter-spacing: 1rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  min-width: 0;
}

.badge {
  font-size: var(--fs-xs);
  font-weight: 500;
  padding: 6rpx 20rpx;
  border-radius: 999rpx;
  letter-spacing: 1rpx;
  flex-shrink: 0;
}

.badge--pending { background-color: var(--orange-bg); color: var(--orange); }
.badge--accepted { background-color: var(--green-bg); color: var(--green); }
.badge--completed { background-color: var(--purple-bg); color: var(--purple); }
.badge--cancelled,
.badge--rejected { background-color: var(--fill-primary); color: var(--text-secondary); }

.card-info {
  font-size: var(--fs-xs);
  color: var(--text-secondary);
  margin-top: 12rpx;
  letter-spacing: 1rpx;
}

.card-note {
  display: block;
  font-size: var(--fs-xs);
  color: var(--text-secondary);
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--separator);
  letter-spacing: 1rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.empty-day {
  display: flex;
  justify-content: center;
  padding: 64rpx 0;
}

.empty-day-text {
  font-size: var(--fs-xs);
  color: var(--text-tertiary);
}
```

**5. 修改 `miniprogram/pages/admin/bookings/list.wxml`：**

在 `<view class="filter-bar">` 的结束 `</view>` 标签之后、`<view class="list"` 之前，添加日历视图入口按钮：

```xml
<!-- 日历视图入口（per D-16） -->
<view class="calendar-entry tappable" bindtap="goToCalendar">
  <text class="calendar-entry-icon">📅</text>
  <text class="calendar-entry-text">日历视图</text>
</view>
```

**6. 修改 `miniprogram/pages/admin/bookings/list.js`：**

在 `goToDetail` 方法之后新增：

```javascript
goToCalendar: function () {
  wx.navigateTo({ url: '/pages/admin/bookings/calendar' })
}
```

**7. 在 `miniprogram/pages/admin/bookings/list.wxss` 末尾添加入口按钮样式：**

```css
.calendar-entry {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  margin: 16rpx var(--page-pad);
  padding: 20rpx;
  background-color: var(--accent-soft);
  border-radius: var(--radius-md);
}

.calendar-entry-icon {
  font-size: var(--fs-base);
}

.calendar-entry-text {
  font-size: var(--fs-sm);
  color: var(--accent);
  font-weight: 500;
  letter-spacing: 1rpx;
}
```
  </action>
  <verify>
    <automated>test -f miniprogram/pages/admin/bookings/calendar.js && test -f miniprogram/pages/admin/bookings/calendar.wxml && test -f miniprogram/pages/admin/bookings/calendar.wxss && test -f miniprogram/pages/admin/bookings/calendar.json && grep -q "goToCalendar" miniprogram/pages/admin/bookings/list.js && grep -q "calendar-entry" miniprogram/pages/admin/bookings/list.wxml && echo "Calendar page + entry complete"</automated>
  </verify>
  <done>
    - 日历页面 4 个文件创建完成（js/wxml/wxss/json）
    - calendar.json 引入 t-calendar 组件，导航栏标题 "预约日历"（per D-17）
    - calendar.js 实现 TDesign Calendar 内嵌模式（per D-12），format 标记有预约日期，选中日期显示预约列表
    - calendar.wxml 包含 t-calendar + 日预约列表 + 紧凑日程警告条
    - 有预约日期显示灰色 • 标记，3+ 预约日期显示橙色 • 标记（per D-14, D-23）
    - 3+ 预约显示橙色 "⚠️ 紧凑日程：今日 X 个预约" 警告条（per D-21, D-22）
    - 管理端预约列表页新增 "日历视图" 入口按钮（per D-16）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (getCalendarData) | 管理端日历数据请求，需 isArtist 验证 |
| 云函数→数据库 | getCalendarData 查询 bookings 集合 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | getCalendarData action | mitigate | 服务端 requireArtist 验证（Phase 6 已实现）确保非化妆师无法获取日历数据 |
| T-08-05 | Information Disclosure | calendar page | accept | 日历页面仅展示预约概要信息（服务名+时间+状态），不含敏感个人数据。且已通过 requireArtist 限制访问 |
</threat_model>

<verification>
1. 日历页面 4 个文件存在（js/wxml/wxss/json）
2. calendar.json 引入 t-calendar 组件
3. calendar.js 中 BUSY_DAY_THRESHOLD = 3
4. calendar.wxml 包含 t-calendar + busy-warning + day-list
5. getCalendarData action 在 bookings 云函数中存在
6. getCalendarData 在 bookings.js service 中存在
7. app.json admin pages 包含 bookings/calendar
8. admin/list 页面有 goToCalendar 方法和 calendar-entry 按钮
</verification>

<success_criteria>
- 化妆师在管理端预约列表页看到"日历视图"按钮，点击跳转日历页面
- 日历页面以月历内嵌模式展示，有预约日期显示圆点标记
- 点击日期后下方显示当天预约列表（按时间排序）
- 3+ 预约日期显示橙色标记，预约列表上方显示"紧凑日程"警告条
- getCalendarData 云函数通过 requireArtist 验证保护
</success_criteria>

<output>
After completion, create `.planning/phases/08-booking-notifications-calendar/08-02-SUMMARY.md`
</output>
