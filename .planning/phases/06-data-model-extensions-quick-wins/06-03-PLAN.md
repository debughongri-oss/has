---
phase: 06-data-model-extensions-quick-wins
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - miniprogram/utils/constants.js
  - miniprogram/pages/booking/create.js
  - miniprogram/pages/booking/create.wxml
  - miniprogram/pages/booking/create.wxss
  - miniprogram/pages/admin/bookings/detail.js
  - miniprogram/pages/admin/bookings/detail.wxml
  - miniprogram/pages/admin/bookings/detail.wxss
  - cloudfunctions/bookings/index.js
autonomous: true
requirements:
  - BOOK-09
  - BOOK-10
  - BOOK-11
  - BOOK-12

must_haves:
  truths:
    - "客户在预约表单看到肤质类型单选（标签按钮组，5个选项）"
    - "客户在预约表单看到特殊需求 textarea（最多200字）"
    - "客户在预约表单看到场合说明 textarea（最多200字）"
    - "原有单个留言 textarea 已移除，被三个结构化字段替代"
    - "提交预约时 skin_type/special_needs/occasion 三个字段发送到云函数"
    - "化妆师在管理后台预约详情页以独立行查看肤质/特殊需求/场合说明"
    - "旧预约数据中如有 notes 字段仍可正常展示（向后兼容）"
  artifacts:
    - path: "miniprogram/utils/constants.js"
      provides: "SKIN_TYPE_OPTIONS 常量（5个肤质选项）"
      contains: "SKIN_TYPE_OPTIONS"
    - path: "miniprogram/pages/booking/create.wxml"
      provides: "肤质标签按钮组 + 特殊需求 textarea + 场合说明 textarea（替代原 notes textarea）"
    - path: "miniprogram/pages/booking/create.js"
      provides: "skin_type/special_needs/occasion 数据绑定和提交逻辑"
    - path: "miniprogram/pages/admin/bookings/detail.wxml"
      provides: "结构化字段独立行展示（肤质/特殊需求/场合）"
    - path: "cloudfunctions/bookings/index.js"
      provides: "create action 接收新字段替代 notes"
  key_links:
    - from: "miniprogram/pages/booking/create.js"
      to: "miniprogram/utils/constants.js"
      via: "require SKIN_TYPE_OPTIONS"
      pattern: "SKIN_TYPE_OPTIONS"
    - from: "miniprogram/pages/booking/create.js"
      to: "cloudfunctions/bookings/index.js"
      via: "bookingsService.createBooking({ skin_type, special_needs, occasion })"
      pattern: "createBooking"
    - from: "cloudfunctions/bookings/index.js"
      to: "bookings collection"
      via: "db.collection('bookings').add({ data: { skin_type, special_needs, occasion } })"
      pattern: "skin_type.*special_needs.*occasion"
---

<objective>
用三个结构化字段（肤质类型/特殊需求/场合说明）替代现有预约留言 textarea，更新预约表单、云函数和管理后台展示。

Purpose: 预约备注更结构化，化妆师获得明确分类的客户需求信息，而不是一段模糊的留言。

Output: 预约表单含肤质单选+特殊需求+场合说明三字段，管理后台独立行展示（per D-05, D-06, D-07, D-08, D-09）
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-data-model-extensions-quick-wins/06-CONTEXT.md
@.planning/phases/06-data-model-extensions-quick-wins/06-01-SUMMARY.md

<interfaces>
<!-- 现有预约表单模式 -->

From miniprogram/pages/booking/create.js:
```javascript
// data: { services, selectedService, dateList, selectedDate, timeSlots, selectedTime, loadingSlots, notes, loading, submitting }
// notes 字段通过 onNotesInput 更新
// submitBooking 发送: { service_id, service_name, booking_date, booking_time, notes, user_info }
```

From miniprogram/pages/booking/create.wxml:
```xml
<!-- 留言 textarea 在 selectedTime 条件下展示（第 85-94 行）-->
<view class="form-block" wx:if="{{selectedTime}}">
  <text class="form-title">留言</text>
  <textarea class="note" placeholder="你的需求、肤质情况等..." value="{{notes}}" bindinput="onNotesInput" maxlength="200" />
</view>
```

From cloudfunctions/bookings/index.js (after Plan 01):
```javascript
// create case: 接收 service_id, service_name, booking_date, booking_time, notes, user_info
// booking 对象中: notes: notes || ''
```

From miniprogram/pages/admin/bookings/detail.wxml:
```xml
<!-- 现有留言展示（第 28-31 行）-->
<view class="row" wx:if="{{booking.notes}}">
  <text class="key">留言</text>
  <text class="val val--wrap">{{booking.notes}}</text>
</view>
```

From miniprogram/pages/admin/bookings/detail.js:
```javascript
// loadDetail: setData({ booking: data }) — 直接使用 booking 对象
// 无需修改 JS — 新字段通过 booking 对象自动到达模板
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加肤质选项常量 + 更新预约创建表单和云函数</name>
  <files>miniprogram/utils/constants.js, miniprogram/pages/booking/create.js, miniprogram/pages/booking/create.wxml, miniprogram/pages/booking/create.wxss, cloudfunctions/bookings/index.js</files>
  <action>Per D-05, D-06, D-07, D-09:

**1. miniprogram/utils/constants.js:**
添加 `SKIN_TYPE_OPTIONS` 常量（per D-06）：
```javascript
const SKIN_TYPE_OPTIONS = [
  { key: 'dry', label: '干性' },
  { key: 'oily', label: '油性' },
  { key: 'combination', label: '混合性' },
  { key: 'sensitive', label: '敏感性' },
  { key: 'unknown', label: '不确定' }
]
```
在 `module.exports` 中添加 `SKIN_TYPE_OPTIONS`。

**2. miniprogram/pages/booking/create.js:**
- 在 require 中添加 `const { SKIN_TYPE_OPTIONS } = require('../../utils/constants')`
- data 中：
  - 移除 `notes: ''`
  - 添加 `skinType: ''`（选中的肤质 key）
  - 添加 `specialNeeds: ''`（特殊需求文本）
  - 添加 `occasion: ''`（场合说明文本）
  - 添加 `skinTypeOptions: []`（onLoad 时设置）
- onLoad 中：`this.setData({ skinTypeOptions: SKIN_TYPE_OPTIONS.map(t => ({ ...t, selected: false })) })`，并在 `this.loadServices()` 之前
- 移除 `onNotesInput` 方法
- 添加新方法：
  - `onSelectSkinType(e)`: 获取 `e.currentTarget.dataset.key`，设置 `skinType`，更新 `skinTypeOptions` 的 selected 状态（单选：点击已选中则取消，点击未选中则选中并取消其他）
  - `onSpecialNeedsInput(e)`: `this.setData({ specialNeeds: e.detail.value })`
  - `onOccasionInput(e)`: `this.setData({ occasion: e.detail.value })`
- submitBooking 中：
  - 移除 `notes` 解构
  - 改为 `const { selectedService, selectedDate, selectedTime, skinType, specialNeeds, occasion } = this.data`
  - createBooking 参数中：移除 `notes`，替换为：
    ```javascript
    skin_type: skinType || '',
    special_needs: specialNeeds.trim().slice(0, 200),
    occasion: occasion.trim().slice(0, 200)
    ```

**3. miniprogram/pages/booking/create.wxml:**
替换第 85-94 行的留言 textarea 区域（`wx:if="{{selectedTime}}"` 条件内的"留言"表单块）为三个结构化字段（per D-07 平铺展示，不新增步骤）：

```xml
<view class="form-block" wx:if="{{selectedTime}}">
  <text class="form-title">肤质类型</text>
  <view class="skin-group">
    <view
      class="skin-btn tappable {{item.selected ? 'skin-btn--on' : ''}}"
      wx:for="{{skinTypeOptions}}"
      wx:key="key"
      bindtap="onSelectSkinType"
      data-key="{{item.key}}"
    >
      {{item.label}}
    </view>
  </view>
</view>

<view class="form-block" wx:if="{{selectedTime}}">
  <text class="form-title">特殊需求</text>
  <textarea
    class="note"
    placeholder="过敏史、皮肤问题等..."
    value="{{specialNeeds}}"
    bindinput="onSpecialNeedsInput"
    maxlength="200"
  />
</view>

<view class="form-block" wx:if="{{selectedTime}}">
  <text class="form-title">场合说明</text>
  <textarea
    class="note"
    placeholder="婚礼主题、活动类型等..."
    value="{{occasion}}"
    bindinput="onOccasionInput"
    maxlength="200"
  />
</view>
```

**4. miniprogram/pages/booking/create.wxss:**
在 `.note` 样式后添加肤质按钮组样式：
```css
.skin-group {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.skin-btn {
  padding: 14rpx 28rpx;
  border-radius: var(--radius-sm);
  font-size: var(--fs-xs);
  background-color: var(--fill-secondary);
  color: var(--text-secondary);
  border: 2rpx solid transparent;
}

.skin-btn--on {
  background-color: var(--accent-soft);
  color: var(--accent);
  border-color: var(--accent);
}
```

**5. cloudfunctions/bookings/index.js:**
在 `create` case 中（约第 60 行）：
- 解构时添加新字段：`const { service_id, service_name, booking_date, booking_time, skin_type, special_needs, occasion, user_info } = event`
- 移除 `notes`
- booking 对象中替换 `notes: notes || ''` 为：
  ```javascript
  skin_type: skin_type || '',
  special_needs: special_needs || '',
  occasion: occasion || '',
  ```
- **向后兼容**：不移除对 `notes` 的其他引用（sendNotify 函数中 `booking.notes || '无'`），改为 `booking.special_needs || booking.notes || '无'`，兼容旧数据。</action>
  <verify>
    <automated>node -e "
const fs = require('fs');
// Check constants
const c = fs.readFileSync('miniprogram/utils/constants.js','utf8');
console.assert(c.includes('SKIN_TYPE_OPTIONS'), 'constants: missing SKIN_TYPE_OPTIONS');
// Check create.js
const cj = fs.readFileSync('miniprogram/pages/booking/create.js','utf8');
console.assert(cj.includes('SKIN_TYPE_OPTIONS'), 'create.js: missing import');
console.assert(cj.includes('onSelectSkinType'), 'create.js: missing skin type handler');
console.assert(cj.includes('onSpecialNeedsInput'), 'create.js: missing special needs handler');
console.assert(cj.includes('onOccasionInput'), 'create.js: missing occasion handler');
console.assert(cj.includes('skin_type'), 'create.js: missing skin_type in submit');
console.assert(!cj.includes('notes,'), 'create.js: notes should be removed from submit');
// Check create.wxml
const cw = fs.readFileSync('miniprogram/pages/booking/create.wxml','utf8');
console.assert(cw.includes('skinTypeOptions'), 'create.wxml: missing skin type UI');
console.assert(cw.includes('specialNeeds'), 'create.wxml: missing special needs');
console.assert(cw.includes('occasion'), 'create.wxml: missing occasion');
// Check bookings cloud function
const bi = fs.readFileSync('cloudfunctions/bookings/index.js','utf8');
console.assert(bi.includes('skin_type'), 'bookings cf: missing skin_type');
console.assert(bi.includes('special_needs'), 'bookings cf: missing special_needs');
console.log('Booking notes restructure OK');
"
    </automated>
  </verify>
  <done>预约表单用肤质单选+特殊需求textarea+场合说明textarea替代留言textarea，云函数接收新字段，向后兼容旧 notes 数据</done>
</task>

<task type="auto">
  <name>Task 2: 更新管理后台预约详情展示结构化字段</name>
  <files>miniprogram/pages/admin/bookings/detail.wxml, miniprogram/pages/admin/bookings/detail.wxss</files>
  <action>Per D-08: 化妆师管理后台预约详情页用独立行显示三个结构化字段。

**1. miniprogram/pages/admin/bookings/detail.wxml:**

替换现有的留言展示（第 28-31 行）：
```xml
<view class="row" wx:if="{{booking.notes}}">
  <text class="key">留言</text>
  <text class="val val--wrap">{{booking.notes}}</text>
</view>
```

替换为（per D-08 与现有 key-value 行布局风格一致）：
```xml
<view class="row" wx:if="{{booking.skin_type}}">
  <text class="key">肤质</text>
  <text class="val">{{booking.skin_type}}</text>
</view>
<view class="row" wx:if="{{booking.special_needs}}">
  <text class="key">特殊需求</text>
  <text class="val val--wrap">{{booking.special_needs}}</text>
</view>
<view class="row" wx:if="{{booking.occasion}}">
  <text class="key">场合说明</text>
  <text class="val val--wrap">{{booking.occasion}}</text>
</view>
<view class="row" wx:if="{{booking.notes && !booking.skin_type}}">
  <text class="key">留言</text>
  <text class="val val--wrap">{{booking.notes}}</text>
</view>
```

向后兼容逻辑：新预约有 skin_type 等字段，旧预约只有 notes 字段。条件 `booking.notes && !booking.skin_type` 确保旧预约的 notes 仍能展示，新预约不再显示 notes（因为新预约不存 notes 字段）。

注意：肤质字段展示时需要把 key 转为中文标签。在 detail.js 的 loadDetail 回调中添加转换：
```javascript
// 在 setData 之后，转换 skin_type 为中文显示
if (data.skin_type) {
  const SKIN_LABELS = { dry: '干性', oily: '油性', combination: '混合性', sensitive: '敏感性', unknown: '不确定' }
  data.skin_type_label = SKIN_LABELS[data.skin_type] || data.skin_type
  this.setData({ 'booking.skin_type_label': data.skin_type_label })
}
```

然后在 wxml 中使用 `booking.skin_type_label` 代替 `booking.skin_type`：
```xml
<view class="row" wx:if="{{booking.skin_type}}">
  <text class="key">肤质</text>
  <text class="val">{{booking.skin_type_label || booking.skin_type}}</text>
</view>
```

**2. miniprogram/pages/admin/bookings/detail.wxss:**
无需新增样式 — 现有 `.row`、`.key`、`.val`、`.val--wrap` 样式完全匹配独立行展示需求。</action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const dw = fs.readFileSync('miniprogram/pages/admin/bookings/detail.wxml','utf8');
console.assert(dw.includes('skin_type'), 'detail.wxml: missing skin_type');
console.assert(dw.includes('special_needs'), 'detail.wxml: missing special_needs');
console.assert(dw.includes('occasion'), 'detail.wxml: missing occasion');
console.assert(dw.includes('肤质'), 'detail.wxml: missing 肤质 label');
console.assert(dw.includes('特殊需求'), 'detail.wxml: missing 特殊需求 label');
console.assert(dw.includes('场合说明'), 'detail.wxml: missing 场合说明 label');
// 向后兼容
console.assert(dw.includes('booking.notes && !booking.skin_type'), 'detail.wxml: missing backward compat');
console.log('Admin booking detail display OK');
"
    </automated>
  </verify>
  <done>管理后台预约详情页以独立行展示肤质/特殊需求/场合说明，旧预约 notes 字段仍可展示（向后兼容）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Cloud Function | 客户提交预约时传入肤质/需求/场合文本 |
| Cloud Function → Database | booking 文档存储客户端输入的文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | S (Spoofing) | booking create | accept | create 不需要 isArtist — 任何客户都应能创建预约 |
| T-06-08 | T (Tampering) | special_needs/occasion text | accept | 文本字段存储原样，长度限制在客户端 200 字和云函数 slice(0,200)。内容审核（msgSecCheck）属于 Phase 9 评价系统范围 |
</threat_model>

<verification>
1. constants.js 含 SKIN_TYPE_OPTIONS
2. 预约表单有肤质标签按钮组（5选项单选）+ 特殊需求 textarea + 场合说明 textarea
3. 原 notes textarea 已移除
4. 提交预约时发送 skin_type/special_needs/occasion
5. 云函数 create action 接收并存储新字段
6. 管理后台预约详情页独立行展示三个字段
7. 旧预约 notes 字段向后兼容展示
</verification>

<success_criteria>
- 客户可填写肤质类型（标签单选）、特殊需求（textarea 最多200字）、场合说明（textarea 最多200字）
- 提交后化妆师在管理后台以结构化独立行查看这三个字段
- 旧预约的 notes 字段仍正常展示
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-model-extensions-quick-wins/06-03-SUMMARY.md`
</output>
