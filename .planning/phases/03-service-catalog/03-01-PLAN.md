---
phase: 03-service-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudfunctions/services/index.js
  - cloudfunctions/services/package.json
  - cloudfunctions/services/config.json
  - miniprogram/services/services.js
  - miniprogram/pages/services/list.js
  - miniprogram/pages/services/list.json
  - miniprogram/pages/services/list.wxml
  - miniprogram/pages/services/list.wxss
  - miniprogram/pages/admin/services/list.js
  - miniprogram/pages/admin/services/list.json
  - miniprogram/pages/admin/services/list.wxml
  - miniprogram/pages/admin/services/list.wxss
  - miniprogram/pages/admin/services/edit.js
  - miniprogram/pages/admin/services/edit.json
  - miniprogram/pages/admin/services/edit.wxml
  - miniprogram/pages/admin/services/edit.wxss
  - miniprogram/app.json
  - miniprogram/pages/profile/index.js
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-03
  - SERV-04

must_haves:
  truths:
    - "用户以卡片列表形式浏览所有服务项目，每张卡片显示名称、时长预估、价格区间"
    - "化妆师可在管理后台创建新服务项目"
    - "化妆师可编辑已有服务项目"
    - "化妆师可删除已有服务项目"
  artifacts:
    - path: "cloudfunctions/services/index.js"
      provides: "Cloud function: services CRUD"
      exports: ["main"]
    - path: "miniprogram/services/services.js"
      provides: "Client-side services data access"
      contains: "getServicesList"
    - path: "miniprogram/pages/services/list.wxml"
      provides: "Service catalog card list"
      contains: "service-card"
    - path: "miniprogram/pages/admin/services/edit.js"
      provides: "Admin service create/edit form"
      contains: "saveService"
---

<objective>
Build the complete service catalog: client-facing service list with card layout, and artist admin CRUD for managing service items.

Purpose: Users need to know what services are offered before they can book (Phase 4 depends on services existing). This plan delivers both the public service catalog and the admin management interface.

Output: Services cloud function, client service list page, admin service management pages (list + edit), profile page navigation update.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</execution_context>

<context>

## Phase 3: Service Catalog — Plan 01

**Requirements:**
- SERV-01: Artist can create service items with name, duration estimate, price range, and description
- SERV-02: Artist can edit service items
- SERV-03: Artist can delete service items
- SERV-04: User can browse all service items in a card-style list

**Data model (from ARCHITECTURE.md):**
```
services (collection):
  _id: auto
  name: string
  description: string
  price: string (e.g., "¥500-800" or "面议")
  duration: number (minutes)
  category: string
  is_active: boolean
  sort_order: number
  created_at: date
  updated_at: date
```

**Existing services pattern (from Phase 2):**
- services/api.js → callCloudFunction(name, data)
- services/auth.js → isArtist()
- services/works.js → same CRUD pattern, follow this

**Existing admin pattern (from Phase 2):**
- pages/admin/works/list → admin list with edit/delete
- pages/admin/works/edit → admin create/edit form
- Follow same pattern for services

**app.json subPackages:** Add admin/services/list and admin/services/edit

**Profile page:** Update goToAdminServices to navigate to admin/services/list

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services cloud function and client service</name>
  <files>
    cloudfunctions/services/index.js
    cloudfunctions/services/package.json
    cloudfunctions/services/config.json
    miniprogram/services/services.js
  </files>

  <read_first>
  @miniprogram/services/api.js
  @miniprogram/services/works.js
  </read_first>

  <action>
  **1. Create cloudfunctions/services/index.js**:

  ```javascript
  const cloud = require('wx-server-sdk')
  cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
  const db = cloud.database()

  exports.main = async (event, context) => {
    switch (event.action) {
      case 'list': {
        try {
          const res = await db.collection('services')
            .where({ is_active: true })
            .orderBy('sort_order', 'asc')
            .orderBy('created_at', 'desc')
            .limit(100)
            .get()
          return { errCode: 0, data: res.data }
        } catch (error) {
          console.error('获取服务列表失败:', error)
          return { errCode: -1, errMsg: '获取服务列表失败' }
        }
      }

      case 'listAll': {
        try {
          const res = await db.collection('services')
            .orderBy('sort_order', 'asc')
            .orderBy('created_at', 'desc')
            .limit(100)
            .get()
          return { errCode: 0, data: res.data }
        } catch (error) {
          console.error('获取服务列表失败:', error)
          return { errCode: -1, errMsg: '获取服务列表失败' }
        }
      }

      case 'detail': {
        try {
          const res = await db.collection('services').doc(event.id).get()
          return { errCode: 0, data: res.data }
        } catch (error) {
          console.error('获取服务详情失败:', error)
          return { errCode: -1, errMsg: '服务不存在' }
        }
      }

      case 'create': {
        try {
          const data = {
            ...event.data,
            is_active: true,
            created_at: db.serverDate(),
            updated_at: db.serverDate()
          }
          const res = await db.collection('services').add({ data })
          return { errCode: 0, data: { _id: res._id } }
        } catch (error) {
          console.error('创建服务失败:', error)
          return { errCode: -1, errMsg: '创建服务失败' }
        }
      }

      case 'update': {
        try {
          const data = { ...event.data, updated_at: db.serverDate() }
          await db.collection('services').doc(event.id).update({ data })
          return { errCode: 0, data: { success: true } }
        } catch (error) {
          console.error('更新服务失败:', error)
          return { errCode: -1, errMsg: '更新服务失败' }
        }
      }

      case 'delete': {
        try {
          await db.collection('services').doc(event.id).remove()
          return { errCode: 0, data: { success: true } }
        } catch (error) {
          console.error('删除服务失败:', error)
          return { errCode: -1, errMsg: '删除服务失败' }
        }
      }

      default:
        return { errCode: -1, errMsg: '未知操作' }
    }
  }
  ```

  **2. Create cloudfunctions/services/package.json**:

  ```json
  {
    "name": "services",
    "version": "1.0.0",
    "main": "index.js",
    "dependencies": {
      "wx-server-sdk": "~3.0.4"
    }
  }
  ```

  **3. Create cloudfunctions/services/config.json**:

  ```json
  {
    "permissions": {
      "openapi": []
    }
  }
  ```

  **4. Create miniprogram/services/services.js**:

  ```javascript
  const { callCloudFunction } = require('./api')

  const getServicesList = async () => {
    const result = await callCloudFunction('services', { action: 'list' })
    if (result.errCode !== 0) throw new Error(result.errMsg || '获取服务列表失败')
    return result.data
  }

  const getAllServices = async () => {
    const result = await callCloudFunction('services', { action: 'listAll' })
    if (result.errCode !== 0) throw new Error(result.errMsg || '获取服务列表失败')
    return result.data
  }

  const getServiceDetail = async (id) => {
    const result = await callCloudFunction('services', { action: 'detail', id })
    if (result.errCode !== 0) throw new Error(result.errMsg || '获取服务详情失败')
    return result.data
  }

  const createService = async (data) => {
    const result = await callCloudFunction('services', { action: 'create', data })
    if (result.errCode !== 0) throw new Error(result.errMsg || '创建服务失败')
    return result.data
  }

  const updateService = async (id, data) => {
    const result = await callCloudFunction('services', { action: 'update', id, data })
    if (result.errCode !== 0) throw new Error(result.errMsg || '更新服务失败')
    return result.data
  }

  const deleteService = async (id) => {
    const result = await callCloudFunction('services', { action: 'delete', id })
    if (result.errCode !== 0) throw new Error(result.errMsg || '删除服务失败')
    return result.data
  }

  module.exports = {
    getServicesList,
    getAllServices,
    getServiceDetail,
    createService,
    updateService,
    deleteService
  }
  ```
  </action>

  <verify>
    <automated>test -f cloudfunctions/services/index.js && test -f cloudfunctions/services/package.json && test -f miniprogram/services/services.js && grep -q "getServicesList" miniprogram/services/services.js && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - Cloud function handles list, listAll, detail, create, update, delete
    - 'list' only returns active services (is_active: true)
    - 'listAll' returns all services (for admin)
    - Client service exports getServicesList, getAllServices, getServiceDetail, createService, updateService, deleteService
  </acceptance_criteria>

  <done>
  Services cloud function and client service module created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build client service list page</name>
  <files>
    miniprogram/pages/services/list.js
    miniprogram/pages/services/list.json
    miniprogram/pages/services/list.wxml
    miniprogram/pages/services/list.wxss
  </files>

  <read_first>
  @miniprogram/services/services.js
  </read_first>

  <action>
  **1. Update miniprogram/pages/services/list.json**:

  ```json
  {
    "usingComponents": {
      "t-loading": "tdesign-miniprogram/loading/loading",
      "t-empty": "tdesign-miniprogram/empty/empty"
    },
    "navigationBarTitleText": "服务项目",
    "enablePullDownRefresh": true
  }
  ```

  **2. Update miniprogram/pages/services/list.wxml**:

  ```xml
  <view class="page-services">
    <!-- 加载状态 -->
    <view class="loading-container" wx:if="{{loading}}">
      <t-loading theme="circular" size="80rpx" text="加载中..." />
    </view>

    <!-- 服务卡片列表 -->
    <view class="service-list" wx:if="{{!loading && services.length}}">
      <view class="service-card" wx:for="{{services}}" wx:key="_id">
        <view class="card-header">
          <text class="service-name">{{item.name}}</text>
          <text class="service-price">{{item.price}}</text>
        </view>
        <view class="card-meta">
          <text class="service-duration" wx:if="{{item.duration}}">⏱ {{item.duration}}分钟</text>
          <text class="service-category" wx:if="{{item.category}}">{{item.category}}</text>
        </view>
        <view class="card-desc" wx:if="{{item.description}}">
          <text>{{item.description}}</text>
        </view>
      </view>
    </view>

    <!-- 空状态 -->
    <view class="empty-container" wx:if="{{!loading && !services.length}}">
      <t-empty icon="shop" description="暂无服务项目" />
    </view>
  </view>
  ```

  **3. Update miniprogram/pages/services/list.js**:

  ```javascript
  const servicesService = require('../../services/services')

  Page({
    data: {
      services: [],
      loading: true
    },

    onLoad: function () {
      this.loadServices()
    },

    onPullDownRefresh: function () {
      this.loadServices().then(() => wx.stopPullDownRefresh())
    },

    loadServices: function () {
      this.setData({ loading: true })
      return servicesService.getServicesList()
        .then(data => {
          this.setData({ services: data, loading: false })
        })
        .catch(err => {
          console.error('加载服务失败:', err)
          this.setData({ loading: false })
          wx.showToast({ title: '加载失败', icon: 'none' })
        })
    }
  })
  ```

  **4. Update miniprogram/pages/services/list.wxss**:

  ```css
  .page-services {
    min-height: 100vh;
    background-color: var(--color-bg);
    padding: var(--spacing-md);
    padding-bottom: env(safe-area-inset-bottom);
  }

  .loading-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 50vh;
  }

  .service-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .service-card {
    background-color: var(--color-white);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    box-shadow: var(--shadow-card);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-sm);
  }

  .service-name {
    font-size: var(--font-size-lg);
    font-weight: bold;
    color: var(--color-text);
  }

  .service-price {
    font-size: var(--font-size-lg);
    font-weight: bold;
    color: var(--color-primary);
  }

  .card-meta {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-sm);
  }

  .service-duration {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .service-category {
    font-size: var(--font-size-sm);
    color: var(--color-primary);
    background-color: rgba(194, 24, 91, 0.08);
    padding: 2rpx 16rpx;
    border-radius: 999rpx;
  }

  .card-desc {
    font-size: var(--font-size-md);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-top: var(--spacing-sm);
    padding-top: var(--spacing-sm);
    border-top: 1rpx solid var(--color-border);
  }

  .empty-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 50vh;
  }
  ```
  </action>

  <verify>
    <automated>grep -q "getServicesList" miniprogram/pages/services/list.js && grep -q "service-card" miniprogram/pages/services/list.wxml && grep -q "service-price" miniprogram/pages/services/list.wxml && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - Service list page loads active services on mount
    - Each card shows: name, price, duration, category, description
    - Pull-down refresh supported
    - Loading and empty states present
  </acceptance_criteria>

  <done>
  Client service list page with card layout showing name, price, duration, category, description.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create admin services management pages</name>
  <files>
    miniprogram/pages/admin/services/list.js
    miniprogram/pages/admin/services/list.json
    miniprogram/pages/admin/services/list.wxml
    miniprogram/pages/admin/services/list.wxss
    miniprogram/pages/admin/services/edit.js
    miniprogram/pages/admin/services/edit.json
    miniprogram/pages/admin/services/edit.wxml
    miniprogram/pages/admin/services/edit.wxss
    miniprogram/app.json
    miniprogram/pages/profile/index.js
  </files>

  <read_first>
  @miniprogram/services/services.js
  @miniprogram/services/auth.js
  @miniprogram/pages/admin/works/list.js
  @miniprogram/pages/admin/works/edit.js
  @miniprogram/pages/profile/index.js
  @miniprogram/app.json
  </read_first>

  <action>
  **1. Create miniprogram/pages/admin/services/list.json**:

  ```json
  {
    "usingComponents": {
      "t-loading": "tdesign-miniprogram/loading/loading",
      "t-empty": "tdesign-miniprogram/empty/empty",
      "t-dialog": "tdesign-miniprogram/dialog/dialog"
    },
    "navigationBarTitleText": "服务管理"
  }
  ```

  **2. Create miniprogram/pages/admin/services/list.wxml**:

  ```xml
  <view class="admin-services">
    <view class="loading-container" wx:if="{{loading}}">
      <t-loading theme="circular" size="80rpx" text="加载中..." />
    </view>

    <block wx:if="{{!loading}}">
      <view class="add-btn" bindtap="goToAdd">
        <text>+ 添加服务</text>
      </view>

      <view class="admin-list" wx:if="{{services.length}}">
        <view class="admin-item" wx:for="{{services}}" wx:key="_id">
          <view class="item-info">
            <text class="item-name">{{item.name}}</text>
            <text class="item-meta">{{item.price}} · {{item.duration}}分钟</text>
          </view>
          <view class="item-actions">
            <view class="action-btn" bindtap="goToEdit" data-id="{{item._id}}">编辑</view>
            <view class="action-btn action-delete" bindtap="confirmDelete" data-id="{{item._id}}" data-index="{{index}}">删除</view>
          </view>
        </view>
      </view>

      <view class="empty-container" wx:if="{{!services.length}}">
        <t-empty icon="shop" description="还没有服务项目，点击上方添加" />
      </view>
    </block>

    <t-dialog
      visible="{{showDeleteDialog}}"
      title="确认删除"
      content="删除后无法恢复，确定要删除这个服务吗？"
      confirm-btn="删除"
      cancel-btn="取消"
      bind:confirm="onDeleteConfirm"
      bind:cancel="onDeleteCancel"
    />
  </view>
  ```

  **3. Create miniprogram/pages/admin/services/list.js**:

  ```javascript
  const servicesService = require('../../../services/services')
  const authService = require('../../../services/auth')

  Page({
    data: {
      services: [],
      loading: true,
      showDeleteDialog: false,
      deleteTargetId: '',
      deleteTargetIndex: -1
    },

    onLoad: function () {
      if (!authService.isArtist()) {
        wx.showToast({ title: '无权限访问', icon: 'none' })
        setTimeout(() => wx.navigateBack(), 1500)
        return
      }
      this.loadServices()
    },

    onShow: function () {
      if (authService.isArtist() && !this.data.loading) {
        this.loadServices()
      }
    },

    loadServices: function () {
      this.setData({ loading: true })
      servicesService.getAllServices()
        .then(data => {
          this.setData({ services: data, loading: false })
        })
        .catch(err => {
          console.error('加载服务列表失败:', err)
          this.setData({ loading: false })
        })
    },

    goToAdd: function () {
      wx.navigateTo({ url: '/pages/admin/services/edit' })
    },

    goToEdit: function (e) {
      wx.navigateTo({ url: `/pages/admin/services/edit?id=${e.currentTarget.dataset.id}` })
    },

    confirmDelete: function (e) {
      this.setData({
        showDeleteDialog: true,
        deleteTargetId: e.currentTarget.dataset.id,
        deleteTargetIndex: e.currentTarget.dataset.index
      })
    },

    onDeleteConfirm: function () {
      const { deleteTargetId } = this.data
      this.setData({ showDeleteDialog: false })
      wx.showLoading({ title: '删除中...' })
      servicesService.deleteService(deleteTargetId)
        .then(() => {
          wx.hideLoading()
          wx.showToast({ title: '已删除', icon: 'success' })
          this.loadServices()
        })
        .catch(err => {
          wx.hideLoading()
          console.error('删除失败:', err)
        })
    },

    onDeleteCancel: function () {
      this.setData({ showDeleteDialog: false })
    }
  })
  ```

  **4. Create miniprogram/pages/admin/services/list.wxss** (same pattern as admin works list):

  ```css
  .admin-services {
    min-height: 100vh;
    background-color: var(--color-bg);
  }

  .loading-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 50vh;
  }

  .add-btn {
    margin: var(--spacing-lg);
    padding: var(--spacing-lg);
    background-color: var(--color-primary);
    color: #fff;
    text-align: center;
    border-radius: var(--radius-md);
    font-size: var(--font-size-lg);
    font-weight: bold;
  }

  .admin-list {
    padding: 0 var(--spacing-lg);
  }

  .admin-item {
    display: flex;
    align-items: center;
    background-color: var(--color-white);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
    box-shadow: var(--shadow-card);
  }

  .item-info {
    flex: 1;
  }

  .item-name {
    display: block;
    font-size: var(--font-size-lg);
    font-weight: 500;
    color: var(--color-text);
    margin-bottom: var(--spacing-xs);
  }

  .item-meta {
    font-size: var(--font-size-sm);
    color: var(--color-text-placeholder);
  }

  .item-actions {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .action-btn {
    padding: var(--spacing-xs) var(--spacing-md);
    font-size: var(--font-size-sm);
    text-align: center;
    border-radius: var(--radius-sm);
    background-color: var(--color-bg);
    color: var(--color-text-secondary);
  }

  .action-delete {
    color: #e53935;
    background-color: rgba(229, 57, 53, 0.08);
  }

  .empty-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 40vh;
  }
  ```

  **5. Create miniprogram/pages/admin/services/edit.json**:

  ```json
  {
    "usingComponents": {},
    "navigationBarTitleText": "编辑服务"
  }
  ```

  **6. Create miniprogram/pages/admin/services/edit.wxml**:

  ```xml
  <view class="edit-service">
    <!-- 服务名称 -->
    <view class="form-section">
      <text class="form-label">服务名称</text>
      <input class="form-input" placeholder="如：新娘妆" value="{{name}}" bindinput="onNameInput" />
    </view>

    <!-- 分类 -->
    <view class="form-section">
      <text class="form-label">分类</text>
      <view class="category-picker">
        <view
          class="picker-item {{category === item.key ? 'picker-active' : ''}}"
          wx:for="{{categories}}"
          wx:key="key"
          bindtap="onCategoryPick"
          data-key="{{item.key}}"
        >{{item.label}}</view>
      </view>
    </view>

    <!-- 价格 -->
    <view class="form-section">
      <text class="form-label">价格</text>
      <input class="form-input" placeholder="如：¥500-800 或 面议" value="{{price}}" bindinput="onPriceInput" />
    </view>

    <!-- 时长 -->
    <view class="form-section">
      <text class="form-label">预计时长（分钟）</text>
      <input class="form-input" type="number" placeholder="如：120" value="{{duration}}" bindinput="onDurationInput" />
    </view>

    <!-- 描述 -->
    <view class="form-section">
      <text class="form-label">服务描述</text>
      <textarea class="form-textarea" placeholder="描述服务内容和包含项目..." value="{{description}}" bindinput="onDescInput" maxlength="500" />
    </view>

    <!-- 排序 -->
    <view class="form-section">
      <text class="form-label">排序（数字越小越靠前）</text>
      <input class="form-input" type="number" placeholder="0" value="{{sortOrder}}" bindinput="onSortInput" />
    </view>

    <!-- 保存 -->
    <view class="save-area">
      <button class="save-btn" bindtap="saveService" disabled="{{saving}}">{{saving ? '保存中...' : '保存服务'}}</button>
    </view>
  </view>
  ```

  **7. Create miniprogram/pages/admin/services/edit.js**:

  ```javascript
  const servicesService = require('../../../services/services')
  const authService = require('../../../services/auth')
  const { SERVICE_CATEGORIES } = require('../../../utils/constants')

  Page({
    data: {
      isEdit: false,
      serviceId: '',
      name: '',
      category: 'bridal',
      categories: SERVICE_CATEGORIES,
      price: '',
      duration: '',
      description: '',
      sortOrder: 0,
      saving: false
    },

    onLoad: function (options) {
      if (!authService.isArtist()) {
        wx.showToast({ title: '无权限访问', icon: 'none' })
        setTimeout(() => wx.navigateBack(), 1500)
        return
      }
      if (options.id) {
        this.setData({ isEdit: true, serviceId: options.id })
        wx.setNavigationBarTitle({ title: '编辑服务' })
        this.loadService(options.id)
      } else {
        wx.setNavigationBarTitle({ title: '添加服务' })
      }
    },

    loadService: function (id) {
      wx.showLoading({ title: '加载中...' })
      servicesService.getServiceDetail(id)
        .then(data => {
          this.setData({
            name: data.name,
            category: data.category || 'bridal',
            price: data.price || '',
            duration: data.duration ? String(data.duration) : '',
            description: data.description || '',
            sortOrder: data.sort_order || 0
          })
          wx.hideLoading()
        })
        .catch(err => {
          wx.hideLoading()
          console.error('加载服务失败:', err)
        })
    },

    onNameInput: function (e) { this.setData({ name: e.detail.value }) },
    onPriceInput: function (e) { this.setData({ price: e.detail.value }) },
    onDurationInput: function (e) { this.setData({ duration: e.detail.value }) },
    onDescInput: function (e) { this.setData({ description: e.detail.value }) },
    onSortInput: function (e) { this.setData({ sortOrder: Number(e.detail.value) || 0 }) },

    onCategoryPick: function (e) {
      this.setData({ category: e.currentTarget.dataset.key })
    },

    saveService: function () {
      const { name, category, price, duration, description, sortOrder } = this.data
      if (!name.trim()) {
        wx.showToast({ title: '请输入服务名称', icon: 'none' })
        return
      }

      this.setData({ saving: true })
      const data = {
        name: name.trim(),
        category,
        price: price.trim() || '面议',
        duration: Number(duration) || 0,
        description: description.trim(),
        sort_order: sortOrder
      }

      const promise = this.data.isEdit
        ? servicesService.updateService(this.data.serviceId, data)
        : servicesService.createService(data)

      promise.then(() => {
        this.setData({ saving: false })
        wx.showToast({ title: '保存成功', icon: 'success' })
        setTimeout(() => wx.navigateBack(), 1500)
      }).catch(err => {
        this.setData({ saving: false })
        console.error('保存失败:', err)
        wx.showToast({ title: '保存失败', icon: 'none' })
      })
    }
  })
  ```

  **8. Create miniprogram/pages/admin/services/edit.wxss** (reuse admin works edit styles):

  ```css
  .edit-service {
    min-height: 100vh;
    background-color: var(--color-bg);
    padding-bottom: 200rpx;
  }

  .form-section {
    background-color: var(--color-white);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-sm);
  }

  .form-label {
    display: block;
    font-size: var(--font-size-md);
    color: var(--color-text);
    font-weight: 500;
    margin-bottom: var(--spacing-sm);
  }

  .form-input {
    width: 100%;
    height: 80rpx;
    padding: 0 var(--spacing-md);
    border: 1rpx solid var(--color-border);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-md);
    box-sizing: border-box;
  }

  .form-textarea {
    width: 100%;
    height: 200rpx;
    padding: var(--spacing-md);
    border: 1rpx solid var(--color-border);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-md);
    box-sizing: border-box;
  }

  .category-picker {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
  }

  .picker-item {
    padding: var(--spacing-xs) var(--spacing-lg);
    background-color: var(--color-bg);
    color: var(--color-text-secondary);
    font-size: var(--font-size-sm);
    border-radius: 999rpx;
  }

  .picker-active {
    background-color: var(--color-primary);
    color: #fff;
  }

  .save-area {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--spacing-md) var(--spacing-lg);
    padding-bottom: calc(var(--spacing-md) + env(safe-area-inset-bottom));
    background-color: var(--color-white);
    box-shadow: 0 -4rpx 16rpx rgba(0, 0, 0, 0.06);
  }

  .save-btn {
    width: 100%;
    height: 88rpx;
    line-height: 88rpx;
    background-color: var(--color-primary);
    color: #fff;
    border-radius: var(--radius-md);
    font-size: var(--font-size-lg);
    border: none;
  }

  .save-btn::after {
    border: none;
  }

  .save-btn[disabled] {
    opacity: 0.6;
  }
  ```

  **9. Update miniprogram/app.json** — add admin services pages to subPackages:

  ```json
  "subPackages": [
    {
      "root": "pages/admin",
      "name": "admin",
      "pages": [
        "works/list",
        "works/edit",
        "services/list",
        "services/edit"
      ]
    }
  ]
  ```

  **10. Update miniprogram/pages/profile/index.js** — change goToAdminServices:

  Replace the toast with actual navigation:
  ```javascript
  goToAdminServices: function () {
    wx.navigateTo({ url: '/pages/admin/services/list' })
  }
  ```
  </action>

  <verify>
    <automated>test -f miniprogram/pages/admin/services/list.js && test -f miniprogram/pages/admin/services/edit.js && grep -q "isArtist" miniprogram/pages/admin/services/list.js && grep -q "chooseMedia" miniprogram/pages/admin/works/edit.js 2>/dev/null; grep -q "services/list" miniprogram/app.json && grep -q "services/edit" miniprogram/app.json && grep -q "admin/services/list" miniprogram/pages/profile/index.js && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - Admin services list verifies artist identity
    - Admin services list shows all services with edit/delete
    - Admin services edit supports create and edit modes
    - Edit form has: name, category, price, duration, description, sort_order
    - Delete shows confirmation dialog
    - Admin pages registered in app.json subPackages
    - Profile page navigates to admin services list
  </acceptance_criteria>

  <done>
  Admin services management: list with identity check, edit/create form with all fields, delete confirmation. Profile page navigation updated. App.json subPackages updated.
  </done>
</task>

</tasks>

<verification>
1. Services cloud function handles CRUD operations
2. Client service list page shows card layout with name, price, duration
3. Admin services list page verifies artist identity
4. Admin services edit page supports create/edit
5. Profile page navigates to admin services
6. All admin pages in subPackage
</verification>

<success_criteria>
- Users browse services in card-style list (SERV-04)
- Artist can create service items (SERV-01)
- Artist can edit service items (SERV-02)
- Artist can delete service items (SERV-03)
- Admin pages verify artist identity (Pitfall P12)
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-catalog/03-01-SUMMARY.md`
</output>
