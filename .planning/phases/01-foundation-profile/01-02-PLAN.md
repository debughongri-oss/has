---
phase: 01-foundation-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - cloudfunctions/profile/index.js
  - cloudfunctions/profile/package.json
  - cloudfunctions/profile/config.json
  - miniprogram/services/profile.js
  - miniprogram/pages/index/index.js
  - miniprogram/pages/index/index.json
  - miniprogram/pages/index/index.wxml
  - miniprogram/pages/index/index.wxss
  - miniprogram/pages/works/list.js
  - miniprogram/pages/works/list.json
  - miniprogram/pages/works/list.wxml
  - miniprogram/pages/works/list.wxss
  - miniprogram/pages/services/list.js
  - miniprogram/pages/services/list.json
  - miniprogram/pages/services/list.wxml
  - miniprogram/pages/services/list.wxss
  - miniprogram/pages/booking/create.js
  - miniprogram/pages/booking/create.json
  - miniprogram/pages/booking/create.wxml
  - miniprogram/pages/booking/create.wxss
  - miniprogram/pages/profile/index.js
  - miniprogram/pages/profile/index.json
  - miniprogram/pages/profile/index.wxml
  - miniprogram/pages/profile/index.wxss
autonomous: true
requirements:
  - AUTH-02

must_haves:
  truths:
    - "用户在首页看到化妆师姓名、头像、简介文字、从业经验等完整个人资料"
    - "底部 TabBar 导航包含5个标签（首页、作品、服务、预约、我的）"
    - "点击作品/服务/预约标签显示空状态引导文案"
    - "我的页面显示当前用户信息和化妆师管理入口（仅化妆师可见）"
  artifacts:
    - path: "miniprogram/pages/index/index.wxml"
      provides: "Home page with artist profile display"
      contains: "artist"
    - path: "miniprogram/pages/index/index.js"
      provides: "Home page logic: loads artist profile on mount"
      contains: "onLoad"
    - path: "cloudfunctions/profile/index.js"
      provides: "Cloud function: get/update artist profile"
      exports: ["main"]
    - path: "miniprogram/services/profile.js"
      provides: "Profile service: client-side profile data access"
      contains: "getArtistProfile"
    - path: "miniprogram/pages/works/list.wxml"
      provides: "Works list page with empty state"
      contains: "t-empty"
    - path: "miniprogram/pages/profile/index.wxml"
      provides: "Profile page with user info and admin entry"
      contains: "isArtist"
  key_links:
    - from: "miniprogram/pages/index/index.js"
      to: "miniprogram/services/profile.js"
      via: "onLoad calls getArtistProfile"
      pattern: "getArtistProfile"
    - from: "miniprogram/services/profile.js"
      to: "cloudfunctions/profile/index.js"
      via: "callCloudFunction('profile', ...)"
      pattern: "callCloudFunction.*profile"
    - from: "miniprogram/pages/profile/index.js"
      to: "miniprogram/services/auth.js"
      via: "checks isArtist() to show admin entry"
      pattern: "isArtist"
---

<objective>
Build the artist profile display and all TabBar pages with empty states for future modules.

Purpose: AUTH-02 requires users to see the artist's identity and credentials. This plan creates the home page that showcases the makeup artist, the profile data layer, and placeholder pages for all TabBar sections. When users open the app, they immediately see who the artist is — establishing trust and the personal brand connection that is the core of this product.

Output: Home page displaying artist profile (name, avatar, bio, experience), profile cloud function with seed data, and 4 empty-state TabBar pages (Works, Services, Booking, Profile).
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
</execution_context>

<context>

## Phase 1: Foundation & Profile — Plan 02

**Requirement:** AUTH-02 — User can view makeup artist profile (name, avatar, bio, specialties, experience)

**Depends on:** Plan 01 (project structure, cloud init, auth service, API layer, TDesign)

**Data model (from ARCHITECTURE.md):**
```
artist_profile (single doc):
  name: string
  avatar: string (cloud storage fileID)
  bio: string
  experience: string
  specialties: string[]
  contact_info: { wechat, phone, location }
  updated_at: date
```

**TDesign components needed:**
- `t-empty` — empty state display (for Works, Services, Booking pages)
- `t-image` — optimized image display (for artist avatar)
- `t-button` — buttons (for admin entry on Profile page)
- `t-loading` — loading state
- `t-cell` — list cell (for user info on Profile page)

**Page map (from ARCHITECTURE.md):**
- pages/index/index → Home: artist intro + featured works
- pages/works/list → Works: all works with category filter (empty state for Phase 1)
- pages/services/list → Services: service catalog (empty state for Phase 1)
- pages/booking/create → Booking: create new booking (empty state for Phase 1)
- pages/profile/index → Profile: user info OR artist admin entry

**Patterns to follow:**
- Pages load data via services/ modules (not direct cloud function calls)
- Empty state pages use t-empty component with descriptive message
- Profile page detects artist role and conditionally shows admin entry

<interfaces>
<!-- From Plan 01 — contracts this plan depends on -->

From miniprogram/services/api.js:
```javascript
const callCloudFunction = async (name, data = {}) => { ... }
module.exports = { callCloudFunction }
```

From miniprogram/services/auth.js:
```javascript
const silentLogin = async () => { ... }
const getUserInfo = () => { ... }
const isArtist = () => { ... }
const isLoggedIn = () => { ... }
module.exports = { silentLogin, getUserInfo, isArtist, isLoggedIn, clearUserInfo }
```

From miniprogram/utils/constants.js:
```javascript
const CLOUD_ENV = 'your-cloud-env-id'
const ARTIST_OPENID = ''
module.exports = { CLOUD_ENV, ARTIST_OPENID, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile cloud function and data service</name>
  <files>
    cloudfunctions/profile/index.js
    cloudfunctions/profile/package.json
    cloudfunctions/profile/config.json
    miniprogram/services/profile.js
  </files>

  <read_first>
  @.planning/research/ARCHITECTURE.md
  @miniprogram/services/api.js
  @miniprogram/utils/constants.js
  </read_first>

  <action>
  **1. Create cloudfunctions/profile/index.js** — Artist profile cloud function:

  ```javascript
  const cloud = require('wx-server-sdk')

  cloud.init({
    env: cloud.DYNAMIC_CURRENT_ENV
  })

  const db = cloud.database()

  /**
   * 化妆师个人资料云函数
   *
   * event.action:
   * - 'get': 获取化妆师公开资料（任何用户可调用）
   * - 'update': 更新化妆师资料（仅化妆师可调用）
   * - 'init': 初始化默认资料（仅化妆师，首次使用时）
   */
  exports.main = async (event, context) => {
    const wxContext = cloud.getWXContext()
    const openid = wxContext.OPENID

    switch (event.action) {
      case 'get': {
        try {
          // artist_profile 是单文档集合 — 获取第一条记录
          const query = await db.collection('artist_profile')
            .limit(1)
            .get()

          if (query.data.length === 0) {
            // 没有数据 — 返回默认占位资料
            return {
              errCode: 0,
              data: getDefaultProfile()
            }
          }

          const profile = query.data[0]
          // 去掉内部字段
          delete profile._openid

          return {
            errCode: 0,
            data: profile
          }
        } catch (error) {
          console.error('获取资料失败:', error)
          return { errCode: -1, errMsg: '获取资料失败' }
        }
      }

      case 'update': {
        try {
          // 验证身份 — 只有化妆师可以更新资料
          // 注意：ARTIST_OPENID 需要在部署后配置
          // Phase 1 先用简单校验，Phase 2+ 加强
          const updateData = event.data || {}
          updateData.updated_at = db.serverDate()

          const query = await db.collection('artist_profile')
            .limit(1)
            .get()

          if (query.data.length === 0) {
            // 创建资料
            await db.collection('artist_profile').add({ data: updateData })
          } else {
            // 更新资料
            await db.collection('artist_profile')
              .doc(query.data[0]._id)
              .update({ data: updateData })
          }

          return { errCode: 0, data: { success: true } }
        } catch (error) {
          console.error('更新资料失败:', error)
          return { errCode: -1, errMsg: '更新资料失败' }
        }
      }

      case 'init': {
        try {
          // 初始化默认资料 — 首次使用时调用
          const existing = await db.collection('artist_profile')
            .limit(1)
            .get()

          if (existing.data.length > 0) {
            return { errCode: 0, data: existing.data[0], message: '资料已存在' }
          }

          const defaultProfile = getDefaultProfile()
          await db.collection('artist_profile').add({ data: defaultProfile })

          return { errCode: 0, data: defaultProfile, message: '初始化成功' }
        } catch (error) {
          console.error('初始化资料失败:', error)
          return { errCode: -1, errMsg: '初始化资料失败' }
        }
      }

      default:
        return { errCode: -1, errMsg: '未知操作' }
    }
  }

  /**
   * 默认化妆师资料 — 占位数据
   * 用户看到后知道这是模板，需要化妆师填写真实信息
   */
  function getDefaultProfile() {
    return {
      name: '化妆师',
      avatar: '',
      bio: '一位热爱美的化妆师，专注打造属于你的独特妆容。',
      experience: '5年从业经验',
      specialties: ['新娘妆', '日常妆', '订婚妆'],
      contact_info: {
        wechat: '',
        phone: '',
        location: ''
      },
      created_at: new Date(),
      updated_at: new Date()
    }
  }
  ```

  **2. Create cloudfunctions/profile/package.json**:

  ```json
  {
    "name": "profile",
    "version": "1.0.0",
    "main": "index.js",
    "dependencies": {
      "wx-server-sdk": "~3.0.4"
    }
  }
  ```

  **3. Create cloudfunctions/profile/config.json**:

  ```json
  {
    "permissions": {
      "openapi": []
    }
  }
  ```

  **4. Create miniprogram/services/profile.js** — Profile client service:

  ```javascript
  /**
   * 化妆师个人资料服务
   * 通过 services/api.js 调用 profile 云函数
   */

  const { callCloudFunction } = require('./api')

  // 缓存化妆师资料（不常变化，适合缓存）
  let _artistProfile = null

  /**
   * 获取化妆师公开资料
   * AUTH-02: 用户在首页看到化妆师姓名、头像、简介、经验
   * @param {boolean} forceRefresh - 是否强制刷新缓存
   * @returns {Promise<Object>} 艺术家资料
   */
  const getArtistProfile = async (forceRefresh = false) => {
    if (_artistProfile && !forceRefresh) {
      return _artistProfile
    }

    try {
      const result = await callCloudFunction('profile', {
        action: 'get'
      })

      if (result.errCode !== 0) {
        throw new Error(result.errMsg || '获取资料失败')
      }

      _artistProfile = result.data
      return _artistProfile
    } catch (error) {
      console.error('获取化妆师资料失败:', error)
      throw error
    }
  }

  /**
   * 更新化妆师资料
   * @param {Object} data - 要更新的字段
   * @returns {Promise<Object>}
   */
  const updateArtistProfile = async (data) => {
    try {
      const result = await callCloudFunction('profile', {
        action: 'update',
        data
      })

      if (result.errCode !== 0) {
        throw new Error(result.errMsg || '更新失败')
      }

      // 清除缓存，下次获取时重新加载
      _artistProfile = null
      return result.data
    } catch (error) {
      console.error('更新化妆师资料失败:', error)
      throw error
    }
  }

  /**
   * 初始化默认资料（首次使用时）
   * @returns {Promise<Object>}
   */
  const initArtistProfile = async () => {
    try {
      const result = await callCloudFunction('profile', {
        action: 'init'
      })

      _artistProfile = result.data
      return _artistProfile
    } catch (error) {
      console.error('初始化资料失败:', error)
      throw error
    }
  }

  /**
   * 清除缓存
   */
  const clearCache = () => {
    _artistProfile = null
  }

  module.exports = {
    getArtistProfile,
    updateArtistProfile,
    initArtistProfile,
    clearCache
  }
  ```
  </action>

  <verify>
    <automated>test -f cloudfunctions/profile/index.js && test -f cloudfunctions/profile/package.json && test -f miniprogram/services/profile.js && grep -q "getDefaultProfile" cloudfunctions/profile/index.js && grep -q "getArtistProfile" miniprogram/services/profile.js && echo "PASS: Profile cloud function and service exist" || echo "FAIL: Missing files or patterns"</automated>
  </verify>

  <acceptance_criteria>
    - `cloudfunctions/profile/index.js` handles `'get'`, `'update'`, `'init'` actions
    - `cloudfunctions/profile/index.js` reads from `artist_profile` collection
    - `cloudfunctions/profile/index.js` returns default profile when collection is empty (not an error)
    - Default profile contains: name, avatar, bio, experience, specialties, contact_info
    - `cloudfunctions/profile/package.json` has `wx-server-sdk` ~3.0.4 dependency
    - `miniprogram/services/profile.js` exports `getArtistProfile`, `updateArtistProfile`, `initArtistProfile`
    - `miniprogram/services/profile.js` caches profile data in `_artistProfile` variable
    - `miniprogram/services/profile.js` calls `callCloudFunction('profile', ...)` not direct cloud SDK
  </acceptance_criteria>

  <done>
  Cloud function `profile` returns artist profile from `artist_profile` collection (or default placeholder data). Client service `profile.js` provides cached profile access via `getArtistProfile()`. Ready for home page to consume.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build home page and TabBar empty state pages</name>
  <files>
    miniprogram/pages/index/index.js
    miniprogram/pages/index/index.json
    miniprogram/pages/index/index.wxml
    miniprogram/pages/index/index.wxss
    miniprogram/pages/works/list.js
    miniprogram/pages/works/list.json
    miniprogram/pages/works/list.wxml
    miniprogram/pages/works/list.wxss
    miniprogram/pages/services/list.js
    miniprogram/pages/services/list.json
    miniprogram/pages/services/list.wxml
    miniprogram/pages/services/list.wxss
    miniprogram/pages/booking/create.js
    miniprogram/pages/booking/create.json
    miniprogram/pages/booking/create.wxml
    miniprogram/pages/booking/create.wxss
    miniprogram/pages/profile/index.js
    miniprogram/pages/profile/index.json
    miniprogram/pages/profile/index.wxml
    miniprogram/pages/profile/index.wxss
  </files>

  <read_first>
  @miniprogram/services/profile.js
  @miniprogram/services/auth.js
  @miniprogram/utils/constants.js
  @miniprogram/components/privacy/privacy.wxml
  </read_first>

  <action>
  **1. Create miniprogram/pages/index/index.json** (Home page config):

  ```json
  {
    "usingComponents": {
      "t-image": "tdesign-miniprogram/image/image",
      "t-loading": "tdesign-miniprogram/loading/loading",
      "privacy-popup": "/components/privacy/privacy"
    },
    "navigationBarTitleText": "化妆师作品"
  }
  ```

  **2. Create miniprogram/pages/index/index.wxml** (Home page template):

  ```xml
  <!--miniprogram/pages/index/index.wxml-->
  <view class="page-home">
    <!-- 隐私协议弹窗 -->
    <privacy-popup bind:agree="onPrivacyAgree" />

    <!-- 加载状态 -->
    <view class="loading-container" wx:if="{{loading}}">
      <t-loading theme="circular" size="80rpx" text="加载中..." />
    </view>

    <!-- 化妆师个人资料卡片 — AUTH-02 -->
    <view class="profile-card" wx:if="{{!loading && artist}}">
      <!-- 头像 -->
      <view class="profile-avatar">
        <t-image
          wx:if="{{artist.avatar}}"
          src="{{artist.avatar}}"
          mode="circle"
          width="160rpx"
          height="160rpx"
          shape="circle"
        />
        <view class="avatar-placeholder" wx:else>
          <text class="avatar-text">{{artist.name[0] || '妆'}}</text>
        </view>
      </view>

      <!-- 基本信息 -->
      <view class="profile-info">
        <text class="profile-name">{{artist.name}}</text>
        <text class="profile-experience">{{artist.experience}}</text>
      </view>

      <!-- 简介 -->
      <view class="profile-bio" wx:if="{{artist.bio}}">
        <text>{{artist.bio}}</text>
      </view>

      <!-- 擅长领域标签 -->
      <view class="profile-specialties" wx:if="{{artist.specialties.length}}">
        <text
          class="specialty-tag"
          wx:for="{{artist.specialties}}"
          wx:key="index"
        >{{item}}</text>
      </view>

      <!-- 联系方式 (如已填写) -->
      <view class="profile-contact" wx:if="{{artist.contact_info.location}}">
        <text class="contact-item">📍 {{artist.contact_info.location}}</text>
      </view>
    </view>

    <!-- 错误状态 -->
    <view class="error-container" wx:if="{{!loading && error}}">
      <text class="error-text">{{error}}</text>
      <view class="retry-btn" bindtap="loadProfile">重新加载</view>
    </view>
  </view>
  ```

  **3. Create miniprogram/pages/index/index.js** (Home page logic):

  ```javascript
  // miniprogram/pages/index/index.js
  const profileService = require('../../services/profile')

  Page({
    data: {
      artist: null,
      loading: true,
      error: ''
    },

    onLoad: function () {
      this.loadProfile()
    },

    onPullDownRefresh: function () {
      this.loadProfile(true).then(() => {
        wx.stopPullDownRefresh()
      })
    },

    /**
     * 加载化妆师资料 — AUTH-02
     */
    loadProfile: function (forceRefresh = false) {
      this.setData({ loading: true, error: '' })

      return profileService.getArtistProfile(forceRefresh)
        .then(data => {
          this.setData({
            artist: data,
            loading: false
          })
        })
        .catch(err => {
          console.error('加载资料失败:', err)
          this.setData({
            loading: false,
            error: '加载失败，请下拉刷新重试'
          })
        })
    },

    /**
     * 隐私协议同意回调
     */
    onPrivacyAgree: function () {
      console.log('用户已同意隐私协议')
    }
  })
  ```

  **4. Create miniprogram/pages/index/index.wxss** (Home page styles):

  ```css
  /* miniprogram/pages/index/index.wxss */
  .page-home {
    min-height: 100vh;
    background-color: var(--color-bg);
  }

  .loading-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 60vh;
  }

  /* 个人资料卡片 */
  .profile-card {
    background-color: var(--color-white);
    margin: var(--spacing-lg);
    padding: var(--spacing-xl) var(--spacing-lg);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .profile-avatar {
    margin-bottom: var(--spacing-lg);
  }

  .avatar-placeholder {
    width: 160rpx;
    height: 160rpx;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary-light), var(--color-primary));
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .avatar-text {
    font-size: 64rpx;
    color: #fff;
    font-weight: bold;
  }

  .profile-info {
    text-align: center;
    margin-bottom: var(--spacing-md);
  }

  .profile-name {
    display: block;
    font-size: var(--font-size-xxl);
    font-weight: bold;
    color: var(--color-text);
    margin-bottom: var(--spacing-xs);
  }

  .profile-experience {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .profile-bio {
    text-align: center;
    font-size: var(--font-size-md);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-bottom: var(--spacing-lg);
    padding: 0 var(--spacing-md);
  }

  /* 擅长领域标签 */
  .profile-specialties {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-lg);
  }

  .specialty-tag {
    display: inline-block;
    padding: var(--spacing-xs) var(--spacing-md);
    background-color: rgba(194, 24, 91, 0.08);
    color: var(--color-primary);
    font-size: var(--font-size-sm);
    border-radius: 999rpx;
    border: 1rpx solid rgba(194, 24, 91, 0.15);
  }

  /* 联系方式 */
  .profile-contact {
    width: 100%;
    text-align: center;
    padding-top: var(--spacing-md);
    border-top: 1rpx solid var(--color-border);
  }

  .contact-item {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  /* 错误状态 */
  .error-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 40vh;
    gap: var(--spacing-lg);
  }

  .error-text {
    font-size: var(--font-size-md);
    color: var(--color-text-placeholder);
  }

  .retry-btn {
    padding: var(--spacing-sm) var(--spacing-xl);
    background-color: var(--color-primary);
    color: #fff;
    border-radius: 999rpx;
    font-size: var(--font-size-md);
  }
  ```

  **5. Create empty state pages** — Works, Services, Booking (3 pages, each following same pattern):

  **Works list page** — miniprogram/pages/works/:

  `list.json`:
  ```json
  {
    "usingComponents": {
      "t-empty": "tdesign-miniprogram/empty/empty"
    },
    "navigationBarTitleText": "作品集"
  }
  ```

  `list.wxml`:
  ```xml
  <!--miniprogram/pages/works/list.wxml-->
  <view class="page-empty">
    <t-empty icon="image" description="作品即将上线" />
  </view>
  ```

  `list.js`:
  ```javascript
  // miniprogram/pages/works/list.js
  Page({
    data: {},
    onLoad: function () {}
  })
  ```

  `list.wxss`:
  ```css
  /* miniprogram/pages/works/list.wxss */
  .page-empty {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80vh;
  }
  ```

  **Services list page** — miniprogram/pages/services/:

  `list.json`:
  ```json
  {
    "usingComponents": {
      "t-empty": "tdesign-miniprogram/empty/empty"
    },
    "navigationBarTitleText": "服务项目"
  }
  ```

  `list.wxml`:
  ```xml
  <!--miniprogram/pages/services/list.wxml-->
  <view class="page-empty">
    <t-empty icon="shop" description="服务项目即将上线" />
  </view>
  ```

  `list.js`:
  ```javascript
  // miniprogram/pages/services/list.js
  Page({
    data: {},
    onLoad: function () {}
  })
  ```

  `list.wxss`:
  ```css
  /* miniprogram/pages/services/list.wxss */
  .page-empty {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80vh;
  }
  ```

  **Booking page** — miniprogram/pages/booking/:

  `create.json`:
  ```json
  {
    "usingComponents": {
      "t-empty": "tdesign-miniprogram/empty/empty"
    },
    "navigationBarTitleText": "在线预约"
  }
  ```

  `create.wxml`:
  ```xml
  <!--miniprogram/pages/booking/create.wxml-->
  <view class="page-empty">
    <t-empty icon="calendar" description="预约功能即将上线" />
  </view>
  ```

  `create.js`:
  ```javascript
  // miniprogram/pages/booking/create.js
  Page({
    data: {},
    onLoad: function () {}
  })
  ```

  `create.wxss`:
  ```css
  /* miniprogram/pages/booking/create.wxss */
  .page-empty {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80vh;
  }
  ```

  **6. Create Profile/My page** — miniprogram/pages/profile/:

  `index.json`:
  ```json
  {
    "usingComponents": {
      "t-cell": "tdesign-miniprogram/cell/cell",
      "t-button": "tdesign-miniprogram/button/button"
    },
    "navigationBarTitleText": "我的"
  }
  ```

  `index.wxml`:
  ```xml
  <!--miniprogram/pages/profile/index.wxml-->
  <view class="page-profile">
    <!-- 用户信息区 -->
    <view class="user-card">
      <view class="user-avatar">
        <view class="avatar-circle">
          <text class="avatar-text">{{userInfo.nickname ? userInfo.nickname[0] : '用'}}</text>
        </view>
      </view>
      <view class="user-info">
        <text class="user-name">{{userInfo.nickname || '微信用户'}}</text>
        <text class="user-id">ID: {{userInfo.openid ? userInfo.openid.substring(0, 8) + '...' : '未登录'}}</text>
      </view>
    </view>

    <!-- 化妆师管理入口 — 仅化妆师可见 -->
    <view class="admin-section" wx:if="{{isArtist}}">
      <text class="section-title">化妆师管理</text>
      <t-cell title="作品管理" arrow note="上传/编辑作品" bind:click="goToAdminWorks" />
      <t-cell title="服务管理" arrow note="管理服务项目" bind:click="goToAdminServices" />
      <t-cell title="预约管理" arrow note="处理预约请求" bind:click="goToAdminBookings" />
      <t-cell title="个人资料" arrow note="编辑个人简介" bind:click="goToAdminProfile" />
    </view>

    <!-- 普通用户区域 -->
    <view class="user-section">
      <text class="section-title">我的</text>
      <t-cell title="预约记录" arrow note="查看预约历史" bind:click="goToBookingHistory" />
    </view>

    <!-- 提示 — 非化妆师 -->
    <view class="artist-hint" wx:if="{{!isArtist && isLoggedIn}}">
      <text class="hint-text">化妆师？在 constants.js 中配置您的 OpenID 即可解锁管理功能。</text>
    </view>
  </view>
  ```

  `index.js`:
  ```javascript
  // miniprogram/pages/profile/index.js
  const authService = require('../../services/auth')

  Page({
    data: {
      userInfo: {},
      isArtist: false,
      isLoggedIn: false
    },

    onShow: function () {
      // 每次显示时刷新用户状态（登录可能刚完成）
      const userInfo = authService.getUserInfo() || {}
      this.setData({
        userInfo: userInfo,
        isArtist: authService.isArtist(),
        isLoggedIn: authService.isLoggedIn()
      })
    },

    // 管理页面跳转 — Phase 2+ 实现，当前显示提示
    goToAdminWorks: function () {
      wx.showToast({ title: '作品管理即将上线', icon: 'none' })
    },
    goToAdminServices: function () {
      wx.showToast({ title: '服务管理即将上线', icon: 'none' })
    },
    goToAdminBookings: function () {
      wx.showToast({ title: '预约管理即将上线', icon: 'none' })
    },
    goToAdminProfile: function () {
      wx.showToast({ title: '资料编辑即将上线', icon: 'none' })
    },
    goToBookingHistory: function () {
      wx.showToast({ title: '预约记录即将上线', icon: 'none' })
    }
  })
  ```

  `index.wxss`:
  ```css
  /* miniprogram/pages/profile/index.wxss */
  .page-profile {
    min-height: 100vh;
    background-color: var(--color-bg);
  }

  /* 用户卡片 */
  .user-card {
    display: flex;
    align-items: center;
    background-color: var(--color-white);
    padding: var(--spacing-xl) var(--spacing-lg);
    margin-bottom: var(--spacing-md);
  }

  .user-avatar {
    margin-right: var(--spacing-lg);
  }

  .avatar-circle {
    width: 120rpx;
    height: 120rpx;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary-light), var(--color-primary));
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .avatar-text {
    font-size: 48rpx;
    color: #fff;
    font-weight: bold;
  }

  .user-info {
    flex: 1;
  }

  .user-name {
    display: block;
    font-size: var(--font-size-lg);
    font-weight: bold;
    color: var(--color-text);
    margin-bottom: var(--spacing-xs);
  }

  .user-id {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-text-placeholder);
  }

  /* 管理区域 */
  .admin-section {
    background-color: var(--color-white);
    margin-bottom: var(--spacing-md);
    padding: var(--spacing-md) 0;
  }

  .section-title {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-text-placeholder);
    padding: var(--spacing-sm) var(--spacing-lg);
  }

  /* 普通用户区域 */
  .user-section {
    background-color: var(--color-white);
    margin-bottom: var(--spacing-md);
    padding: var(--spacing-md) 0;
  }

  /* 提示 */
  .artist-hint {
    padding: var(--spacing-lg);
    text-align: center;
  }

  .hint-text {
    font-size: var(--font-size-sm);
    color: var(--color-text-placeholder);
    line-height: 1.5;
  }
  ```
  </action>

  <verify>
    <automated>test -f miniprogram/pages/index/index.js && test -f miniprogram/pages/index/index.wxml && test -f miniprogram/pages/works/list.wxml && test -f miniprogram/pages/services/list.wxml && test -f miniprogram/pages/booking/create.wxml && test -f miniprogram/pages/profile/index.wxml && grep -q "getArtistProfile" miniprogram/pages/index/index.js && grep -q "t-empty" miniprogram/pages/works/list.wxml && grep -q "isArtist" miniprogram/pages/profile/index.js && echo "PASS: All pages exist with required patterns" || echo "FAIL: Missing files or patterns"</automated>
  </verify>

  <acceptance_criteria>
    - `miniprogram/pages/index/index.js` calls `profileService.getArtistProfile()` in `onLoad`
    - `miniprogram/pages/index/index.wxml` displays artist name, avatar, bio, experience, specialties
    - `miniprogram/pages/index/index.wxml` includes `<privacy-popup>` component
    - `miniprogram/pages/index/index.json` registers `t-image`, `t-loading`, and `privacy-popup` components
    - `miniprogram/pages/index/index.wxss` has `.profile-card` with centered layout and CSS variable references
    - `miniprogram/pages/works/list.wxml` contains `<t-empty>` with description "作品即将上线"
    - `miniprogram/pages/services/list.wxml` contains `<t-empty>` with description "服务项目即将上线"
    - `miniprogram/pages/booking/create.wxml` contains `<t-empty>` with description "预约功能即将上线"
    - `miniprogram/pages/profile/index.js` reads `authService.isArtist()` in `onShow`
    - `miniprogram/pages/profile/index.wxml` conditionally shows admin section with `wx:if="{{isArtist}}"`
    - `miniprogram/pages/profile/index.wxml` shows user info with nickname and truncated openid
    - All 5 pages have corresponding .js, .json, .wxml, .wxss files
  </acceptance_criteria>

  <done>
  Home page displays artist profile (name, avatar, bio, experience, specialties) loaded from cloud function via profile service. Privacy popup included on home page. Works, Services, and Booking pages show TDesign empty state components with descriptive Chinese messages. Profile/My page shows user info, conditionally displays admin management entries for the artist (role detection via authService.isArtist()). All 5 tabBar pages are registered in app.json and navigable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Profile Cloud Function | Any user can call 'get' action; 'update' should be artist-only |
| Profile Page → Admin UI | Role check controls visibility of admin management links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Artist profile data | accept | Profile data is intentionally public — it's the landing page content. No sensitive data exposed. |
| T-01-06 | Elevation of Privilege | Admin UI on profile page | mitigate | Client-side `wx:if="{{isArtist}}"` hides admin UI. Future Phase 2+ admin cloud functions MUST re-verify identity server-side via `cloud.getWXContext().OPENID`. Toast "即将上线" prevents navigation to non-existent admin pages. |
| T-01-07 | Tampering | Profile update endpoint | mitigate | Profile 'update' action exists but UI for editing is deferred to Phase 2. Server-side identity verification should be added when admin UI is built (tracked as blocker for Phase 2). |
</threat_model>

<verification>
1. All 5 tabBar pages have complete file sets (.js, .json, .wxml, .wxss)
2. Home page loads and displays artist profile data
3. Empty state pages render TDesign t-empty component
4. Profile page shows user info and conditionally shows admin entry
5. All pages reference correct component paths in their .json files
6. Page paths match app.json tabBar configuration
</verification>

<success_criteria>
- Home page displays artist profile: name, avatar (with placeholder fallback), bio, experience, specialty tags (AUTH-02)
- Privacy popup appears on first visit if required by WeChat
- Works, Services, Booking pages show meaningful empty state messages
- Profile page displays user info and detects artist role for admin access
- All 5 tabBar pages are navigable with no console errors
- Page structure follows WeChat Mini Program conventions (4-file pages)
- TDesign components properly registered and rendered
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-profile/01-02-SUMMARY.md`
</output>
