---
phase: 21-review-enhancement
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - miniprogram/pages/admin/reviews/list.js
  - miniprogram/pages/admin/reviews/list.wxml
  - miniprogram/pages/admin/reviews/list.wxss
  - miniprogram/pages/admin/customers/detail.wxml
autonomous: true
requirements: [REVW-13, REVW-14, REVW-12, REVW-15]
tags: [ui, admin, filter, sort, delete, subscribe-authorize]

must_haves:
  truths:
    - "Artist can filter the admin review list by rating (全部/5星…1星) and by tag"
    - "Artist can sort the admin review list by 最新/最高/最低 and the list responds server-side"
    - "Artist can delete a review from the admin list (with confirmation); deletion calls deleteReview and refreshes"
    - "Admin list shows review thumbnails (tap to wx.previewImage), tag chips, and an '匿名' mark on anonymous reviews while still showing the real nickname"
    - "Artist is prompted to authorize subscribe messages (新评价) when entering the reviews admin page"
    - "Customer detail page review list shows an '匿名' mark on anonymous reviews (real nickname still visible to artist)"
  artifacts:
    - path: "miniprogram/pages/admin/reviews/list.js"
      provides: "Filter/sort state, delete handler, subscribe authorization, tag/image/anonymous rendering data"
      contains: "deleteReview"
    - path: "miniprogram/pages/admin/reviews/list.wxml"
      provides: ".segs filter bar, sort toggle, thumbnails, tags, anonymous mark, delete button"
      contains: "匿名"
    - path: "miniprogram/pages/admin/reviews/list.wxss"
      provides: "segs + sort + thumb + tag + anon-mark styles"
      contains: "seg--on"
    - path: "miniprogram/pages/admin/customers/detail.wxml"
      provides: "Anonymous mark on customer review items"
      contains: "匿名"
  key_links:
    - from: "miniprogram/pages/admin/reviews/list.js loadReviews"
      to: "services/reviews.getReviewsList"
      via: "pass ratingFilter/tagFilter/sortBy filters"
      pattern: "ratingFilter|sortBy"
    - from: "miniprogram/pages/admin/reviews/list.js onDeleteReview"
      to: "services/reviews.deleteReview"
      via: "confirm then delete + refresh"
      pattern: "deleteReview"
    - from: "miniprogram/pages/admin/reviews/list.js onLoad"
      to: "wx.requestSubscribeMessage"
      via: "artist authorizes 新评价 notification"
      pattern: "requestSubscribeMessage"
---

<objective>
Turn the admin reviews list into a manageable console: server-side filter (rating/tag) + sort (latest/highest/lowest), review deletion with image cleanup, enriched display (thumbnails, tags, anonymous mark), and the artist-side subscribe-message authorization trigger for 新评价 notifications.

Purpose: REVW-13 (manage efficiently), REVW-14 (delete + image cleanup), REVW-12 admin-display consistency (real identity + 匿名 mark), REVW-15 client-side authorization half.

Output: A full artist review-management page plus the customer-detail anonymous-mark consistency fix.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-review-enhancement/21-CONTEXT.md
@.planning/phases/21-review-enhancement/21-01-PLAN.md

<interfaces>
<!-- Contracts from Plan 01 and existing assets. -->

### From Plan 01 — miniprogram/services/reviews.js (extended)
```js
getReviewsList(page, pageSize, filters)
// filters = { ratingFilter: 1-5|undefined, tagFilter: 'professional'|undefined, sortBy: 'latest'|'highest'|'lowest' }
deleteReview(reviewId) // → callCloudFunction('reviews', { action:'delete', review_id })
```
List items now carry `tags: [{key,label}]`, `images: string[]` (fileIDs), `is_anonymous: boolean`.

### From Plan 01 — miniprogram/utils/constants.js
```js
const REVIEW_TAGS = [{key,label} x5]   // reuse for the tag filter segs (D-16)
const SUBSCRIBE_TEMPLATE_ID = '-i6OevJwdS5fGFXCsB9Xux4zaaxUkXTR0xfLg5T48jM'
```

### Existing — miniprogram/pages/admin/reviews/list.js current state
- `data`: reviews, loading, hasMore, page, replyingId, replyText, replySubmitting.
- `loadReviews(reset)`: `reviewsService.getReviewsList(page,10)` → maps `timeLabel`. Pull-down refresh + reach-bottom pagination.
- Reply handlers (onToggleReply/onReplyInput/onSubmitReply) — KEEP intact.

### Existing .segs filter pattern — miniprogram/pages/admin/bookings/list.wxml
```html
<view class="segs">
  <view class="seg tappable {{currentStatus===item.key?'seg--on':''}}" wx:for="{{statusSegs}}">
    <text class="seg-label">{{item.label}}</text>
  </view>
</view>
```

### Existing — miniprogram/pages/admin/customers/detail.wxml review-item (lines 139-149)
Renders `rstars`, `rdate`, `rsvc`, `rcontent` for each review in `reviews`. Real nickname not currently shown here — add 匿名 mark beside stars when `item.is_anonymous`.

### Existing authorization pattern — miniprogram/pages/profile/index.js:59 / booking/create.js:433
```js
wx.requestSubscribeMessage({ tmplIds: [SUBSCRIBE_TEMPLATE_ID], success(res){...} })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: list.js — filter/sort state, delete handler, subscribe authorize, tag/image/anonymous map</name>
  <files>miniprogram/pages/admin/reviews/list.js</files>
  <action>
**Per D-13/D-14/D-15/D-16/D-18/D-23.** Extend existing `data` and `loadReviews`; preserve reply handlers.

1. Require: `const { REVIEW_TAGS, SUBSCRIBE_TEMPLATE_ID } = require('../../../utils/constants')`.
2. `data` additions: `ratingSegs: [{key:0,label:'全部'},{key:5,label:'5星'}...{key:1,label:'1星'}]`, `tagSegs: [{key:'',label:'全部标签'}, ...REVIEW_TAGS]`, `currentRating: 0`, `currentTag: ''`, `sortBy: 'latest'`, `sortOptions: [{key:'latest',label:'最新'},{key:'highest',label:'最高'},{key:'lowest',label:'最低'}]`, `deletingId: ''`.
3. `onLoad`: after the existing auth check + before `loadReviews`, **request subscribe authorization (D-23)**: `wx.requestSubscribeMessage({ tmplIds:[SUBSCRIBE_TEMPLATE_ID], success(){}, fail(){} })` — non-blocking, swallow errors (artist may decline). Keep it best-effort; do not gate page load on it.
4. `loadReviews(reset)`: build `filters = { ratingFilter: currentRating || undefined, tagFilter: currentTag || undefined, sortBy }`; call `reviewsService.getReviewsList(page, 10, filters)`. In the map function, add: `tagLabels: (r.tags||[]).map(t=>t.label)`, `thumb: (r.images&&r.images[0])||''`, `imageCount: (r.images||[]).length`, `isAnonymous: !!r.is_anonymous`. Keep `timeLabel`.
5. Filter/sort change handlers: `onRatingSeg(e)` (setData currentRating=dataset.key, reset page=1, reload with reset=true), `onTagSeg(e)` (currentTag=dataset.key, same reset+reload), `onSortChange(e)` (sortBy=dataset.key, reset+reload). All three must reset to page 1 and reload from scratch.
6. `onPreviewImage(e)`: read `dataset.url` (a fileID) and `dataset.id`; find that review's `images` array; `wx.previewImage({ current: dataset.url, urls: reviewImages })`.
7. `onDeleteReview(e)`: `reviewId = dataset.id`. `wx.showModal({ title:'删除评价', content:'确定删除该评价？评价图片将一并删除。' })`; on confirm set `deletingId`, call `reviewsService.deleteReview(reviewId)`; on success remove the item from `this.data.reviews` (or reload page 1) + toast '已删除'; reset `deletingId`. Catch → toast err.message.

NOTE: `wx.previewImage` accepts cloud fileIDs as urls directly (WeChat resolves them).
  </action>
  <verify>
    <automated>node --check miniprogram/pages/admin/reviews/list.js && node -e "const fs=require('fs');const s=fs.readFileSync('miniprogram/pages/admin/reviews/list.js','utf8');['REVIEW_TAGS','SUBSCRIBE_TEMPLATE_ID','requestSubscribeMessage','ratingFilter','sortBy','onDeleteReview','deleteReview','onPreviewImage','isAnonymous'].forEach(p=>{if(s.indexOf(p)<0)throw new Error('missing '+p)});console.log('list.js patterns ok')"</automated>
  </verify>
  <done>Page holds rating/tag filter + sort state, requests subscribe authorization on load, reloads from page 1 on any filter/sort change, previews images, and deletes reviews via deleteReview with confirmation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: list.wxml + list.wxss — .segs filter bar, sort toggle, thumbnails, tags, anonymous mark, delete</name>
  <files>miniprogram/pages/admin/reviews/list.wxml, miniprogram/pages/admin/reviews/list.wxss</files>
  <action>
**Per D-12/D-13/D-14.** Insert a filter/sort control bar at the top of `.admin-reviews` (above the list, below loading); enhance each review card.

**list.wxml:**
1. **Filter bar** (only when not loading): a `.segs` row for rating (`wx:for="{{ratingSegs}}"`, `class="seg tappable {{currentRating===item.key?'seg--on':''}}"`, `bindtap="onRatingSeg"`, `data-key="{{item.key}}"`, `{{item.label}}`). A second `.segs` row (or same row wrap) for tags using `tagSegs` (`currentTag===item.key` → `seg--on`, `onTagSeg`). A sort row: small toggle `wx:for="{{sortOptions}}"`, `class="sort-opt {{sortBy===item.key?'sort-opt--on':''}}"`, `bindtap="onSortChange"`, `data-key="{{item.key}}"`.
2. **Card enhancements** (inside each `wx:for="{{reviews}}"` card):
   - In `.rtop`: keep nickname; **add `<text class="anon-mark" wx:if="{{item.isAnonymous}}">匿名</text>`** after the nickname (per D-12 — admin sees real nickname + a mark). Show nickname even when anonymous (admin privilege).
   - After `.rrate`: **tag chips row** `<view class="rtags" wx:if="{{item.tagLabels.length}}"><text class="rtag" wx:for="{{item.tagLabels}}" wx:for-item="tl" wx:key="*this">{{tl}}</text></view>`.
   - After content: **thumbnail strip** `<view class="rthumbs" wx:if="{{item.thumb}}"><image class="rthumb" wx:for="{{item.images}}" wx:for-item="img" wx:key="*this" src="{{img}}" mode="aspectFill" bindtap="onPreviewImage" data-url="{{img}}" data-id="{{item._id}}"></image></view>`. Show count badge if `imageCount>3` optional (keep simple: show all, max 3 from backend).
   - **Delete action**: a `.delete-action` text `bindtap="onDeleteReview" data-id="{{item._id}}"` labeled '删除', shown disabled-ish when `deletingId===item._id` ('删除中...').
   - Keep the existing reply display + reply editor blocks intact.
3. The empty state stays.

**list.wxss:**
- Reuse the bookings `.segs`/`.seg`/`.seg--on`/`.seg-label` styles — copy them into this file's scope if not globally available (admin pages may share app.wxss; check first, only add what's missing).
- `.anon-mark`: small pill, muted/gold background, font-size 20rpx, margin-left.
- `.rtags`: flex wrap, margin; `.rtag`: pill like the create-form tag-chip (smaller), muted border.
- `.rthumbs`: flex, gap; `.rthumb`: ~120rpx square, border-radius, margin-bottom.
- `.sort-opt`: small text toggle; `.sort-opt--on`: accent color + underline/bold.
- `.delete-action`: muted destructive (red) text, small, aligned right.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('miniprogram/pages/admin/reviews/list.wxml','utf8');const x=fs.readFileSync('miniprogram/pages/admin/reviews/list.wxss','utf8');['onRatingSeg','onTagSeg','onSortChange','anon-mark','rtags','rthumb','onDeleteReview'].forEach(p=>{if(w.indexOf(p)<0)throw new Error('wxml missing '+p)});['anon-mark','rtag','rthumb','sort-opt'].forEach(p=>{if(x.indexOf(p)<0)throw new Error('wxss missing '+p)});console.log('list.wxml/wxss patterns ok')"</automated>
  </verify>
  <done>Admin page shows rating+tag filter segs, sort toggle, per-card tag chips + image thumbnails (previewable) + 匿名 mark on anonymous reviews (real nickname visible) + delete action with confirmation state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: customers/detail.wxml — anonymous mark on customer review items (D-12 consistency)</name>
  <files>miniprogram/pages/admin/customers/detail.wxml</files>
  <action>
**Per D-12.** In the existing review-item block (around line 139-149), add an '匿名' mark when `item.is_anonymous`. The customer detail page already shows the artist-privileged real customer identity context, so just append a small mark beside the stars/date row:
- Inside `.rtop` (after `.rdate`), add: `<text class="anon-tag" wx:if="{{item.is_anonymous}}">匿名评价</text>`.
- The customer detail JS already loads reviews (Phase 20); ensure the review objects include `is_anonymous` — they come from a reviews query that now stores the field, so no JS change needed unless the detail query uses a restrictive `.field()` projection. If `customers` cloud fn projects fields explicitly, add `is_anonymous: true` to that projection (check the customers detail data path; if it returns the full doc, no change).
- Add `.anon-tag` style to the existing `customers/detail.wxss` ONLY if a matching rule is absent (small pill, gold/muted). If adding, also list `customers/detail.wxss` in files_modified — but first check whether it already has review styling to extend. Prefer minimal: inline a small style or reuse an existing tag class in that file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('miniprogram/pages/admin/customers/detail.wxml','utf8');if(s.indexOf('is_anonymous')<0&&s.indexOf('isAnonymous')<0)throw new Error('no anonymous mark');console.log('customer detail anonymous mark ok')"</automated>
  </verify>
  <done>Customer detail review items display an '匿名评价' mark when the review was submitted anonymously; artist still sees the customer's real identity in the page context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artist admin page → reviews list/delete fn | Filter/sort params + delete intent cross to server |
| Artist device → WeChat subscribe consent | requestSubscribeMessage system dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Elevation (E) | delete from admin UI | mitigate | UI confirm modal + server requireArtist (Plan 01 T-21-04); non-artist cannot reach the page (existing isArtist gate) |
| T-21-11 | Info Disclosure (I) | anonymous review real nickname shown to artist | accept | Intended by D-12 — artist needs real identity for service follow-up; public endpoints hide it (Plan 01 T-21-03). Mark makes the privacy status explicit |
| T-21-12 | Tampering (T) | filter/sort params | mitigate | Server validates enums (Plan 01 T-21-02); client just toggles preset keys |
</threat_model>

<verification>
- `node --check list.js` passes; wxml/wxss + customers/detail.wxml contain required patterns.
- Filter/sort changes reset to page 1 and reload; delete confirms then removes; image tap previews; anonymous reviews show the mark.
- Subscribe authorization is requested on load, non-blocking.
</verification>

<success_criteria>
- Artist can filter (rating/tag), sort (latest/highest/lowest), preview images, and delete reviews; anonymous reviews are marked but real identity visible; artist can authorize 新评价 notifications.
- Customer detail page marks anonymous reviews consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/21-review-enhancement/21-03-SUMMARY.md`
</output>
