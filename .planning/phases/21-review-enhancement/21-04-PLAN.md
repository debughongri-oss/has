---
phase: 21-review-enhancement
plan: 04
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - miniprogram/pages/index/index.js
  - miniprogram/pages/index/index.wxml
  - miniprogram/pages/index/index.wxss
autonomous: true
requirements: [REVW-14, REVW-10]
tags: [ui, index, performance, tag-cloud, redundancy-read]

must_haves:
  truths:
    - "Index review average and total read from artist_profile redundancy fields (no aggregate compute per page load)"
    - "Index shows a '大家这样说' tag-cloud section aggregating high-frequency review tags when tags exist"
    - "Anonymous recent reviews on the index show '匿名客户' instead of a real nickname"
    - "Index review module does NOT render review images (kept light per D-08)"
  artifacts:
    - path: "miniprogram/pages/index/index.js"
      provides: "loadReviewStats merging artist_profile avg/total with getStats recent + topTags"
      contains: "avg_rating"
    - path: "miniprogram/pages/index/index.wxml"
      provides: "'大家这样说' tag-cloud block; anonymized recent nickname"
      contains: "大家这样说"
    - path: "miniprogram/pages/index/index.wxss"
      provides: "tag-cloud chip styles"
      contains: "tag-cloud"
  key_links:
    - from: "miniprogram/pages/index/index.js loadReviewStats"
      to: "this.data.artist.avg_rating / total_reviews"
      via: "read redundancy from already-loaded profile"
      pattern: "avg_rating|total_reviews"
    - from: "miniprogram/pages/index/index.js loadReviewStats"
      to: "services/reviews.getReviewStats"
      via: "fetch recent + topTags only"
      pattern: "topTags"
---

<objective>
Make the index review module read avg/total from the artist_profile redundancy fields (faster, zero compute) and surface a '大家这样说' high-frequency tag cloud to boost social-proof conversion, while anonymizing public nickname display.

Purpose: REVW-14 read side (performance — no per-load aggregate) + D-04/D-11 (tag cloud conversion + public anonymity).

Output: A lighter, more persuasive index review module.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-review-enhancement/21-CONTEXT.md
@.planning/phases/21-review-enhancement/21-01-PLAN.md

<interfaces>
<!-- Contracts from Plan 01 and existing index code. -->

### From Plan 01 — getStats new return shape
```js
{ errCode:0, data:{ average, total, recent:[{rating,content,user_nickname,artist_reply,created_at,tags,images,is_anonymous}], topTags:[{key,label,count}] } }
```
NOTE: avg/total in getStats now come from artist_profile redundancy (Plan 01 Task 3), but for the INDEX we read redundancy DIRECTLY from the already-loaded `this.data.artist` to avoid even the getStats call for those two numbers — getStats is still called for `recent` + `topTags`.

### Existing — miniprogram/pages/index/index.js
- `data.reviewStats = null`.
- `loadProfile()` calls `profileService.getArtistProfile(forceRefresh)` → `setData({ artist: data })` then fires `loadReviewStats()` (and loadFeaturedWorks/loadServices).
- `loadReviewStats()` (line ~112): `reviewsService.getReviewStats().then(data => setData({ reviewStats: data }))`. Catches + logs, non-blocking.
- `this.data.artist` is the full artist_profile (now includes `avg_rating`, `total_reviews` per Plan 01 Task 1).

### Existing — miniprogram/pages/index/index.wxml review module (lines ~131-162)
- Shows when `reviewStats && reviewStats.total > 0`.
- `.review-top`: `reviewStats.average` + `/ 5.0` + stars + `{{reviewStats.total}} 条真实评价`.
- `.review-quote`: `reviewStats.recent[0].content` + `— {{reviewStats.recent[0].user_nickname || '匿名客户'}}`.
- `.review-reply`: recent[0].artist_reply.

### CSS custom properties available
--accent, --gold, --text-secondary, --bg-elevated (reuse; no new palette).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: index.js — merge artist_profile avg/total with getStats recent + topTags; anonymize public nickname</name>
  <files>miniprogram/pages/index/index.js</files>
  <action>
**Per D-04/D-11/D-20/D-21.** Refactor `loadReviewStats` only; do not touch loadProfile/loadFeaturedWorks/loadServices.

New `loadReviewStats`:
1. Call `reviewsService.getReviewStats()` (still needed for `recent` + `topTags`; the backend now returns avg/total from redundancy too, but we prefer the already-loaded profile copy — see step 3).
2. In the `.then(data)`:
   - Prefer redundancy from the loaded profile: `const avg = (this.data.artist && this.data.artist.avg_rating != null) ? Number(this.data.artist.avg_rating).toFixed(1) : data.average;` and `const total = (this.data.artist && this.data.artist.total_reviews != null) ? this.data.artist.total_reviews : data.total;`. This makes the headline numbers consistent with the profile read (already shown elsewhere) and avoids drift.
   - **Anonymize public nickname (D-11):** map `data.recent` so that for any review with `is_anonymous`, the displayed nickname becomes '匿名客户' and avatar hidden: `const recent = (data.recent||[]).map(r => ({ ...r, displayNickname: r.is_anonymous ? '匿名客户' : (r.user_nickname || '匿名客户') }))`.
   - Extract tag cloud: `const topTags = (data.topTags || []).slice(0, 5)` (top N = 5).
   - `setData({ reviewStats: { average: avg, total: total, recent, topTags } })`.
3. Keep the existing `.catch` (log, non-blocking). If the profile isn't loaded yet (artist null), fall back to `data.average`/`data.total` from getStats — the redundancy read is a preference, not a hard dependency.

Do NOT add image rendering here (D-08: index stays light). The `recent` objects retain `images` for potential future use but the WXML (Task 2) will not render them.
  </action>
  <verify>
    <automated>node --check miniprogram/pages/index/index.js && node -e "const fs=require('fs');const s=fs.readFileSync('miniprogram/pages/index/index.js','utf8');['avg_rating','total_reviews','topTags','is_anonymous','匿名客户'].forEach(p=>{if(s.indexOf(p)<0)throw new Error('missing '+p)});console.log('index.js patterns ok')"</automated>
  </verify>
  <done>Review headline numbers read from artist_profile redundancy (with getStats fallback); recent nicknames anonymized for is_anonymous reviews; topTags passed to the view.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: index.wxml + index.wxss — '大家这样说' tag cloud; use anonymized nickname</name>
  <files>miniprogram/pages/index/index.wxml, miniprogram/pages/index/index.wxss</files>
  <action>
**Per D-04/D-08/D-11.** Minimal, on-brand edits to the existing review module.

**index.wxml** (within the `wx:if="{{reviewStats && reviewStats.total > 0}}"` review block):
1. The `.review-quote` attribution line: change `— {{reviewStats.recent[0].user_nickname || '匿名客户'}}` to `— {{reviewStats.recent[0].displayNickname}}` (uses the anonymized value from Task 1). Keep everything else in `.review-top`/`.review-reply` as-is.
2. **Add the tag-cloud block** after `.review-card` (still inside the `.reviews` section): 
```html
<view class="tag-cloud" wx:if="{{reviewStats.topTags.length}}">
  <text class="tag-cloud-title">大家这么说</text>
  <view class="tag-cloud-list">
    <view class="cloud-chip" wx:for="{{reviewStats.topTags}}" wx:key="key">
      <text class="cloud-chip-label">{{item.label}}</text>
      <text class="cloud-chip-count">×{{item.count}}</text>
    </view>
  </view>
</view>
```
3. Do NOT add any image markup for reviews (D-08).

**index.wxss:**
- `.tag-cloud`: margin-top, padding consistent with `.review-card`.
- `.tag-cloud-title`: small eyebrow label, muted (--text-secondary), letter-spacing, font-size ~24rpx.
- `.tag-cloud-list`: flex wrap, gap.
- `.cloud-chip`: pill, background var(--bg-elevated) or a gold tint, border-radius 999rpx, padding; `.cloud-chip-label` font-size 26rpx; `.cloud-chip-count` muted/gold smaller (22rpx), margin-left 6rpx. Selected/active not needed (read-only display).
- Visually echo the create-form `.tag-chip` shape (D-02) for consistency.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('miniprogram/pages/index/index.wxml','utf8');const x=fs.readFileSync('miniprogram/pages/index/index.wxss','utf8');['大家这样说','topTags','displayNickname','cloud-chip'].forEach(p=>{if(w.indexOf(p)<0)throw new Error('wxml missing '+p)});['tag-cloud','cloud-chip'].forEach(p=>{if(x.indexOf(p)<0)throw new Error('wxss missing '+p)});console.log('index.wxml/wxss patterns ok')"</automated>
  </verify>
  <done>Index shows avg/total from redundancy, a '大家这样说' tag cloud (top 5), and anonymized nicknames; no review images rendered on the index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public index page ← reviews getStats / artist_profile | Public visitors read aggregated review data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Info Disclosure (I) | anonymous reviewer nickname on public index | mitigate | Anonymize displayNickname to '匿名客户' for is_anonymous reviews (D-11); server also flags is_anonymous; defense-in-depth |
| T-21-14 | Info Disclosure (I) | tag cloud / avg leakage | accept | Aggregated public review stats are intended marketing content; no PII in tags (preset list only) |
</threat_model>

<verification>
- `node --check index.js` passes; wxml/wxss contain required patterns.
- Headline avg/total come from artist_profile when present; tag cloud renders only when topTags exist; anonymous reviews show '匿名客户'.
</verification>

<success_criteria>
- Index review module is faster (reads redundancy) and more persuasive (tag cloud) while preserving customer anonymity publicly and staying image-free.
</success_criteria>

<output>
After completion, create `.planning/phases/21-review-enhancement/21-04-SUMMARY.md`
</output>
