---
phase: 21-review-enhancement
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - miniprogram/pages/review/create.js
  - miniprogram/pages/review/create.wxml
  - miniprogram/pages/review/create.wxss
autonomous: true
requirements: [REVW-10, REVW-11, REVW-12]
tags: [ui, client-form, tags, image-upload, anonymous]

must_haves:
  truths:
    - "Customer can multi-select up to 5 preset tag chips in the review form"
    - "Customer can attach 1-3 images via chooseMedia and remove individual images before submit"
    - "Customer can toggle an '匿名评价' switch (default off)"
    - "On submit, images upload to cloud storage first, then the review is created with tags/imageFileIDs/isAnonymous"
    - "Submitted review carries tags, images, and is_anonymous to the backend"
  artifacts:
    - path: "miniprogram/pages/review/create.js"
      provides: "Review form with tag selection, image upload/remove, anonymous toggle, upload-then-create submit"
      contains: "uploadWorkImages"
    - path: "miniprogram/pages/review/create.wxml"
      provides: "Tag chips, image grid, anonymous switch markup"
      contains: "匿名评价"
    - path: "miniprogram/pages/review/create.wxss"
      provides: "Tag chip + image grid + switch styles"
      contains: "tag-chip"
  key_links:
    - from: "miniprogram/pages/review/create.js onSubmit"
      to: "services/storage.uploadWorkImages"
      via: "upload local temp paths to get fileIDs before creating review"
      pattern: "uploadWorkImages"
    - from: "miniprogram/pages/review/create.js onSubmit"
      to: "services/reviews.createReview"
      via: "pass tags/imageFileIDs/isAnonymous options"
      pattern: "imageFileIDs|isAnonymous"
---

<objective>
Upgrade the customer review form from text+stars to tags + images + optional anonymity, reusing the project's native chooseMedia grid pattern (works/edit) and storage upload service.

Purpose: REVW-10/11/12 client side — make reviews more persuasive (tags + photos) and give customers a privacy option. This is the input half of the enhanced review; Plan 03/04 handle display.

Output: A review form that submits enriched review data to the Plan 01 backend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-review-enhancement/21-CONTEXT.md
@.planning/phases/21-review-enhancement/21-01-PLAN.md

<interfaces>
<!-- Contracts provided by Plan 01 and existing assets. Build directly against these. -->

### From Plan 01 — miniprogram/utils/constants.js
```js
const REVIEW_TAGS = [
  { key: 'professional', label: '手法专业' },
  { key: 'natural', label: '妆面自然' },
  { key: 'punctual', label: '准时' },
  { key: 'friendly', label: '态度好' },
  { key: 'value', label: '性价比高' }
]
```

### From Plan 01 — miniprogram/services/reviews.js (extended signature)
```js
createReview(bookingId, rating, content, options)
// options = { tags: [{key,label}] | [keys], imageFileIDs: string[], isAnonymous: boolean }
```
NOTE: confirm the exact `tags` shape Plan 01 expects (array of keys vs objects) and pass consistently — read the Plan 01 SUMMARY before implementing; default to passing the full `{key,label}` objects the form holds and let the server whitelist-filter (Task 2 of Plan 01 filters by key).

### Existing — miniprogram/services/storage.js
```js
uploadWorkImages(filePaths /* string[] */, onProgress) // → Promise<fileID[]>  (compress + bounded concurrency)
```

### Existing pattern — miniprogram/pages/admin/works/edit.js (chooseMedia + removeImage)
```js
wx.chooseMedia({ count: remaining, mediaType:['image'], sourceType:['album','camera'],
  success: (res) => { setData({ images: images.concat(res.tempFiles.map(f=>f.tempFilePath)) }) } })
removeImage(e) { splice at dataset.index; setData({images}) }
```

### Existing — miniprogram/pages/review/create.js current state
- `data`: bookingId, booking, rating, ratingLabel, stars, content, contentLength, maxLength, submitting.
- `onRateStar`, `onContentInput`, `onSubmit` (validates rating≥1, slices content 200, calls createReview(bookingId,rating,content)).
- On success: toast '评价成功' + navigateBack.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: create.js — tag selection state, image choose/remove, anonymous toggle, upload-then-create submit</name>
  <files>miniprogram/pages/review/create.js</files>
  <action>
**Per D-02/D-05/D-06/D-10.** Extend `data` and handlers; keep existing rating/content logic intact.

1. Require storage service: `const storageService = require('../../services/storage')` and `const { REVIEW_TAGS } = require('../../utils/constants')`.
2. `data` additions: `tags: REVIEW_TAGS` (the full preset list for rendering), `selectedTagKeys: []` (selected tag keys), `images: []` (local temp file paths), `isAnonymous: false`, `maxImages: 3`, `uploading: false`.
3. `onToggleTag(e)`: read `dataset.key`; if already in `selectedTagKeys` remove it, else push (cap at 5 per D-02 — if at 5 and adding new, show toast '最多选择5个标签'). `setData({ selectedTagKeys })`.
4. `onChooseImage()`: `remaining = maxImages - images.length`; if ≤0 toast '最多3张图片'; else `wx.chooseMedia({ count: remaining, mediaType:['image'], sourceType:['album','camera'], sizeType:['compressed'], success: res => setData({ images: images.concat(res.tempFiles.map(f=>f.tempFilePath)) }) })` (mirror works/edit.js).
5. `onRemoveImage(e)`: splice at `dataset.index`, `setData({ images })`.
6. `onToggleAnonymous(e)`: `setData({ isAnonymous: e.detail.value })` (switch bind:change).
7. Rewrite `onSubmit` to an async flow (per D-06 — upload first, then create):
   - Existing guards (submitting lock, rating≥1).
   - Set `submitting:true, uploading: images.length>0`.
   - If `images.length`: `const fileIDs = await storageService.uploadWorkImages(this.data.images)`. Catch → toast '图片上传失败，请重试', reset submitting/uploading, return.
   - Build selected tag objects: `const selectedTags = REVIEW_TAGS.filter(t => selectedTagKeys.includes(t.key))`.
   - `await reviewsService.createReview(bookingId, rating, content, { tags: selectedTags, imageFileIDs: fileIDs || [], isAnonymous })`.
   - On success: toast '评价成功' + navigateBack (unchanged). On failure: toast err.message, reset submitting/uploading.
   - Keep the existing `authService.ensureLogin()` call before submit.
  </action>
  <verify>
    <automated>node --check miniprogram/pages/review/create.js && node -e "const fs=require('fs');const s=fs.readFileSync('miniprogram/pages/review/create.js','utf8');['REVIEW_TAGS','selectedTagKeys','onToggleTag','onChooseImage','onRemoveImage','onToggleAnonymous','uploadWorkImages','isAnonymous'].forEach(p=>{if(s.indexOf(p)<0)throw new Error('missing '+p)});console.log('create.js patterns ok')"</automated>
  </verify>
  <done>Form holds selected tags (≤5), 1-3 local images, anonymous flag; submit uploads images via storage then calls createReview with the options object.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: create.wxml + create.wxss — tag chips, image grid, anonymous switch</name>
  <files>miniprogram/pages/review/create.wxml, miniprogram/pages/review/create.wxss</files>
  <action>
**Per D-02/D-05/D-08/D-10.** Native/custom components only (no TDesign), matching the existing custom rate-star + native textarea style.

**create.wxml** — insert three new blocks between the 文字评价 card and the submit button:
1. **标签 chip block** (after the textarea card): a section titled '服务标签（可多选）' with a wrapping flex row of `tag-chip` views. Each chip: `class="tag-chip {{selectedTagKeys.includes(item.key) ? 'tag-chip--on' : ''}}"`, `bindtap="onToggleTag"`, `data-key="{{item.key}}"`, text `{{item.label}}`. `wx:for="{{tags}}"`.
2. **图片 block**: a section titled '添加图片（最多3张）' with a grid row. Render existing images as thumbnail cells with a remove `×` button (`bindtap="onRemoveImage"` `data-index`). Add an 'add' cell (visible when `images.length < maxImages`) bound to `onChooseImage`, showing a `+` and label '添加图片'. Mirror the works/edit image-grid visual.
3. **匿名评价 switch block**: a row with label '匿名评价' + hint '勾选后他人将看不到你的昵称头像' + a native `<switch checked="{{isAnonymous}}" bindchange="onToggleAnonymous" color="#c79a5a"/>` (use --gold via inline or a class; native switch color attr accepts hex).
4. Update the submit button `disabled` to also account for `uploading`: `disabled="{{submitting || rating === 0}}"` (rating is the only hard gate; uploading state shows in button text). Button text: `{{submitting ? (uploading ? '上传图片中...' : '提交中...') : '提交评价'}}`.

**create.wxss** — add styles consistent with existing card/`.rate-star--on` accent:
- `.tag-chip`: padding, border-radius 999rpx (pill), border 1rpx solid var(--text-secondary or muted), font-size ~26rpx, margin between chips.
- `.tag-chip--on`: background var(--accent) or gold tint, color #fff, border-color transparent.
- Image grid: `.img-grid` flex wrap; `.img-cell` fixed ~200rpx square, border-radius, overflow hidden; `.img-cell image` width/height 100%; `.img-remove` absolute top-right × badge; `.img-add` dashed border + centered +.
- `.anon-row`: flex space-between, align center, padding; `.anon-hint` muted small text.
- Reuse existing CSS custom properties (--accent, --gold, --text-secondary, --bg-elevated) — do not introduce a new palette.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['create.wxml','create.wxss'].forEach(f=>{const s=fs.readFileSync('miniprogram/pages/review/'+f,'utf8');const need=f.endsWith('wxml')?['tag-chip','onToggleTag','onChooseImage','匿名评价','onToggleAnonymous']:['tag-chip','img-grid'];need.forEach(p=>{if(s.indexOf(p)<0)throw new Error(f+' missing '+p)})});console.log('create.wxml/wxss patterns ok')"</automated>
  </verify>
  <done>Form renders multi-select tag chips (selected = accent), a 3-cell image grid with add/remove, and an anonymous switch (default off) with a clear hint; submit button reflects uploading state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → cloud storage upload | Customer-chosen local images cross into cloud storage |
| Form submit → reviews create fn | Enriched review payload (tags/images/anonymous) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Content abuse (DoS) | image upload | mitigate | Hard client cap 3 images (D-06); server imgSecCheck fail-closed (Plan 01 T-21-06); chooseMedia sizeType compressed |
| T-21-09 | Spoofing (S) | tags / isAnonymous client state | accept | Client only sends values; server whitelists tags (Plan 01 T-21-01) and ignores client nickname. Client cannot escalate by editing state |
</threat_model>

<verification>
- `node --check create.js` passes; wxml/wxss contain required class/handler patterns.
- Tag chips toggle selected state; image grid add/remove works; anonymous switch defaults off.
- Submit uploads images then creates review with the options object.
</verification>

<success_criteria>
- Customer can pick tags (≤5), add 1-3 images, toggle anonymity, and submit a review carrying all three enrichment fields to the backend.
- Upload errors and create errors surface as toasts without leaving the form in a stuck submitting state.
</success_criteria>

<output>
After completion, create `.planning/phases/21-review-enhancement/21-02-SUMMARY.md`
</output>
