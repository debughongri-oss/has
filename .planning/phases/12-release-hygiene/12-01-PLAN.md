---
phase: 12-release-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - project.private.config.json
  - miniprogram/app.json
  - miniprogram/sitemap.json
  - miniprogram/pages/demo-ui/demo-ui.js
  - miniprogram/pages/demo-ui/demo-ui.wxml
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
granularity: standard

must_haves:
  truths:
    - "project.private.config.json 不再被 git 跟踪，且被 .gitignore 排除"
    - "生产构建的 app.json pages 列表不包含 pages/demo-ui/demo-ui"
    - "demo-ui 页面文件从代码库移除（不再存在于 miniprogram/pages/demo-ui/）"
    - "sitemap.json 对 pages/admin/* 子包页面设为 disallow，微信不会收录管理后台页面"
  artifacts:
    - path: ".gitignore"
      provides: "排除 project.private.config.json 的规则"
      contains: "project.private.config.json"
    - path: "miniprogram/app.json"
      provides: "不含 demo-ui 的生产页面注册表"
      not_contains: "demo-ui"
    - path: "miniprogram/sitemap.json"
      provides: "admin 子包 disallow 规则"
      contains: "pages/admin"
    - path: "miniprogram/pages/demo-ui/demo-ui.js"
      provides: "(已删除) 不再存在"
      absent: true
  key_links:
    - from: "miniprogram/sitemap.json rules"
      to: "pages/admin/* 子包"
      via: "disallow action 匹配 admin 路径前缀"
      pattern: "pages/admin"
---

<objective>
清理发布包卫生：将本地私有配置移出版本控制、移除开发用的 demo-ui 页面注册与文件、用 sitemap 屏蔽管理后台子包不被微信收录。

Purpose: 达到可上线卫生标准——生产包不含开发遗留（demo 页）与本地私有配置，管理后台不被搜索引擎/微信索引（信息暴露面收敛）。

Output: .gitignore 增项、project.private.config.json 出库、app.json 精简、sitemap.json 收敛规则、demo-ui 文件删除。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 当前状态（已核实）
- `project.private.config.json` 当前被 git 跟踪（自 init commit de38448），内容为本地开发者工具设置（libVersion/urlCheck/useApiHook 等），非密钥但属本地化配置，按微信官方要求不应入库。
- `miniprogram/app.json` 第 11 行注册了 `"pages/demo-ui/demo-ui"`，该页面是开发期 UI 调试用，生产无引用（已 grep 确认仅 app.json 与自身文件引用）。
- `miniprogram/sitemap.json` 当前是全局 `allow *`，admin 子包（works/services/bookings/profile/reviews 管理页）会被微信收录，存在信息暴露。
- `miniprogram/pages/demo-ui/` 含 demo-ui.js、demo-ui.wxml（无独立 .json/.wxss）。

# HYG 需求原文（来自 REQUIREMENTS.md）
- HYG-01: project.private.config.json 移出版本控制（加入 .gitignore 并 git rm --cached）
- HYG-02: 移除 pages/demo-ui/demo-ui 在 app.json 的生产注册及 sitemap 索引
- HYG-03: sitemap.json 对 pages/admin/* 子包页面设 disallow，避免管理页被微信收录

# sitemap.json 现状（待修改）
```json
{
  "desc": "...",
  "rules": [{ "action": "allow", "page": "*" }]
}
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HYG-01 — 将 project.private.config.json 移出 git 版本控制</name>
  <files>.gitignore, project.private.config.json</files>
  <read_first>
    - .gitignore（当前 17 行，确认无重复规则）
    - project.private.config.json（确认内容为本地工具设置，非密钥）
  </read_first>
  <action>
    1. 在 `.gitignore` 末尾新增一个分段（保留现有内容，追加不替换），添加：
       ```
       # 微信开发者工具本地配置（含本地环境 ID / 编译设置，不应入库）
       project.private.config.json
       project.config.json
       ```
       （同时排除 project.config.json，因为它也属本地工具配置；若 project.config.json 当前未跟踪也无妨，规则保证未来不入库。）
    2. 将已跟踪文件移出索引但保留本地副本：
       `git rm --cached project.private.config.json`
       （注意：是 `--cached`，不是物理删除。执行后 git status 应显示该文件为 deleted（from index）+ 未跟踪。）
    3. 确认本地文件仍在：`test -f project.private.config.json && echo OK`
  </action>
  <verify>
    <automated>cd /Users/works/project-codes/learns/hzs && git check-ignore project.private.config.json && ! git ls-files --error-unmatch project.private.config.json 2>/dev/null && test -f project.private.config.json</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the exact line `project.private.config.json`
    - `git check-ignore project.private.config.json` prints the path (文件被忽略规则命中)
    - `git ls-files project.private.config.json` 输出为空（已出库）
    - `test -f project.private.config.json` 成功（本地副本保留）
  </acceptance_criteria>
  <done>project.private.config.json 已从 git 索引移除、本地保留、.gitignore 永久排除；git status 显示 1 file removed from index。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HYG-02 + HYG-03 — 移除 demo-ui 生产注册并用 sitemap 屏蔽 admin 子包</name>
  <files>miniprogram/app.json, miniprogram/sitemap.json, miniprogram/pages/demo-ui/demo-ui.js, miniprogram/pages/demo-ui/demo-ui.wxml</files>
  <read_first>
    - miniprogram/app.json（pages 数组第 2-14 行；subPackages admin 子包第 59-75 行）
    - miniprogram/sitemap.json（当前 allow * 单规则）
  </read_first>
  <action>
    1. **HYG-02 移除 demo-ui 注册**：编辑 `miniprogram/app.json`，从 `pages` 数组中删除 `    "pages/demo-ui/demo-ui"` 这一行（第 11 行，注意删掉该行及其前导逗号——它位于数组最后一项，删除后 `"pages/review/create"` 行末尾的逗号需去掉，使 JSON 合法）。删除后 pages 数组以 `"pages/review/create"` 结尾，无尾逗号。

    2. **HYG-02 删除 demo-ui 页面文件**：物理删除 `miniprogram/pages/demo-ui/demo-ui.js` 与 `miniprogram/pages/demo-ui/demo-ui.wxml`，并删除空目录 `miniprogram/pages/demo-ui/`：
       `git rm miniprogram/pages/demo-ui/demo-ui.js miniprogram/pages/demo-ui/demo-ui.wxml`
       然后 `rmdir miniprogram/pages/demo-ui` 若目录已空。

    3. **HYG-03 sitemap 屏蔽 admin 子包**：重写 `miniprogram/sitemap.json` 为多规则——保留 allow * 作为默认，但在其之前增加一条 disallow 规则匹配 admin 子包。微信 sitemap 规则按数组顺序匹配，先 disallow 后 allow：
       ```json
       {
         "desc": "关于本文件的更多信息，请参考文档 https://developers.weixin.qq.com/miniprogram/dev/framework/sitemap.html",
         "rules": [
           {
             "action": "disallow",
             "page": "pages/admin/*"
           },
           {
             "action": "allow",
             "page": "*"
           }
         ]
       }
       ```
       （`pages/admin/*` 匹配 admin 子包下所有页面。微信 sitemap 的 page 字段匹配注册路径前缀；admin 子包 root 为 pages/admin，其页面注册为 works/list 等，完整路径为 pages/admin/works/list，故 `pages/admin/*` 通配匹配。）
  </action>
  <verify>
    <automated>cd /Users/works/project-codes/learns/hzs && node -e "JSON.parse(require('fs').readFileSync('miniprogram/app.json','utf8')); JSON.parse(require('fs').readFileSync('miniprogram/sitemap.json','utf8'));" && ! grep -q "demo-ui" miniprogram/app.json && test ! -d miniprogram/pages/demo-ui && grep -q '"pages/admin/\*"' miniprogram/sitemap.json</automated>
  </verify>
  <acceptance_criteria>
    - `miniprogram/app.json` is valid JSON (node JSON.parse succeeds)
    - `grep -c demo-ui miniprogram/app.json` returns 0
    - `miniprogram/pages/demo-ui/` directory does not exist
    - `miniprogram/sitemap.json` is valid JSON
    - `miniprogram/sitemap.json` contains the string `"pages/admin/*"` inside a `"disallow"` rule
    - `miniprogram/sitemap.json` still contains an `"allow"` `"*"` rule (default allow for public pages)
  </acceptance_criteria>
  <done>app.json 不再注册 demo-ui 且 JSON 合法；demo-ui 文件已删除；sitemap.json 含 disallow pages/admin/* 规则且 JSON 合法，公开页仍 allow *。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git 仓库 → 公开/协作者 | 私有本地配置若入库会随仓库分发 |
| 微信搜索/收录 → 任意用户 | sitemap allow 的页面会被微信索引，管理后台页面暴露路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | sitemap.json (admin 子包) | mitigate | HYG-03: disallow pages/admin/* 规则使管理页不被收录。注意：这是纵深防御（信息暴露面收敛），真正鉴权仍由 requireArtist（Phase 11 已交付）保证。 |
| T-12-02 | Information Disclosure | project.private.config.json in git | mitigate | HYG-01: git rm --cached + .gitignore。残留历史中的副本内容为本地工具设置（useApiHook/urlCheck/libVersion），非密钥，风险评估为 Low，不做历史重写（重写代价 > 收益）。 |
</threat_model>

<verification>
- `git status` 显示 project.private.config.json removed from index，.gitignore modified
- `git check-ignore project.private.config.json` 命中
- app.json / sitemap.json 均 `node -e JSON.parse(...)` 通过（合法 JSON）
- 生产构建（微信开发者工具「预览」）确认无 demo-ui 页面、admin 页面 404 于搜索索引（人工抽检，非阻塞）
</verification>

<success_criteria>
1. project.private.config.json 不在 git 索引中，本地文件保留，.gitignore 永久排除 (HYG-01)
2. app.json pages 不含 demo-ui，demo-ui 文件已删除 (HYG-02)
3. sitemap.json 对 pages/admin/* 设 disallow，公开页仍 allow (HYG-03)
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-hygiene/12-01-SUMMARY.md`
</output>
