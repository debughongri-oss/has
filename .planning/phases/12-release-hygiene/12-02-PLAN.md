---
phase: 12-release-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - miniprogram/services/api.js
  - miniprogram/services/auth.js
  - miniprogram/services/bookings.js
  - miniprogram/services/profile.js
  - miniprogram/services/reviews.js
  - miniprogram/services/services.js
  - miniprogram/services/works.js
autonomous: true
requirements: [HYG-04, HYG-05]
granularity: standard

must_haves:
  truths:
    - "errCode 成功/失败判定有且仅有一个判定点（api.js 的 isApiError），api.js 与所有 services/*.js 不再各自用不同写法判定"
    - "api.js 不再弹 wx.showToast，错误以 reject 上抛；调用方（页面）负责用户可见的错误提示"
    - "任意一次失败的云函数调用，用户至多看到一条 toast（api.js 与调用方不再叠加）"
    - "services/*.js 中不再出现 if (result.errCode !== 0) 的冗余守卫——它们信任 api.js 已 reject 失败结果"
  artifacts:
    - path: "miniprogram/services/api.js"
      provides: "单一 errCode 判定点（isApiError 导出）+ 纯传输层（无 toast）"
      exports: ["callCloudFunction", "isApiError"]
      not_contains: "wx.showToast"
    - path: "miniprogram/services/bookings.js"
      provides: "信任 api.js 契约的精简服务层"
      not_contains: "errCode !== 0"
    - path: "miniprogram/services/works.js"
      provides: "信任 api.js 契约的精简服务层"
      not_contains: "errCode !== 0"
    - path: "miniprogram/services/reviews.js"
      provides: "信任 api.js 契约的精简服务层"
      not_contains: "errCode !== 0"
  key_links:
    - from: "miniprogram/services/api.js callCloudFunction"
      to: "所有 services/*.js 调用方"
      via: "reject 失败结果（携带 errMsg）；resolve 仅成功结果（errCode===0）"
      pattern: "callCloudFunction"
    - from: "页面 .catch(err)"
      to: "wx.showToast"
      via: "页面捕获 rejected promise 后负责展示错误"
      pattern: "showToast.*err.message"
---

<objective>
统一 errCode 响应契约并消除重复错误提示：让 api.js 成为唯一的成功/失败判定点（单一 envelope：errCode===0 为成功），api.js 不再弹 toast（纯传输层），由调用页面负责用户可见的错误提示，从而保证每次失败至多一条 toast。

Purpose: HYG-04 消除 `if(errCode)`（api.js，truthy 判定）与 `if(errCode!==0)`（services，显式判定）的契约分歧——两者对 undefined 的处理相反，是潜在 bug 源；HYG-05 消除 api.js 弹 toast 与页面 catch 弹 toast 叠加出现的重复提示。

Output: api.js 重构为纯传输 + isApiError 导出；6 个 service 文件移除冗余 errCode 守卫；页面错误提示归属明确。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 关键契约：云函数返回 envelope 与客户端现状。执行者据此直接改，无需探索代码库。 -->

## 服务端 errCode envelope（已核实，所有 7 个云函数一致）
所有云函数返回：
- 成功：`{ errCode: 0, data: ... }`
- 失败：`{ errCode: -1, errMsg: '中文错误描述' }`
（grep 已确认 bookings/login/profile/reviews/services/works 全部遵循；booking-reminder 仅 errCode:0。无其他 errCode 值，除 works/index.js:152 的 `error.errCode || -1` 传播。）

## 当前 api.js（miniprogram/services/api.js，42 行）— 待重构
```javascript
const callCloudFunction = async (name, data = {}) => {
  try {
    const res = await wx.cloud.callFunction({ name, data })
    const result = res.result || {}
    // 统一错误处理
    if (result.errCode) {                              // ← truthy 判定（HYG-04 问题点）
      console.error(`云函数 ${name} 错误:`, result.errMsg || result.errCode)
      wx.showToast({ title: result.errMsg || '请求失败，请重试', icon: 'none', duration: 2000 })  // ← HYG-05 问题点①
      return Promise.reject(result)
    }
    return result
  } catch (error) {
    console.error(`云函数 ${name} 调用失败:`, error)
    wx.showToast({ title: '网络异常，请重试', icon: 'none', duration: 2000 })  // ← HYG-05 问题点②
    return Promise.reject(error)
  }
}
```

## services/*.js 现状（冗余守卫，待移除）
每个 service 方法形如（以 bookings.js 为例）：
```javascript
const createBooking = async (data) => {
  const result = await callCloudFunction('bookings', { action: 'create', ...data })
  if (result.errCode !== 0) throw new Error(result.errMsg)   // ← 冗余：api.js 已 reject 失败结果，此处 result 必为成功
  return result.data
}
```
受影响文件与守卫数量（grep 已核实）：
- miniprogram/services/services.js — 6 处 `if (result.errCode !== 0)`
- miniprogram/services/works.js — 6 处
- miniprogram/services/bookings.js — 8 处
- miniprogram/services/reviews.js — 4 处
- miniprogram/services/profile.js — 2 处
- miniprogram/services/auth.js — 3 处（silentLogin/refreshUserInfo，含 errCode===0 分支，需特殊处理见下）

## auth.js 特殊处理（miniprogram/services/auth.js）
auth.js 的 errCode 检查不是简单守卫，需分别处理：
- `silentLogin`（第 42 行）：`if (result.errCode !== 0) throw new Error(result.errMsg || '登录失败')` → 移除守卫，直接 `_userInfo = result.data; return _userInfo`
- `refreshUserInfo`（第 78 行）：`if (result.errCode === 0) { _userInfo = result.data; return _userInfo } throw new Error(result.errMsg)` → 简化为 `_userInfo = result.data; return _userInfo`（api.js 已保证成功才 resolve）

## 页面 catch + toast 现状（已 grep，HYG-05 归属层）
页面普遍已有自己的 catch toast（带上下文消息，如"保存失败"/"提交失败"），例如：
- booking/create.js:355 → `wx.showToast({ title: err.message || '提交失败', icon: 'none' })`
- admin/services/edit.js:98 → `wx.showToast({ title: '保存失败', icon: 'none' })`
- review/create.js:101 → `wx.showToast({ title: err.message || '提交失败' })`
这些页面 toast 与 api.js 的 toast 叠加 = 重复提示（HYG-05）。移除 api.js 的 toast 后，页面保留自己的 = 单一提示。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HYG-04 + HYG-05 核心 — 重构 api.js 为单一判定点 + 纯传输层（无 toast）</name>
  <files>miniprogram/services/api.js</files>
  <read_first>
    - miniprogram/services/api.js（当前 42 行，待重构）
  </read_first>
  <action>
    重写 `miniprogram/services/api.js`，使其成为：(1) 唯一的 errCode 成功/失败判定点；(2) 纯传输层——不弹任何 toast，错误以 rejected Promise 上抛。

    新 api.js 完整内容：
    ```javascript
    /**
     * 集中式 API 层 — 所有云函数调用统一经过此模块
     * Pattern from ARCHITECTURE.md: Centralized API Layer
     *
     * HYG-04 (Phase 12): isApiError 是 errCode 契约的【唯一判定点】
     *   envelope: { errCode: 0 为成功, errCode !== 0 为失败 }
     * HYG-05 (Phase 12): api.js 为纯传输层，不弹 toast；错误由调用方（页面）负责提示
     */

    /**
     * 判定云函数响应是否为错误（errCode 契约的单一判定点）
     * 成功 = errCode === 0；失败 = errCode !== 0（含 undefined/null）
     * @param {Object} result - 云函数返回的 result 对象
     * @returns {boolean}
     */
    const isApiError = (result) => {
      return !result || result.errCode !== 0
    }

    /**
     * 调用云函数（纯传输层，不弹 toast）
     * @param {string} name - 云函数名称
     * @param {Object} data - 传递数据
     * @returns {Promise<Object>} 成功时 resolve 完整 result（含 data）；失败时 reject Error（含 .errCode/.errMsg/.message）
     */
    const callCloudFunction = async (name, data = {}) => {
      try {
        const res = await wx.cloud.callFunction({ name, data })
        const result = res.result || {}

        // HYG-04: 单一判定点
        if (isApiError(result)) {
          const err = new Error(result.errMsg || '请求失败')
          err.errCode = result.errCode
          err.errMsg = result.errMsg || '请求失败'
          throw err
        }

        return result
      } catch (error) {
        // 已处理的业务错误直接上抛（携带 errCode/errMsg）
        if (error && error.errCode !== undefined) {
          throw error
        }
        // 未处理的异常（网络错误等）包装为通用错误上抛
        console.error(`云函数 ${name} 调用失败:`, error)
        throw new Error('网络异常，请重试')
      }
    }

    module.exports = {
      callCloudFunction,
      isApiError
    }
    ```

    关键变更说明（对照旧代码）：
    - 新增 `isApiError(result)` 导出 = 单一判定点（HYG-04）。语义：`!result || result.errCode !== 0`。注意用显式 `!== 0` 而非旧代码的 truthy `if(result.errCode)`——两者对 undefined 的判定相反，显式版本正确（undefined !== 0 === true = 判为错误）。
    - 移除两处 `wx.showToast`（HYG-05）：业务错误分支的 toast 与 catch 分支的"网络异常"toast 全部删除。api.js 不再负责用户可见提示。
    - 失败时 reject 一个 `Error` 对象，其 `.message` = errMsg，并附带 `.errCode`/`.errMsg` 字段，方便调用方按需读取。
    - catch 分支区分「已处理业务错误」（有 errCode 字段）与「未处理异常」（网络错误等）：前者直接上抛，后者包装为"网络异常，请重试"。
  </action>
  <verify>
    <automated>cd /Users/works/project-codes/learns/hzs && node -e "const m=require('./miniprogram/services/api.js'); if(typeof m.callCloudFunction!=='function'||typeof m.isApiError!=='function')process.exit(1); if(!m.isApiError({errCode:-1}))process.exit(1); if(!m.isApiError(undefined))process.exit(1); if(m.isApiError({errCode:0}))process.exit(1); console.log('OK')" && ! grep -c "wx.showToast" miniprogram/services/api.js | grep -qv 0</automated>
  </verify>
  <acceptance_criteria>
    - `miniprogram/services/api.js` contains `const isApiError`
    - `miniprogram/services/api.js` contains `module.exports` with both `callCloudFunction` and `isApiError`
    - `grep -c "wx.showToast" miniprogram/services/api.js` returns 0（纯传输层，无 toast）
    - `node -e "require('./miniprogram/services/api.js').isApiError({errCode:-1})"`  evaluates truthy
    - `node -e "require('./miniprogram/services/api.js').isApiError({errCode:0})"` evaluates falsy
    - `node -e "require('./miniprogram/services/api.js').isApiError(undefined)"` evaluates truthy
  </acceptance_criteria>
  <done>api.js 导出 isApiError 单一判定点；无 wx.showToast；失败 reject 携带 errCode/errMsg 的 Error；isApiError 对 errCode:-1 与 undefined 返回 true，对 errCode:0 返回 false。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HYG-04 迁移 — 移除 services/*.js 的冗余 errCode 守卫 + HYG-05 验证页面错误归属</name>
  <files>miniprogram/services/auth.js, miniprogram/services/bookings.js, miniprogram/services/profile.js, miniprogram/services/reviews.js, miniprogram/services/services.js, miniprogram/services/works.js</files>
  <read_first>
    - miniprogram/services/api.js（Task 1 已重构，确认 isApiError 契约：失败 reject、成功 resolve）
    - miniprogram/services/bookings.js（86 行，8 处守卫，作为模板）
    - miniprogram/services/auth.js（132 行，silentLogin 第 42 行、refreshUserInfo 第 78 行需特殊处理）
    - miniprogram/services/works.js, reviews.js, services.js, profile.js（逐一核对守卫位置）
  </read_first>
  <action>
    **第一部分：移除 6 个 service 文件的冗余 errCode 守卫（HYG-04 单一判定点）**

    原理：api.js 的 callCloudFunction 现在保证——失败时 reject（不会 resolve 到 service），成功时 resolve 的 result 必有 errCode===0。因此 service 里的 `if (result.errCode !== 0) throw ...` 永远不会触发，是死代码，移除它以消除契约分歧。

    对每个普通 service 方法，做如下机械变换：
    ```javascript
    // 变换前
    const getXxx = async (...) => {
      const result = await callCloudFunction('xxx', { action: '...', ... })
      if (result.errCode !== 0) throw new Error(result.errMsg)
      return result.data
    }
    // 变换后
    const getXxx = async (...) => {
      const result = await callCloudFunction('xxx', { action: '...', ... })
      return result.data
    }
    ```

    逐文件处理（保持文件其余部分如 getStatusLabel/getStatusColor 等纯函数不变）：
    - **services.js**（6 处）：getServicesList/getAllServices/getServiceDetail/createService/updateService/deleteService 各删 1 行守卫
    - **works.js**（6 处）：getWorksList/getWorkDetail/createWork/updateWork/deleteWork + 1 处（逐一删除 `if (result.errCode !== 0)` 块）
    - **bookings.js**（8 处）：getAvailableSlots/createBooking/getBookingsList/getMyBookings/getBookingDetail/updateBookingStatus/cancelBooking/getCalendarData 各删守卫，保留 getStatusLabel/getStatusColor 纯函数
    - **reviews.js**（4 处）：逐一删除守卫
    - **profile.js**（2 处）：逐一删除守卫
    - **auth.js**（3 处，特殊处理）：
      - `silentLogin`：删除 `if (result.errCode !== 0) { throw new Error(result.errMsg || '登录失败') }`，保留 `_userInfo = result.data || {}; return _userInfo`
      - `refreshUserInfo`：将 `if (result.errCode === 0) { _userInfo = result.data; return _userInfo } throw new Error(result.errMsg)` 简化为 `_userInfo = result.data; return _userInfo`（成功才到达此处，无需分支）

    **第二部分：HYG-05 验证页面错误归属（只读审计 + 必要补全）**

    api.js 不再弹 toast 后，所有用户可见错误提示依赖页面的 `.catch()`。执行只读审计：
    ```
    # 列出所有调用 service 方法处，检查是否在 .catch() 中有 toast
    grep -rn "Service\.\|services\." miniprogram/pages --include="*.js" | grep -v miniprogram_npm
    ```
    对每个用户主动发起的操作（提交预约/保存作品/评价提交/审核操作等），确认其 `.catch()` 中有 `wx.showToast`。对于已在后台静默加载且 .catch 仅 console.error 的（如首页 loadFeaturedWorks），保持静默是可接受的（非用户主动操作，不打扰）。

    若发现某个**用户主动操作**缺少 catch toast（依赖了旧 api.js 的 toast），补一行：
    ```javascript
    .catch(err => { wx.showToast({ title: err.message || '操作失败', icon: 'none' }) })
    ```
    根据现有 grep 结果，主要页面（booking/create, admin/services/edit, review/create, admin/works/edit, admin/bookings/detail, profile/history）均已自带 catch toast，预期无需大量补全——以审计确认为准，补全真正缺失的。

    注意：不要移除页面现有的 catch toast——它们现在是唯一的错误提示来源（HYG-05 归属页面层）。
  </action>
  <verify>
    <automated>cd /Users/works/project-codes/learns/hzs && echo "=== 残留冗余守卫（应为空）===" && grep -rn "errCode !== 0" miniprogram/services/*.js; echo "=== api.js 仍是唯一判定点 ===" && grep -rn "isApiError\|errCode !== 0\|errCode === 0" miniprogram/services/*.js | grep -v "api.js"; echo "=== 6 service 文件仍 require api ===" && grep -l "require('./api')\|require('../api')\|require('../../services/api')" miniprogram/services/*.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "errCode !== 0" miniprogram/services/*.js` 仅匹配 api.js 中的 isApiError 实现（即除 api.js 外的 6 个 service 文件无任何 `errCode !== 0`）
    - `miniprogram/services/bookings.js` 不含字符串 `errCode !== 0`
    - `miniprogram/services/works.js` 不含字符串 `errCode !== 0`
    - `miniprogram/services/reviews.js` 不含字符串 `errCode !== 0`
    - `miniprogram/services/auth.js` 中 silentLogin 不含 `errCode !== 0` 守卫、refreshUserInfo 不含 `errCode === 0` 分支
    - `grep -rn "isApiError" miniprogram/services/api.js` 命中（唯一判定点存在）
    - 每个被用户主动调用的 service 方法（createBooking/saveService/submitReview 等）的页面调用处，其 Promise 链含 `.catch` 且 catch 体内含 `showToast`（人工/grep 抽检 booking/create.js、admin/services/edit.js、review/create.js）
  </acceptance_criteria>
  <done>6 个 service 文件无冗余 errCode 守卫，api.js isApiError 是唯一判定点；页面层是唯一 toast 归属层；无用户主动操作静默失败。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 云函数响应 → 客户端 api.js | errCode envelope 在此边界被解析，判定点决定后续控制流 |
| api.js → 页面 .catch | 失败以 rejected Promise 跨越此边界，页面负责用户提示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Repudiation | errCode 契约分歧（api.js truthy vs services 显式） | mitigate | HYG-04: isApiError 单一判定点用显式 `!== 0`，对 undefined 一致判为错误，消除 `if(errCode)` 对 undefined 误判为成功的分歧。 |
| T-12-04 | Denial of Service (UX) | 重复 toast 干扰用户 | mitigate | HYG-05: api.js 移除 toast，错误归属页面层，每次失败至多一条 toast。 |
</threat_model>

<verification>
- `node -e "require('./miniprogram/services/api.js')"` 可加载（无语法错误）
- `grep -rn "errCode !== 0" miniprogram/services/*.js` 仅命中 api.js
- `grep -c "wx.showToast" miniprogram/services/api.js` = 0
- 抽检：booking/create.js、admin/services/edit.js、review/create.js 的提交逻辑 catch 仍含 toast（页面层归属）
- 人工验证（微信开发者工具，非阻塞）：触发一次失败调用（如断网提交预约），确认仅出现一条 toast
</verification>

<success_criteria>
1. api.js 导出 isApiError 作为 errCode 唯一判定点，无 wx.showToast (HYG-04 + HYG-05)
2. 6 个 service 文件移除冗余 errCode 守卫，信任 api.js 契约 (HYG-04)
3. 页面层是唯一 toast 归属层，无用户主动操作静默失败 (HYG-05)
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-hygiene/12-02-SUMMARY.md`
</output>
