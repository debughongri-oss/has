# Deploy secrets — NEVER commit these
.deploy-key/
*.key

# Auto-synced shared/ inside each cloudfunction (regenerate with `npm run sync`)
# 源在 cloudfunctions/shared/，副本是部署前生成的，不要直接编辑
cloudfunctions/*/shared/

# Node
node_modules/

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# 微信开发者工具本地配置（含本地环境 ID / 编译设置，不应入库）
project.private.config.json
project.config.json

# 本地工具产物（playwright MCP 截图/snapshot，HTTP server 日志等）
.playwright-mcp/
redesign-cover.jpeg
redesign-cover.png
